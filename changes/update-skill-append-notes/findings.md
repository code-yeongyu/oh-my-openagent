## Findings
- Updated `src/features/builtin-skills/creating-changes/SKILL.md` to include instructions for using the `Edit` tool to append to `findings.md` and `progress.md` files.
- This prevents agents from accidentally overwriting these critical log files using the `Write` tool.
- Typecheck passed successfully.