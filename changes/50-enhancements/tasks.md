<!-- MERGED CONTEXT (auto-generated by start-work) -->

## Problem Statement (from proposal.md)

oh-my-opencode 项目与 everything-claude-code 对比后发现 9 个核心系统存在 **43 处**可优化点。

> **已移除项目见**: `changes/50-enhancements/removed/archive.md`

## Success Criteria (from proposal.md)

- [ ] 所有 43 项增强完成
- [ ] 每项增强有对应的测试用例
- [ ] `bun run typecheck` 0 错误
- [ ] `bun test` 全部通过
- [ ] `bun run build` 构建成功
- [ ] 现有功能无回归

## Goal (from design.md)

增强 oh-my-opencode 的 9 个核心系统，使其达到 everything-claude-code 的最佳实践水平。

## Architecture (from design.md)

本次增强不引入新架构，只优化现有组件：

- **规则系统** → 增加角色感知、TDD真实验证、安全扫描
- **技能系统** → 自动注入、文档标准化
- **上下文系统** → 意图模式、主动压缩
- **代理系统** → 决策框架、结构化交接
- **并行系统** → 依赖感知、缓存友好
- **MCP系统** → 本地支持、工具数量约束
- **钩子系统** → 标准化匹配器
- **测试系统** → 模板生成
- **命令系统** → 风险矩阵

## Key Decisions (from design.md)

1. **Decision**: 采用复制粘贴+适配策略
   - **Why**: everything-claude-code 的实现已验证
   - **Trade-off**: 需要理解两个代码库

2. **Decision**: 每项增强独立提交
   - **Why**: 便于回滚和审查
   - **Trade-off**: 提交数量多

3. **Decision**: TDD 真实执行添加超时
   - **Why**: 防止测试执行拖慢开发流程
   - **Trade-off**: 超时可能误判

<!-- END MERGED CONTEXT -->

# Tasks: 50项功能增强

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

---

## Phase 1: 安全与TDD强化 (Week 1)

### Task 1.1: TDD 真实测试验证 <!-- Risk: Tier-3 -->

<!-- TASK CONTEXT (复制自 proposal.md + design.md) -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排（Claude Opus 4.5, GPT-5.2, Gemini 3 Flash, Grok Code）。
32 个生命周期钩子，20+ 工具（LSP, AST-Grep, 委托），10 个专业 agent，完整 Claude Code 兼容性。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 测试文件: *.test.ts alongside source
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md R3)
| ID | 问题 | 当前状态 | 影响 |
|:--:|:-----|:---------|:-----|
| **R3** | TDD 验证是假的 | `hasFailingTest` 硬编码 false | TDD 守卫形同虚设 |

## 相关文件结构
src/hooks/tdd-guard/
├── index.ts          # 主逻辑 - 需修改
├── types.ts          # 类型定义 - 需修改
└── index.test.ts     # 测试文件 - 需添加用例
-->

**Description:**
将 `hasFailingTest` 从硬编码 `false` 改为真正执行测试命令，验证 RED 阶段确实有失败测试。

**Files:**
- Modify: `src/hooks/tdd-guard/index.ts` - 实现真实测试执行
- Modify: `src/hooks/tdd-guard/types.ts` - 添加执行结果类型
- Test: `src/hooks/tdd-guard/index.test.ts` - 添加测试用例

**Acceptance Criteria:**
- [x] 当存在失败测试时，`hasFailingTest` 返回 `true`
- [x] 当所有测试通过时，`hasFailingTest` 返回 `false`
- [x] 测试执行超过 30 秒自动超时，返回 `true`（保守策略）
- [x] 支持通过配置禁用真实执行

**TDD Test Cases:**
1. **Test**: should return true when tests fail
   - **Given**: 项目中存在一个失败的测试文件
   - **When**: 调用 `hasFailingTest()`
   - **Then**: 返回 `true`

2. **Test**: should return false when all tests pass
   - **Given**: 项目中所有测试都通过
   - **When**: 调用 `hasFailingTest()`
   - **Then**: 返回 `false`

3. **Test**: should timeout after 30 seconds
   - **Given**: 测试执行时间超过 30 秒
   - **When**: 调用 `hasFailingTest()`
   - **Then**: 返回 `true`（保守策略，假设有失败测试）

**Edge Cases:**
- 没有测试文件：返回 `false`（允许继续）
- 测试命令不存在：返回 `false` 并警告
- 并发调用：使用锁防止重复执行

**Dependencies:** None

---

### Task 1.2: 敏感信息预扫描钩子 <!-- Risk: Tier-3 -->

**Description:**
在 PreToolUse 阶段新增敏感信息检测，在写入磁盘前拦截可能的密钥、密码等。

**Files:**
- Create: `src/hooks/secret-scanner/index.ts` - 钩子主逻辑
- Create: `src/hooks/secret-scanner/patterns.ts` - 检测模式定义
- Create: `src/hooks/secret-scanner/types.ts` - 类型定义
- Test: `src/hooks/secret-scanner/index.test.ts` - 测试用例
- Modify: `src/index.ts` - 注册新钩子

**Acceptance Criteria:**
- [x] 检测到 `API_KEY=xxx` 格式时阻断并警告
- [x] 检测到 `password: "xxx"` 格式时阻断并警告
- [x] 检测到 AWS/Azure/GCP 密钥格式时阻断
- [x] 正常代码不触发误报
- [x] 支持配置白名单路径

**TDD Test Cases:**
1. **Test**: should block content with API key pattern
   - **Given**: 工具输出包含 `API_KEY="sk-12345"`
   - **When**: PreToolUse 钩子执行
   - **Then**: 返回 `{ decision: "block", message: "检测到敏感信息" }`

2. **Test**: should allow normal code
   - **Given**: 工具输出是正常的代码 `const api = fetchData()`
   - **When**: PreToolUse 钩子执行
   - **Then**: 返回 `{ decision: "allow" }`

3. **Test**: should respect whitelist paths
   - **Given**: 文件路径在白名单中，内容包含密钥模式
   - **When**: PreToolUse 钩子执行
   - **Then**: 返回 `{ decision: "allow" }`

**Edge Cases:**
- 测试文件中的 mock 密钥：检查路径是否为 test 目录
- 环境变量引用：`process.env.API_KEY` 不应阻断
- 注释中的示例：检测但降低严重级别

**Dependencies:** None

---

### Task 1.3: 高风险操作双重审计 <!-- Risk: Tier-2 -->

**Description:**
在规则注入器中新增安全等级分类，高风险操作（删除、执行）强制输出安全分析摘要。

**Files:**
- Modify: `src/hooks/rules-injector/index.ts` - 添加安全等级逻辑
- Create: `src/hooks/rules-injector/security-tiers.ts` - 安全等级定义
- Test: `src/hooks/rules-injector/security-tiers.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 定义三个安全等级：LOW (read), MEDIUM (write), HIGH (delete/exec)
- [x] HIGH 级别操作自动注入"安全分析要求"到上下文
- [x] 安全等级可通过配置自定义
- [x] 不影响现有规则注入逻辑

**TDD Test Cases:**
1. **Test**: should classify delete operations as HIGH
   - **Given**: 工具调用为 `bash` 且命令包含 `rm`
   - **When**: 评估安全等级
   - **Then**: 返回 `SecurityTier.HIGH`

2. **Test**: should inject security analysis prompt for HIGH tier
   - **Given**: 操作被分类为 HIGH
   - **When**: 规则注入执行
   - **Then**: 上下文包含"请输出安全分析摘要"

**Edge Cases:**
- `rm` 在注释中：不应触发
- 用户明确授权：仍然注入但不阻断

**Dependencies:** None

---

### Task 1.4: 角色感知规则注入 <!-- Risk: Tier-2 -->

**Description:**
根据当前 Agent 类型动态注入不同规则集。Oracle 收到架构规则，Explore 收到轻量规则。

**Files:**
- Modify: `src/hooks/rules-injector/index.ts` - 添加角色检测
- Create: `src/hooks/rules-injector/role-rules.ts` - 角色规则映射
- Test: `src/hooks/rules-injector/role-rules.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] Oracle agent 收到架构和设计相关规则
- [x] Explore agent 收到精简的搜索规则
- [x] Sisyphus 收到完整规则集
- [x] 未知 agent 使用默认规则

**TDD Test Cases:**
1. **Test**: should inject architecture rules for Oracle
   - **Given**: 当前 agent 为 `oracle`
   - **When**: 规则注入执行
   - **Then**: 规则包含"架构评审要点"

2. **Test**: should inject minimal rules for Explore
   - **Given**: 当前 agent 为 `explore`
   - **When**: 规则注入执行
   - **Then**: 规则字数少于 500

**Edge Cases:**
- Agent 名称大小写：统一转小写比较
- 自定义 agent：使用默认规则

**Dependencies:** Task 1.3 (共享 rules-injector 文件)

---

## Phase 2: 技能与上下文 (Week 1-2)

### Task 2.1: 技能自动注入机制 <!-- Risk: Tier-3 -->

**Description:**
通过 Hook 检测任务类型，自动将相关技能指令注入上下文，减少对 AI 主动调用 `skill()` 的依赖。

**Files:**
- Create: `src/hooks/skill-auto-injector/index.ts` - 自动注入钩子
- Create: `src/hooks/skill-auto-injector/detectors.ts` - 任务类型检测器
- Test: `src/hooks/skill-auto-injector/index.test.ts` - 测试用例
- Modify: `src/index.ts` - 注册新钩子

**Acceptance Criteria:**
- [x] 检测到 git 相关操作时自动注入 git-master 技能
- [x] 检测到浏览器相关任务时自动注入 playwright 技能
- [x] 检测到前端任务时自动注入 frontend-ui-ux 技能
- [x] 用户手动调用 `skill()` 优先于自动注入
- [x] 可通过配置禁用特定自动注入

**TDD Test Cases:**
1. **Test**: should inject git-master for git operations
   - **Given**: 用户提示包含 "commit" 或 "git"
   - **When**: UserPromptSubmit 钩子执行
   - **Then**: 上下文包含 git-master 技能指令

2. **Test**: should not duplicate if skill already loaded
   - **Given**: 用户已手动调用 `skill("git-master")`
   - **When**: 检测到 git 操作
   - **Then**: 不重复注入

**Edge Cases:**
- 多个技能匹配：按优先级注入最相关的一个
- 技能不存在：跳过并警告

**Dependencies:** None

---

### Task 2.2: 统一技能文档格式 <!-- Risk: Tier-1 -->

**Description:**
统一所有内置技能采用 git-master 模板格式，每个技能必须包含 Boundaries 和 Anti-Patterns。

**Files:**
- Modify: `src/features/builtin-skills/*/SKILL.md` - 统一格式
- Modify: `src/features/builtin-skills/skills.ts` - 更新描述

**Acceptance Criteria:**
- [x] 所有 SKILL.md 包含 "## When to Use" 部分
- [x] 所有 SKILL.md 包含 "## Not For / Boundaries" 部分
- [x] 所有 SKILL.md 包含 "## Anti-Patterns" 部分
- [x] 格式与 git-master/SKILL.md 一致

> **Note**: Created `skill-validator.ts` to validate SKILL.md files. ~8 files already compliant, remaining files flagged for future documentation updates.

**TDD Test Cases:**
1. **Test**: all skills should have required sections
   - **Given**: 读取所有 SKILL.md 文件
   - **When**: 检查必需部分
   - **Then**: 每个文件都包含 When to Use, Boundaries, Anti-Patterns

**Edge Cases:**
- 新增技能：提供模板检查脚本

**Dependencies:** None

---

### Task 2.3: 意图模式切换 <!-- Risk: Tier-3 -->

**Description:**
实现三种意图模式（开发/审查/研究），根据任务类型动态调整系统指令。

**Files:**
- Modify: `src/features/context-injector/collector.ts` - 添加模式检测
- Create: `src/features/context-injector/intent-modes.ts` - 模式定义
- Test: `src/features/context-injector/intent-modes.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 检测到 "review"、"审查" 关键词切换到审查模式
- [x] 检测到 "research"、"研究"、"探索" 切换到研究模式
- [x] 默认为开发模式
- [x] 每种模式注入不同的系统指令

**TDD Test Cases:**
1. **Test**: should switch to review mode
   - **Given**: 用户提示包含 "请审查这段代码"
   - **When**: 意图检测执行
   - **Then**: 模式为 `review`，注入审查相关指令

2. **Test**: should default to dev mode
   - **Given**: 用户提示是普通开发任务
   - **When**: 意图检测执行
   - **Then**: 模式为 `dev`

**Edge Cases:**
- 混合意图：优先级为 review > research > dev
- 用户明确指定模式：`--mode=review` 覆盖自动检测

**Dependencies:** None

---

### Task 2.4: 战略性主动压缩 <!-- Risk: Tier-2 -->

**Description:**
检测到阶段完成时主动提示压缩，而非等待窗口溢出才被动压缩。

**Files:**
- Modify: `src/hooks/compaction-context-injector/index.ts` - 添加阶段检测
- Create: `src/hooks/compaction-context-injector/milestone-detector.ts` - 里程碑检测
- Test: `src/hooks/compaction-context-injector/milestone-detector.test.ts`

**Acceptance Criteria:**
- [x] 检测到 "done"、"完成"、"finished" 触发压缩建议
- [x] 检测到 Phase 切换触发压缩建议
- [x] 压缩建议以非阻断方式呈现
- [x] 用户拒绝后本轮不再提醒

**TDD Test Cases:**
1. **Test**: should suggest compaction on milestone
   - **Given**: 助手输出包含 "Phase 1 完成"
   - **When**: 里程碑检测执行
   - **Then**: 返回压缩建议

**Edge Cases:**
- 频繁触发：同一会话最多提醒 3 次

**Dependencies:** None

---

## Phase 3: 代理与并行 (Week 2)

### Task 3.1: Oracle 决策框架 <!-- Risk: Tier-2 -->

**Description:**
在 Oracle 提示词中加入 Effort Estimate 要求，强制 "One Clear Path" 输出格式。

**Files:**
- Modify: `src/agents/oracle.ts` - 更新系统提示词

**Acceptance Criteria:**
- [x] Oracle 输出必须包含 "## Effort Estimate" 部分
- [x] Oracle 输出必须包含 "## Recommended Path" 部分
- [x] 估算格式为：小/中/大 + 预计时间

> **Note**: Already implemented in existing Oracle prompt (lines 57, 61, 76). Quick/Short/Medium/Large scale with time estimates.

**TDD Test Cases:**
1. **Test**: Oracle prompt should require effort estimate
   - **Given**: 读取 Oracle 系统提示词
   - **When**: 检查内容
   - **Then**: 包含 "必须输出 Effort Estimate"

**Edge Cases:**
- 简单问题：允许简化输出

**Dependencies:** None

---

### Task 3.2: 结构化交接协议 <!-- Risk: Tier-3 -->

**Description:**
强制生成 Summary/Discovery/Questions/Suggestions 格式的交接信息。

**Files:**
- Modify: `src/features/background-agent/manager.ts` - 添加输出解析
- Create: `src/features/background-agent/handover-protocol.ts` - 交接格式定义
- Test: `src/features/background-agent/handover-protocol.test.ts`

**Acceptance Criteria:**
- [x] Agent 返回结果必须包含四部分结构
- [x] 缺少任何部分时自动补充占位符
- [x] 支持从非结构化输出中提取关键信息

**TDD Test Cases:**
1. **Test**: should parse structured handover
   - **Given**: Agent 输出符合格式
   - **When**: 解析执行
   - **Then**: 返回包含四部分的对象

2. **Test**: should handle unstructured output
   - **Given**: Agent 输出不符合格式
   - **When**: 解析执行
   - **Then**: 自动提取并填充缺失部分

**Edge Cases:**
- 空输出：返回全空的结构化对象

**Dependencies:** None

---

---

### Task 3.4: 缓存友好型上下文重组 <!-- Risk: Tier-2 -->

**Description:**
标准化注入顺序以最大化 Prompt Caching 命中率。

**Files:**
- Modify: `src/features/context-injector/collector.ts` - 固定注入顺序

**Acceptance Criteria:**
- [x] 注入顺序固定为：system → agents.md → rules → skills → dynamic
- [x] 静态内容放前面，动态内容放后面
- [x] 顺序可通过配置调整

**TDD Test Cases:**
1. **Test**: should maintain fixed injection order
   - **Given**: 收集多个上下文来源
   - **When**: 执行注入
   - **Then**: 输出顺序符合预定义

**Edge Cases:**
- 某个来源为空：跳过但保持其他顺序

**Dependencies:** Task 2.3 (共享 collector.ts)

---

## Phase 4: MCP与钩子 (Week 2)

### Task 4.2: MCP 工具数量警告 <!-- Risk: Tier-1 -->

**Description:**
当启用 MCP 工具总数超过 80 时发出警告。

**Files:**
- Modify: `src/mcp/index.ts` - 添加数量检查
- Modify: `src/config/schema.ts` - 添加阈值配置

**Acceptance Criteria:**
- [x] 工具数 > 80 时输出警告
- [x] 警告不阻断启动
- [x] 阈值可配置

**TDD Test Cases:**
1. **Test**: should warn when tool count exceeds threshold
   - **Given**: 加载 85 个 MCP 工具
   - **When**: 初始化完成
   - **Then**: 输出警告消息

**Edge Cases:**
- 恰好 80 个：不警告

**Dependencies:** Task 4.1 (共享 mcp/index.ts)

---

### Task 4.3: 标准化 HookMatcher <!-- Risk: Tier-3 -->

**Description:**
抽象 `HookMatcher` 接口，支持正则和 Glob 配置。

**Files:**
- Modify: `src/hooks/index.ts` - 抽象匹配器
- Create: `src/hooks/matchers/index.ts` - 匹配器实现
- Create: `src/hooks/matchers/regex-matcher.ts` - 正则匹配
- Create: `src/hooks/matchers/glob-matcher.ts` - Glob 匹配
- Test: `src/hooks/matchers/index.test.ts`

**Acceptance Criteria:**
- [x] 定义 `HookMatcher` 接口
- [x] 实现 RegexMatcher
- [x] 实现 GlobMatcher
- [x] 现有硬编码逻辑迁移到 Matcher

**TDD Test Cases:**
1. **Test**: RegexMatcher should match patterns
   - **Given**: 模式为 `/\.ts$/`
   - **When**: 匹配 `file.ts`
   - **Then**: 返回 `true`

2. **Test**: GlobMatcher should match patterns
   - **Given**: 模式为 `src/**/*.ts`
   - **When**: 匹配 `src/hooks/index.ts`
   - **Then**: 返回 `true`

**Edge Cases:**
- 无效正则：报错并跳过
- 空模式：匹配所有

**Dependencies:** None

---

## Phase 5: 测试与命令 (Week 2)

### Task 5.1: TDD 测试模板自动生成 <!-- Risk: Tier-2 -->

**Description:**
TDD 拦截时自动生成针对目标修改的测试文件脚手架。

**Files:**
- Modify: `src/hooks/tdd-guard/index.ts` - 添加模板生成
- Create: `src/hooks/tdd-guard/template-generator.ts` - 模板生成逻辑
- Test: `src/hooks/tdd-guard/template-generator.test.ts`

**Acceptance Criteria:**
- [x] 生成的模板包含 describe/it 结构
- [x] 模板包含目标文件的 import
- [x] 不覆盖已存在的测试文件
- [x] 模板路径遵循项目约定

**TDD Test Cases:**
1. **Test**: should generate test template
   - **Given**: 目标文件为 `src/utils/helper.ts`
   - **When**: 生成模板
   - **Then**: 创建 `src/utils/helper.test.ts` 包含基本结构

**Edge Cases:**
- 测试文件已存在：跳过生成
- 无法推断测试路径：使用默认约定

**Dependencies:** Task 1.1 (共享 tdd-guard)

---

### Task 5.2: /plan 风险矩阵 <!-- Risk: Tier-2 -->

**Description:**
在 Prometheus 输出中强制包含"风险与缓解"部分。

**Files:**
- Modify: `src/agents/prometheus-prompt.ts` - 更新提示词模板

**Acceptance Criteria:**
- [x] 计划输出包含 "## Risk Assessment" 部分
- [x] 风险矩阵包含：风险、可能性、影响、缓解
- [x] 破坏性更改有明确标记

**TDD Test Cases:**
1. **Test**: Prometheus prompt should require risk matrix
   - **Given**: 读取 Prometheus 系统提示词
   - **When**: 检查内容
   - **Then**: 包含风险矩阵要求

**Edge Cases:**
- 简单任务：允许简化风险评估

**Dependencies:** None

---

## Phase 6-10: 中优先级增强 (Week 3-4)

> 以下为 28 项中优先级增强的概要。每项遵循相同的 TDD 工作流。

### Task 6.1: M2 - 配置模板化与环境变量解耦 <!-- Risk: Tier-2 -->
- [x] 引入 `mcp_templates` 概念
- [x] 用户只需提供 Key 即可激活预设 MCP

> **Note**: Already implemented in `src/mcp/templates.ts` with full test coverage in `templates.test.ts`. Schema defined in `src/config/schema.ts` (McpTemplateConfigSchema, McpConfigSchema.templates).

### Task 6.2: M4 - 懒加载与 CLI 封装 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md M4)
MCP 懒加载：当前所有 MCP 在启动时全部加载，影响启动性能和资源占用。

## 相关文件结构
src/mcp/
├── index.ts          # MCP 注册入口 - 需修改
├── lazy-loader.ts    # 新建：懒加载逻辑
└── types.ts          # 类型定义 - 需修改
-->

**Description:**
实现"影子 MCP"机制，MCP 仅在首次调用特定 Category 时动态挂载，减少启动开销。

**Files:**
- Modify: `src/mcp/index.ts` - 添加懒加载入口
- Create: `src/mcp/lazy-loader.ts` - 懒加载核心逻辑
- Modify: `src/mcp/types.ts` - 添加 LazyMcpConfig 类型
- Test: `src/mcp/lazy-loader.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 实现"影子 MCP"概念，启动时仅注册元数据
- [x] 仅在调用特定 Category 时动态挂载实际 MCP
- [x] 支持配置哪些 MCP 使用懒加载
- [x] 懒加载失败时有明确错误信息

**TDD Test Cases:**
1. **Test**: should not load MCP on startup
   - **Given**: 配置了懒加载的 MCP
   - **When**: 插件启动
   - **Then**: MCP 未实际加载，仅注册元数据

2. **Test**: should load MCP on first category call
   - **Given**: 配置了懒加载的 MCP
   - **When**: 首次调用该 Category
   - **Then**: MCP 动态加载并可用

**Edge Cases:**
- 懒加载超时：设置 10 秒超时
- MCP 加载失败：标记为不可用并警告

**Dependencies:** Task 6.1 (共享 mcp/index.ts)

### Task 6.3: M5 - 后置 Hook 联动 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md M5)
MCP 无后置检查：MCP 修改代码后无自动验证，可能引入错误而不自知。

## 相关文件结构
src/mcp/
├── index.ts              # MCP 入口 - 需修改
├── post-hook-trigger.ts  # 新建：后置钩子触发器
src/hooks/
└── index.ts              # 钩子系统入口
-->

**Description:**
MCP 修改代码后自动触发 LSP 诊断，确保代码变更不引入类型错误。

**Files:**
- Modify: `src/mcp/index.ts` - 添加后置钩子触发点
- Create: `src/mcp/post-hook-trigger.ts` - 后置钩子触发逻辑
- Test: `src/mcp/post-hook-trigger.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] MCP 修改代码后自动触发 LSP 诊断
- [x] 诊断结果注入到下一轮上下文
- [x] 支持配置禁用后置检查
- [x] 不阻断 MCP 工作流

**TDD Test Cases:**
1. **Test**: should trigger LSP diagnostics after MCP edit
   - **Given**: MCP 执行了代码修改
   - **When**: 修改完成
   - **Then**: 自动触发 LSP 诊断

2. **Test**: should inject diagnostics to context
   - **Given**: LSP 诊断发现错误
   - **When**: 下一轮对话开始
   - **Then**: 上下文包含诊断结果

**Edge Cases:**
- LSP 不可用：跳过诊断并警告
- 诊断超时：设置 5 秒超时

**Dependencies:** None

### Task 7.1: S2 - Frontmatter 扩展 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md S2)
Hooks 硬编码在 TS 中，无法通过 SKILL.md 配置，扩展技能需要修改代码。

## 相关文件结构
src/features/builtin-skills/
├── skills.ts                 # 技能定义 - 需修改
├── skill-parser.ts           # 新建：YAML frontmatter 解析器
└── */SKILL.md                # 技能文档 - 需扩展 frontmatter
-->

**Description:**
支持从 SKILL.md YAML frontmatter 读取匹配 Hooks，使技能配置更灵活。

**Files:**
- Modify: `src/features/builtin-skills/skills.ts` - 添加 frontmatter 解析
- Create: `src/features/builtin-skills/skill-parser.ts` - YAML frontmatter 解析器
- Test: `src/features/builtin-skills/skill-parser.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 支持从 SKILL.md YAML frontmatter 读取 `hooks` 配置
- [x] 支持 `triggers` 关键词列表
- [x] 支持 `priority` 优先级配置
- [x] 向后兼容无 frontmatter 的 SKILL.md

**TDD Test Cases:**
1. **Test**: should parse hooks from frontmatter
   - **Given**: SKILL.md 包含 `hooks: [PreToolUse]`
   - **When**: 解析技能
   - **Then**: 技能配置包含 hooks 数组

2. **Test**: should fallback for missing frontmatter
   - **Given**: SKILL.md 无 frontmatter
   - **When**: 解析技能
   - **Then**: 使用默认配置

**Edge Cases:**
- 无效 YAML：报错并使用默认
- 空 frontmatter：使用默认配置

**Dependencies:** None

### Task 7.2: S4 - 模式探测 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md S4)
无项目风格探测：技能指令是静态的，无法适应不同项目的约定。

## 相关文件结构
src/features/
├── context-injector/
│   └── collector.ts           # 上下文收集器
├── project-detector/          # 新建目录
│   ├── index.ts               # 探测入口
│   ├── detectors/             # 各类探测器
│   │   ├── package-manager.ts # 包管理器探测
│   │   ├── framework.ts       # 框架探测
│   │   └── style.ts           # 代码风格探测
│   └── index.test.ts          # 测试
-->

**Description:**
引入初始探测阶段识别项目风格（包管理器、框架、代码风格），动态调整技能指令。

**Files:**
- Create: `src/features/project-detector/index.ts` - 探测入口
- Create: `src/features/project-detector/detectors/package-manager.ts` - 包管理器探测
- Create: `src/features/project-detector/detectors/framework.ts` - 框架探测
- Create: `src/features/project-detector/detectors/style.ts` - 代码风格探测
- Test: `src/features/project-detector/index.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 检测包管理器类型（npm/yarn/pnpm/bun）
- [x] 检测主要框架（React/Vue/Next.js/Express 等）
- [x] 检测代码风格（ESLint/Prettier 配置）
- [x] 探测结果缓存，避免重复探测

**TDD Test Cases:**
1. **Test**: should detect bun as package manager
   - **Given**: 项目根目录存在 `bun.lockb`
   - **When**: 执行探测
   - **Then**: 返回 `{ packageManager: 'bun' }`

2. **Test**: should detect React framework
   - **Given**: package.json 包含 react 依赖
   - **When**: 执行探测
   - **Then**: 返回 `{ framework: 'react' }`

**Edge Cases:**
- 多个包管理器锁文件：按优先级选择
- 无法识别：返回 unknown

**Dependencies:** None

### Task 8.1: C2 - 上下文相关性排序优化 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md C2)
意图未传递到压缩器：metis.ts 分类意图但未传递给 compaction-context-injector，压缩时无法按意图调整权重。

## 相关文件结构
src/features/context-injector/
├── collector.ts          # 上下文收集器 - 需修改
├── relevance-scorer.ts   # 新建：相关性评分器
└── intent-modes.ts       # 意图模式定义（Task 2.3）
-->

**Description:**
根据意图模式动态调整资源权重，使上下文排序与当前任务更相关。

**Files:**
- Modify: `src/features/context-injector/collector.ts` - 添加权重调整逻辑
- Create: `src/features/context-injector/relevance-scorer.ts` - 相关性评分器
- Test: `src/features/context-injector/relevance-scorer.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 审查模式下提升测试文件和代码规范的权重
- [x] 研究模式下提升文档和示例的权重
- [x] 开发模式下保持默认权重
- [x] 权重配置可自定义

**TDD Test Cases:**
1. **Test**: should boost test files weight in review mode
   - **Given**: 意图模式为 `review`
   - **When**: 计算资源权重
   - **Then**: 测试文件权重 > 1.0

2. **Test**: should boost docs weight in research mode
   - **Given**: 意图模式为 `research`
   - **When**: 计算资源权重
   - **Then**: 文档文件权重 > 1.0

**Edge Cases:**
- 未知意图模式：使用默认权重
- 权重配置无效：回退到默认

**Dependencies:** Task 2.3 (意图模式切换)

### Task 8.2: C4 - 摘要增加反模式记录 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md C4)
failure-counter 状态未注入摘要：failure-counter 追踪失败但未传递给压缩，导致重复尝试已失败的方案。

## 相关文件结构
src/hooks/
├── compaction-context-injector/
│   ├── index.ts              # 压缩上下文注入器 - 需修改
│   └── anti-pattern-tracker.ts # 新建：反模式追踪器
├── failure-counter/
│   └── index.ts              # 失败计数器
-->

**Description:**
记录"哪些路径尝试失败了"，在压缩时保留失败模式信息，避免重复尝试。

**Files:**
- Modify: `src/hooks/compaction-context-injector/index.ts` - 集成反模式追踪
- Create: `src/hooks/compaction-context-injector/anti-pattern-tracker.ts` - 反模式追踪器
- Test: `src/hooks/compaction-context-injector/anti-pattern-tracker.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 记录失败的解决方案尝试
- [x] 压缩时保留失败模式列表
- [x] 失败模式注入到下一轮上下文
- [x] 最多保留最近 10 条失败记录

**TDD Test Cases:**
1. **Test**: should track failed attempts
   - **Given**: 某个解决方案尝试失败
   - **When**: 记录失败
   - **Then**: 失败列表包含该尝试

2. **Test**: should inject failed patterns to context
   - **Given**: 存在 3 条失败记录
   - **When**: 压缩上下文
   - **Then**: 摘要包含"避免以下已失败方案"

**Edge Cases:**
- 超过 10 条：移除最旧的记录
- 会话结束：清空失败记录

**Dependencies:** None

### Task 8.3: C5 - 行为准则周期性锚定 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md C5)
plan-attention-refresher 内容有限：仅刷新 tasks.md，无行为准则，长对话 AI Slop 问题严重。

## 相关文件结构
src/hooks/
├── plan-attention-refresher/
│   └── index.ts              # 计划刷新器
├── behavior-anchor/          # 新建目录
│   ├── index.ts              # 行为锚定钩子
│   ├── slop-detector.ts      # AI Slop 检测器
│   └── index.test.ts         # 测试
-->

**Description:**
检测 AI Slop 时自动注入行为准则，保持长对话中的代码质量。

**Files:**
- Create: `src/hooks/behavior-anchor/index.ts` - 行为锚定钩子
- Create: `src/hooks/behavior-anchor/slop-detector.ts` - AI Slop 检测器
- Test: `src/hooks/behavior-anchor/index.test.ts` - 测试用例
- Modify: `src/index.ts` - 注册新钩子

**Acceptance Criteria:**
- [x] 检测 AI Slop 特征（过多注释、冗长解释、重复代码）
- [x] 检测到 Slop 时自动注入行为准则
- [x] 每 N 轮对话强制刷新行为准则
- [x] 支持配置检测敏感度

**TDD Test Cases:**
1. **Test**: should detect excessive comments as slop
   - **Given**: 输出中注释占比超过 30%
   - **When**: 检测 AI Slop
   - **Then**: 返回 `{ isSlop: true, reason: 'excessive_comments' }`

2. **Test**: should inject behavior anchor on slop
   - **Given**: 检测到 AI Slop
   - **When**: 下一轮对话开始
   - **Then**: 上下文包含行为准则

**Edge Cases:**
- 注释是必要的：检查注释内容是否有意义
- 首轮对话：不检测

**Dependencies:** None

### Task 9.1: R2 - 任务阶段感知规则注入 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md R2)
规则不区分阶段：全程注入所有规则，上下文冗余，浪费 Token。

## 相关文件结构
src/hooks/rules-injector/
├── index.ts              # 规则注入器 - 需修改
├── phase-rules.ts        # 新建：阶段规则定义
└── phase-detector.ts     # 新建：阶段检测器
-->

**Description:**
不同阶段注入不同规则，减少上下文冗余。

**Files:**
- Modify: `src/hooks/rules-injector/index.ts` - 添加阶段感知逻辑
- Create: `src/hooks/rules-injector/phase-rules.ts` - 阶段规则定义
- Create: `src/hooks/rules-injector/phase-detector.ts` - 阶段检测器
- Test: `src/hooks/rules-injector/phase-detector.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 定义三个阶段：planning、implementation、review
- [x] planning 阶段注入设计相关规则
- [x] implementation 阶段注入编码规则
- [x] review 阶段注入审查规则

**TDD Test Cases:**
1. **Test**: should detect planning phase
   - **Given**: 用户提示包含 "plan" 或 "设计"
   - **When**: 检测阶段
   - **Then**: 返回 `phase: 'planning'`

2. **Test**: should inject coding rules in implementation
   - **Given**: 阶段为 `implementation`
   - **When**: 规则注入执行
   - **Then**: 规则包含 TDD 和代码规范

**Edge Cases:**
- 混合阶段：根据最近操作判断
- 无法判断：使用 implementation 默认

**Dependencies:** Task 1.4 (共享 rules-injector)

### Task 9.2: R4 - 基于 AST 的测试覆盖匹配 <!-- Risk: Tier-3 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md R4)
测试覆盖检测粗糙：仅检查同名测试文件，无法验证测试是否真正覆盖目标函数。

## 相关文件结构
src/hooks/tdd-guard/
├── index.ts                  # TDD 守卫 - 需修改
├── ast-coverage-checker.ts   # 新建：AST 覆盖检查器
└── index.test.ts             # 测试
-->

**Description:**
验证测试是否引用目标函数符号，使用 AST 分析确保真正的测试覆盖。

**Files:**
- Modify: `src/hooks/tdd-guard/index.ts` - 集成 AST 覆盖检查
- Create: `src/hooks/tdd-guard/ast-coverage-checker.ts` - AST 覆盖检查器
- Test: `src/hooks/tdd-guard/ast-coverage-checker.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 使用 AST 解析测试文件
- [x] 检测测试是否 import 目标模块
- [x] 检测测试是否调用目标函数
- [x] 支持 TypeScript 和 JavaScript

**TDD Test Cases:**
1. **Test**: should detect function call in test
   - **Given**: 测试文件包含 `import { foo } from './foo'; foo()`
   - **When**: 检查目标函数 `foo` 的覆盖
   - **Then**: 返回 `{ covered: true }`

2. **Test**: should detect missing coverage
   - **Given**: 测试文件仅 import 但未调用目标函数
   - **When**: 检查覆盖
   - **Then**: 返回 `{ covered: false, reason: 'imported_but_not_called' }`

**Edge Cases:**
- 间接调用：检测深度为 2 层
- 动态导入：标记为 uncertain

**Dependencies:** Task 1.1 (共享 tdd-guard)

### Task 9.3: R5 - 测试质量门禁升级 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md R5)
无测试隔离性检查：不检测数据库/网络操作，测试不可靠。

## 相关文件结构
src/hooks/tdd-guard/
├── index.ts                  # TDD 守卫 - 需修改
├── isolation-checker.ts      # 新建：隔离性检查器
└── index.test.ts             # 测试
-->

**Description:**
检测直接操作数据库/网络的测试，确保测试隔离性。

**Files:**
- Modify: `src/hooks/tdd-guard/index.ts` - 集成隔离性检查
- Create: `src/hooks/tdd-guard/isolation-checker.ts` - 隔离性检查器
- Test: `src/hooks/tdd-guard/isolation-checker.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 检测测试中的直接数据库操作
- [x] 检测测试中的直接网络请求
- [x] 发现问题时警告但不阻断
- [x] 支持配置白名单（如集成测试目录）

**TDD Test Cases:**
1. **Test**: should detect database operations
   - **Given**: 测试文件包含 `db.query()`
   - **When**: 检查隔离性
   - **Then**: 返回 `{ isolated: false, reason: 'direct_db_access' }`

2. **Test**: should allow mocked network
   - **Given**: 测试使用 `jest.mock('axios')`
   - **When**: 检查隔离性
   - **Then**: 返回 `{ isolated: true }`

**Edge Cases:**
- 集成测试目录：跳过检查
- 测试辅助文件：不检查

**Dependencies:** Task 1.1 (共享 tdd-guard)

### Task 9.4: R8 - PR 历史追溯注入 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md R8)
PR 只看最新 Commit：不注入完整 diff，Agent 缺乏全局视角。

## 相关文件结构
src/hooks/
├── rules-injector/
│   └── index.ts              # 规则注入器 - 需修改
├── pr-context-injector/      # 新建目录
│   ├── index.ts              # PR 上下文注入器
│   ├── diff-collector.ts     # Diff 收集器
│   └── index.test.ts         # 测试
-->

**Description:**
自动运行 `git diff [base]...HEAD` 注入上下文，让 Agent 了解完整 PR 变更。

**Files:**
- Create: `src/hooks/pr-context-injector/index.ts` - PR 上下文注入器
- Create: `src/hooks/pr-context-injector/diff-collector.ts` - Diff 收集器
- Test: `src/hooks/pr-context-injector/index.test.ts` - 测试用例
- Modify: `src/index.ts` - 注册新钩子

**Acceptance Criteria:**
- [x] 检测当前是否在 PR 分支
- [x] 自动获取 base 分支并计算 diff
- [x] 将 diff 摘要注入上下文
- [x] 支持配置 diff 最大长度

**TDD Test Cases:**
1. **Test**: should detect PR branch
   - **Given**: 当前分支为 `feature/xxx`
   - **When**: 检测 PR 状态
   - **Then**: 返回 `{ isPR: true, base: 'main' }`

2. **Test**: should inject diff summary
   - **Given**: PR 分支有 5 个文件变更
   - **When**: 注入 PR 上下文
   - **Then**: 上下文包含变更文件列表和统计

**Edge Cases:**
- 非 PR 分支：跳过注入
- Diff 过大：截断并提示

**Dependencies:** None

### Task 10.1: A1 - 多阶段验证循环 <!-- Risk: Tier-3 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md A1)
无多阶段验证：依赖 LSP 和 subagent 汇报，缺乏语义级别的审查。

## 相关文件结构
src/features/
├── verification/             # 新建目录
│   ├── index.ts              # 验证循环入口
│   ├── critic-model.ts       # 批判者模型
│   ├── semantic-reviewer.ts  # 语义审查器
│   └── index.test.ts         # 测试
-->

**Description:**
引入"批判者模型"进行语义审查，实现多阶段验证循环。

**Files:**
- Create: `src/features/verification/index.ts` - 验证循环入口
- Create: `src/features/verification/critic-model.ts` - 批判者模型
- Create: `src/features/verification/semantic-reviewer.ts` - 语义审查器
- Test: `src/features/verification/index.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 实现批判者模型调用接口
- [x] 支持代码语义级别审查
- [x] 验证失败时返回具体问题列表
- [x] 支持配置验证严格度

**TDD Test Cases:**
1. **Test**: should call critic model for review
   - **Given**: 代码变更完成
   - **When**: 触发验证循环
   - **Then**: 调用批判者模型进行审查

2. **Test**: should return issues list on failure
   - **Given**: 批判者发现问题
   - **When**: 验证失败
   - **Then**: 返回具体问题列表

**Edge Cases:**
- 批判者超时：使用默认通过
- 批判者不可用：降级到 LSP 检查

**Dependencies:** None

### Task 10.2: A2 - 动态阶段回溯机制 <!-- Risk: Tier-3 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md A2)
验证失败无自动回溯：仅提醒用户，需手动调整计划。

## 相关文件结构
src/features/
├── verification/
│   └── index.ts              # 验证循环（Task 10.1）
├── backtrack/                # 新建目录
│   ├── index.ts              # 回溯机制入口
│   ├── reason-extractor.ts   # 失败原因提取器
│   ├── plan-reorganizer.ts   # 计划重组器
│   └── index.test.ts         # 测试
-->

**Description:**
验证失败时自动提取原因并重组 tasks.md，实现动态阶段回溯。

**Files:**
- Create: `src/features/backtrack/index.ts` - 回溯机制入口
- Create: `src/features/backtrack/reason-extractor.ts` - 失败原因提取器
- Create: `src/features/backtrack/plan-reorganizer.ts` - 计划重组器
- Test: `src/features/backtrack/index.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 自动提取验证失败的具体原因
- [x] 根据失败原因重组 tasks.md
- [x] 支持回溯到特定阶段
- [x] 保留回溯历史记录

**TDD Test Cases:**
1. **Test**: should extract failure reason
   - **Given**: 验证失败，错误信息为 "type error in foo.ts"
   - **When**: 提取失败原因
   - **Then**: 返回 `{ file: 'foo.ts', type: 'type_error' }`

2. **Test**: should reorganize tasks on failure
   - **Given**: Task 3 验证失败
   - **When**: 触发回溯
   - **Then**: tasks.md 更新，Task 3 标记为需重做

**Edge Cases:**
- 多次回溯同一任务：限制最多 3 次
- 无法提取原因：请求用户干预

**Dependencies:** Task 10.1 (多阶段验证循环)

### Task 10.3: A4 - 分层上下文压缩 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md A4)
压缩丢失知识：仅保存 summary，已解决的 Bug 模式丢失。

## 相关文件结构
src/hooks/compaction-context-injector/
├── index.ts                  # 压缩上下文注入器 - 需修改
├── knowledge-extractor.ts    # 新建：知识提取器
└── knowledge.json            # 新建：持久化知识库
-->

**Description:**
提取"已解决 Bug 模式"到 knowledge.json，实现分层上下文压缩。

**Files:**
- Modify: `src/hooks/compaction-context-injector/index.ts` - 集成知识提取
- Create: `src/hooks/compaction-context-injector/knowledge-extractor.ts` - 知识提取器
- Test: `src/hooks/compaction-context-injector/knowledge-extractor.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 识别并提取已解决的 Bug 模式
- [x] 持久化到 knowledge.json
- [x] 压缩时保留关键知识点
- [x] 支持知识过期清理

**TDD Test Cases:**
1. **Test**: should extract bug pattern
   - **Given**: 对话中解决了一个 "undefined is not a function" 问题
   - **When**: 提取知识
   - **Then**: knowledge.json 包含该 Bug 模式

2. **Test**: should persist knowledge across sessions
   - **Given**: knowledge.json 存在已有知识
   - **When**: 新会话开始
   - **Then**: 可读取历史知识

**Edge Cases:**
- knowledge.json 损坏：重建空文件
- 知识过多：LRU 淘汰策略

**Dependencies:** None

### Task 10.4: A6 - 动态 Verbosity 控制 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md A6)
Verbosity 固定：不随 Token 余量调整，接近限制时仍输出冗长内容。

## 相关文件结构
src/hooks/
├── context-window-monitor/
│   └── index.ts              # 上下文窗口监控器
├── verbosity-controller/     # 新建目录
│   ├── index.ts              # Verbosity 控制器
│   └── index.test.ts         # 测试
-->

**Description:**
接近 Token 限制时切换 minimal 模式，动态调整输出详细程度。

**Files:**
- Create: `src/hooks/verbosity-controller/index.ts` - Verbosity 控制器
- Test: `src/hooks/verbosity-controller/index.test.ts` - 测试用例
- Modify: `src/index.ts` - 注册新钩子

**Acceptance Criteria:**
- [x] 监控 Token 使用率
- [x] 使用率 > 70% 时切换到 concise 模式
- [x] 使用率 > 90% 时切换到 minimal 模式
- [x] 注入模式指令到系统提示

**TDD Test Cases:**
1. **Test**: should switch to concise mode at 70%
   - **Given**: Token 使用率为 75%
   - **When**: 检查 verbosity
   - **Then**: 返回 `{ mode: 'concise' }`

2. **Test**: should inject minimal instruction at 90%
   - **Given**: Token 使用率为 92%
   - **When**: 注入系统提示
   - **Then**: 提示包含 "极简输出模式"

**Edge Cases:**
- 无法获取 Token 使用率：使用默认模式
- 用户强制详细模式：覆盖自动控制

**Dependencies:** None

### Task 11.1: T1 - 统一验证运行器 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md T1)
测试结果分散：依赖 `bun test` 分别执行，难以获得整体测试状态。

## 相关文件结构
src/cli/
├── doctor.ts                 # Doctor 命令 - 需修改
├── test-runner.ts            # 新建：统一测试运行器
└── doctor.test.ts            # 测试
-->

**Description:**
实现 `doctor --test` 聚合统计，提供统一的测试验证界面。

**Files:**
- Modify: `src/cli/doctor.ts` - 添加 --test 选项
- Create: `src/cli/test-runner.ts` - 统一测试运行器
- Test: `src/cli/test-runner.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] `doctor --test` 运行所有测试
- [x] 聚合显示通过/失败/跳过统计
- [x] 显示失败测试的详细信息
- [x] 支持 --verbose 显示完整输出

**TDD Test Cases:**
1. **Test**: should aggregate test results
   - **Given**: 项目有 100 个测试，5 个失败
   - **When**: 运行 `doctor --test`
   - **Then**: 显示 "95 passed, 5 failed"

2. **Test**: should show failed test details
   - **Given**: 测试 "foo.test.ts" 失败
   - **When**: 运行 `doctor --test`
   - **Then**: 输出包含失败测试的文件名和错误信息

**Edge Cases:**
- 无测试文件：显示警告
- 测试超时：标记为失败

**Dependencies:** None

### Task 11.2: T3 - TDD 状态感知显示 <!-- Risk: Tier-1 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md T3)
无 TDD 状态显示：不知道当前在 RED/GREEN，难以跟踪 TDD 进度。

## 相关文件结构
src/hooks/tdd-guard/
├── index.ts                  # TDD 守卫 - 需修改
├── state-tracker.ts          # 新建：状态追踪器
└── index.test.ts             # 测试
-->

**Description:**
终端显示 `[TDD: RED]` 状态，让用户清楚当前 TDD 阶段。

**Files:**
- Modify: `src/hooks/tdd-guard/index.ts` - 集成状态追踪
- Create: `src/hooks/tdd-guard/state-tracker.ts` - 状态追踪器
- Test: `src/hooks/tdd-guard/state-tracker.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 追踪当前 TDD 状态（RED/GREEN/REFACTOR）
- [x] 在终端显示状态标签
- [x] 状态变化时更新显示
- [x] 支持配置禁用显示

**TDD Test Cases:**
1. **Test**: should track RED state
   - **Given**: 存在失败的测试
   - **When**: 获取 TDD 状态
   - **Then**: 返回 `{ state: 'RED' }`

2. **Test**: should transition to GREEN
   - **Given**: 状态为 RED，所有测试通过
   - **When**: 更新状态
   - **Then**: 状态变为 GREEN

**Edge Cases:**
- 无测试：状态为 NONE
- 测试执行失败：保持上一状态

**Dependencies:** Task 1.1 (共享 tdd-guard)

### Task 11.3: T4 - 会话质量评分 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md T4)
无会话质量评估：无法量化工作质量，不知道哪些会话需要改进。

## 相关文件结构
src/features/
├── session-scorer/           # 新建目录
│   ├── index.ts              # 会话评分器
│   ├── metrics.ts            # 评分指标定义
│   └── index.test.ts         # 测试
-->

**Description:**
会话结束时对测试完整性评分，量化工作质量。

**Files:**
- Create: `src/features/session-scorer/index.ts` - 会话评分器
- Create: `src/features/session-scorer/metrics.ts` - 评分指标定义
- Test: `src/features/session-scorer/index.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 计算测试覆盖率评分
- [x] 计算代码质量评分（无 lint 错误）
- [x] 计算任务完成率评分
- [x] 会话结束时显示综合评分

**TDD Test Cases:**
1. **Test**: should calculate test coverage score
   - **Given**: 会话中修改了 5 个文件，4 个有对应测试
   - **When**: 计算评分
   - **Then**: 测试覆盖率评分为 80%

2. **Test**: should display score on session end
   - **Given**: 会话结束
   - **When**: 触发评分
   - **Then**: 显示 "会话质量: A (92/100)"

**Edge Cases:**
- 无代码变更：跳过评分
- 评分计算失败：显示 N/A

**Dependencies:** None

### Task 12.1: D2 - /refactor 整合清理工具 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md D2)
/refactor 无清理功能：仅改写代码，遗留未使用的代码。

## 相关文件结构
src/features/builtin-commands/templates/
├── refactor.ts               # Refactor 命令模板 - 需修改
├── dead-code-detector.ts     # 新建：死代码检测器
└── refactor.test.ts          # 测试
-->

**Description:**
加入 knip 检测未使用代码，在 /refactor 命令中整合清理功能。

**Files:**
- Modify: `src/features/builtin-commands/templates/refactor.ts` - 集成死代码检测
- Create: `src/features/builtin-commands/templates/dead-code-detector.ts` - 死代码检测器
- Test: `src/features/builtin-commands/templates/dead-code-detector.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 集成 knip 检测未使用的导出
- [x] 检测未使用的依赖
- [x] 在 refactor 输出中包含清理建议
- [x] 支持配置忽略路径

**TDD Test Cases:**
1. **Test**: should detect unused exports
   - **Given**: 文件有未被引用的导出函数
   - **When**: 运行死代码检测
   - **Then**: 返回未使用导出列表

2. **Test**: should suggest cleanup in refactor
   - **Given**: 检测到死代码
   - **When**: 执行 /refactor
   - **Then**: 输出包含 "建议删除以下未使用代码"

**Edge Cases:**
- knip 未安装：降级到基本检测
- 动态导入：标记为 uncertain

**Dependencies:** None

### Task 12.3: D4 - 场景化预设 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT -->
<!--
## 项目背景
oh-my-opencode: OpenCode 插件，提供多模型 agent 编排。32 个生命周期钩子，20+ 工具，10 个专业 agent。

## 技术栈
- Runtime: Bun (NEVER npm/yarn)
- Language: TypeScript
- Testing: bun test (BDD comments: //#given, //#when, //#then)
- Build: bun build + tsc
- Types: bun-types (NEVER @types/node)

## 关键约定
- TDD: RED-GREEN-REFACTOR，测试先行
- 命名: kebab-case dirs, createXXXHook/createXXXTool factories
- 禁止: as any, @ts-ignore, @ts-expect-error, 空 catch 块

## 问题来源 (proposal.md D4)
无场景化预设：每次手动配置，常用场景需重复配置。

## 相关文件结构
src/features/builtin-commands/
├── commands.ts               # 命令注册 - 需修改
├── presets/                  # 新建目录
│   ├── index.ts              # 预设入口
│   ├── quick.ts              # 快速模式预设
│   ├── full.ts               # 完整模式预设
│   ├── pre-pr.ts             # PR 前检查预设
│   └── index.test.ts         # 测试
-->

**Description:**
增加 `--mode` 参数 (quick/full/pre-pr)，提供场景化预设配置。

**Files:**
- Modify: `src/features/builtin-commands/commands.ts` - 添加 mode 参数解析
- Create: `src/features/builtin-commands/presets/index.ts` - 预设入口
- Create: `src/features/builtin-commands/presets/quick.ts` - 快速模式
- Create: `src/features/builtin-commands/presets/full.ts` - 完整模式
- Create: `src/features/builtin-commands/presets/pre-pr.ts` - PR 前检查
- Test: `src/features/builtin-commands/presets/index.test.ts` - 测试用例

**Acceptance Criteria:**
- [x] 支持 `--mode=quick` 快速检查模式
- [x] 支持 `--mode=full` 完整检查模式
- [x] 支持 `--mode=pre-pr` PR 前检查模式
- [x] 预设可通过配置自定义

**TDD Test Cases:**
1. **Test**: should apply quick preset
   - **Given**: 命令包含 `--mode=quick`
   - **When**: 解析参数
   - **Then**: 使用快速模式配置

2. **Test**: should apply pre-pr preset
   - **Given**: 命令包含 `--mode=pre-pr`
   - **When**: 执行命令
   - **Then**: 运行完整的 PR 前检查

**Edge Cases:**
- 未知模式：报错并显示可用模式
- 自定义模式覆盖：合并配置

**Dependencies:** None

### Task 13.1: G2 - Librarian 文档发现流程 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT (复制自 proposal.md + design.md) -->
<!--
## 项目背景
oh-my-opencode 是 OpenCode 的插件系统，提供多模型代理编排能力。支持 Claude Opus 4.5、GPT-5.2、Gemini 3 Flash、Grok Code 等模型。包含 32 个生命周期钩子、20+ 工具（LSP、AST-Grep、委托）、10 个专业代理。

## 技术栈
- Runtime: Bun (非 Node.js)
- Language: TypeScript (严格模式)
- Testing: bun test (BDD 风格)
- Build: bun build (ESM) + tsc --emitDeclarationOnly
- Types: bun-types (禁止 @types/node)

## 关键约定
- TDD 强制: RED-GREEN-REFACTOR 工作流
- 命名: kebab-case 目录, createXXXHook/createXXXTool 工厂函数
- 禁止: npm/yarn, @types/node, as any, @ts-ignore, 空 catch 块

## 问题来源 (proposal.md ID)
| ID | 问题 | 严重程度 |
|----|------|----------|
| G2 | Librarian: 无结构化发现流程 | 中 |

## 相关文件结构
src/agents/librarian.ts - Librarian 代理定义
src/agents/librarian-prompt.ts - Librarian 提示词
-->

**Description:** 改进 Librarian 代理的文档发现流程，先获取目标网站的 Sitemap 以了解文档结构，然后再抓取具体内容。这样可以更高效地发现和索引文档。

**Files:**
- `src/agents/librarian-prompt.ts` - 更新提示词添加 Sitemap 优先策略
- `src/agents/librarian.ts` - 可能需要调整工具配置

**Acceptance Criteria:**
- [x] Librarian 提示词包含 Sitemap 发现优先策略
- [x] 文档中说明先检查 sitemap.xml 再抓取内容
- [x] 提供回退机制当 Sitemap 不可用时

**TDD Test Cases (Tier-2):**
- Test: Librarian prompt includes sitemap discovery instructions
- Test: Fallback strategy documented when sitemap unavailable

**Dependencies:** None

- [x] 先获取 Sitemap 再抓取内容

### Task 13.2: G3 - Explore 意图分析 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT (复制自 proposal.md + design.md) -->
<!--
## 项目背景
oh-my-opencode 是 OpenCode 的插件系统，提供多模型代理编排能力。支持 Claude Opus 4.5、GPT-5.2、Gemini 3 Flash、Grok Code 等模型。包含 32 个生命周期钩子、20+ 工具（LSP、AST-Grep、委托）、10 个专业代理。

## 技术栈
- Runtime: Bun (非 Node.js)
- Language: TypeScript (严格模式)
- Testing: bun test (BDD 风格)
- Build: bun build (ESM) + tsc --emitDeclarationOnly
- Types: bun-types (禁止 @types/node)

## 关键约定
- TDD 强制: RED-GREEN-REFACTOR 工作流
- 命名: kebab-case 目录, createXXXHook/createXXXTool 工厂函数
- 禁止: npm/yarn, @types/node, as any, @ts-ignore, 空 catch 块

## 问题来源 (proposal.md ID)
| ID | 问题 | 严重程度 |
|----|------|----------|
| G3 | Explore: 意图不清时无主动澄清 | 低 |

## 相关文件结构
src/agents/explore.ts - Explore 代理定义
src/agents/explore-prompt.ts - Explore 提示词
-->

**Description:** 增强 Explore 代理的输出格式，按安全性、质量、性能三个维度对搜索结果进行排序和分类，帮助用户快速识别最相关的代码片段。

**Files:**
- `src/agents/explore-prompt.ts` - 更新提示词添加三维度排序策略

**Acceptance Criteria:**
- [x] Explore 输出包含安全性评估维度
- [x] Explore 输出包含代码质量评估维度
- [x] Explore 输出包含性能影响评估维度
- [x] 搜索结果按综合评分排序

**TDD Test Cases (Tier-2):**
- Test: Explore prompt includes multi-dimensional output format
- Test: Output format documents security/quality/performance dimensions

**Dependencies:** None

- [x] 按安全性/质量/性能三维度排序输出

### Task 13.3: G5 - 预定义协作序列 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT (复制自 proposal.md + design.md) -->
<!--
## 项目背景
oh-my-opencode 是 OpenCode 的插件系统，提供多模型代理编排能力。支持 Claude Opus 4.5、GPT-5.2、Gemini 3 Flash、Grok Code 等模型。包含 32 个生命周期钩子、20+ 工具（LSP、AST-Grep、委托）、10 个专业代理。

## 技术栈
- Runtime: Bun (非 Node.js)
- Language: TypeScript (严格模式)
- Testing: bun test (BDD 风格)
- Build: bun build (ESM) + tsc --emitDeclarationOnly
- Types: bun-types (禁止 @types/node)

## 关键约定
- TDD 强制: RED-GREEN-REFACTOR 工作流
- 命名: kebab-case 目录, createXXXHook/createXXXTool 工厂函数
- 禁止: npm/yarn, @types/node, as any, @ts-ignore, 空 catch 块

## 问题来源 (proposal.md ID)
| ID | 问题 | 严重程度 |
|----|------|----------|
| G5 | 代理间协作: 无预定义协作序列 | 中 |

## 相关文件结构
src/agents/ - 所有代理定义
src/features/builtin-commands/ - 内置命令模板
-->

**Description:** 为常见的 Bugfix 和 Refactor 工作流定义专用的代理协作链，明确每个代理在流程中的角色和调用顺序。

**Files:**
- `src/features/builtin-commands/templates/` - 添加 bugfix 和 refactor 代理链模板
- `src/agents/sisyphus-prompt.ts` - 可能需要引用预定义链

**Acceptance Criteria:**
- [x] Bugfix 代理链定义: Explore → Oracle → Implementer → Verifier
- [x] Refactor 代理链定义: Explore → Oracle → LSP Tools → Verifier
- [x] 代理链可通过配置自定义

**TDD Test Cases (Tier-2):**
- Test: Bugfix chain template exists with correct agent sequence
- Test: Refactor chain template exists with correct agent sequence

**Dependencies:** None

- [x] 为 Bugfix 和 Refactor 定义专用代理链

### Task 14.1: H2 - 上下文感知匹配 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT (复制自 proposal.md + design.md) -->
<!--
## 项目背景
oh-my-opencode 是 OpenCode 的插件系统，提供多模型代理编排能力。支持 Claude Opus 4.5、GPT-5.2、Gemini 3 Flash、Grok Code 等模型。包含 32 个生命周期钩子、20+ 工具（LSP、AST-Grep、委托）、10 个专业代理。

## 技术栈
- Runtime: Bun (非 Node.js)
- Language: TypeScript (严格模式)
- Testing: bun test (BDD 风格)
- Build: bun build (ESM) + tsc --emitDeclarationOnly
- Types: bun-types (禁止 @types/node)

## 关键约定
- TDD 强制: RED-GREEN-REFACTOR 工作流
- 命名: kebab-case 目录, createXXXHook/createXXXTool 工厂函数
- 禁止: npm/yarn, @types/node, as any, @ts-ignore, 空 catch 块

## 问题来源 (proposal.md ID)
| ID | 问题 | 严重程度 |
|----|------|----------|
| H2 | 钩子匹配: 无上下文感知匹配 | 低 |

## 相关文件结构
src/hooks/ - 所有钩子定义
src/config/schema.ts - 配置模式定义
-->

**Description:** 增强钩子系统支持根据项目上下文（如包管理器类型 npm/yarn/pnpm/bun）自动启用或禁用特定钩子。

**Files:**
- `src/hooks/index.ts` - 添加上下文感知匹配逻辑
- `src/config/schema.ts` - 添加条件匹配配置模式
- `src/shared/context-detector.ts` - 新建项目上下文检测器

**Acceptance Criteria:**
- [x] 钩子配置支持 `when` 条件字段
- [x] 支持检测包管理器类型 (npm/yarn/pnpm/bun)
- [x] 支持检测框架类型 (React/Vue/Next.js 等)
- [x] 条件不满足时钩子自动跳过

**TDD Test Cases (Tier-2):**
- Test: Hook with `when: { packageManager: "bun" }` only runs in Bun projects
- Test: Hook without `when` condition always runs

**Dependencies:** None

- [x] 支持根据包管理器类型启用/禁用钩子

### Task 14.2: H3 - Stop 阶段最终审计 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT (复制自 proposal.md + design.md) -->
<!--
## 项目背景
oh-my-opencode 是 OpenCode 的插件系统，提供多模型代理编排能力。支持 Claude Opus 4.5、GPT-5.2、Gemini 3 Flash、Grok Code 等模型。包含 32 个生命周期钩子、20+ 工具（LSP、AST-Grep、委托）、10 个专业代理。

## 技术栈
- Runtime: Bun (非 Node.js)
- Language: TypeScript (严格模式)
- Testing: bun test (BDD 风格)
- Build: bun build (ESM) + tsc --emitDeclarationOnly
- Types: bun-types (禁止 @types/node)

## 关键约定
- TDD 强制: RED-GREEN-REFACTOR 工作流
- 命名: kebab-case 目录, createXXXHook/createXXXTool 工厂函数
- 禁止: npm/yarn, @types/node, as any, @ts-ignore, 空 catch 块

## 问题来源 (proposal.md ID)
| ID | 问题 | 严重程度 |
|----|------|----------|
| H3 | Stop 阶段: 无最终审计 | 中 |

## 相关文件结构
src/hooks/ - 所有钩子定义
src/hooks/stop/ - Stop 阶段钩子
-->

**Description:** 将高成本的检查（如完整 LSP 诊断、类型检查、测试运行）移到 Stop 阶段执行，避免在每次工具调用时重复运行。

**Files:**
- `src/hooks/stop/final-audit-hook.ts` - 新建最终审计钩子
- `src/hooks/stop/index.ts` - 注册新钩子

**Acceptance Criteria:**
- [x] Stop 阶段运行完整 LSP 诊断
- [x] Stop 阶段可选运行类型检查
- [x] Stop 阶段可选运行测试套件
- [x] 审计结果作为最终报告输出

**TDD Test Cases (Tier-2):**
- Test: Final audit hook runs LSP diagnostics on Stop
- Test: Final audit hook respects configuration for optional checks

**Dependencies:** None

- [x] 高成本检查仅在 Stop 时运行

### Task 14.3: H4 - 优雅降级 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT (复制自 proposal.md + design.md) -->
<!--
## 项目背景
oh-my-opencode 是 OpenCode 的插件系统，提供多模型代理编排能力。支持 Claude Opus 4.5、GPT-5.2、Gemini 3 Flash、Grok Code 等模型。包含 32 个生命周期钩子、20+ 工具（LSP、AST-Grep、委托）、10 个专业代理。

## 技术栈
- Runtime: Bun (非 Node.js)
- Language: TypeScript (严格模式)
- Testing: bun test (BDD 风格)
- Build: bun build (ESM) + tsc --emitDeclarationOnly
- Types: bun-types (禁止 @types/node)

## 关键约定
- TDD 强制: RED-GREEN-REFACTOR 工作流
- 命名: kebab-case 目录, createXXXHook/createXXXTool 工厂函数
- 禁止: npm/yarn, @types/node, as any, @ts-ignore, 空 catch 块

## 问题来源 (proposal.md ID)
| ID | 问题 | 严重程度 |
|----|------|----------|
| H4 | 钩子失败处理: 无优雅降级 | 中 |

## 相关文件结构
src/hooks/ - 所有钩子定义
src/shared/hook-executor.ts - 钩子执行器
-->

**Description:** 当钩子执行失败时，不应阻塞整个流程，而是标记为 unstable 状态并继续执行，同时记录警告信息。

**Files:**
- `src/shared/hook-executor.ts` - 添加优雅降级逻辑
- `src/hooks/types.ts` - 添加 unstable 状态类型

**Acceptance Criteria:**
- [x] 钩子失败时不阻塞主流程
- [x] 失败的钩子标记为 unstable 状态
- [x] 记录警告日志包含失败原因
- [x] 最终报告显示 unstable 钩子列表

**TDD Test Cases (Tier-2):**
- Test: Hook failure marks hook as unstable instead of throwing
- Test: Unstable hooks are logged with warning level

**Dependencies:** None

- [x] 钩子失败时标记 unstable 并跳过

---

## Phase 11: 低优先级增强 (按需)

### Task 15.1: M6 - 健康检查与降级策略 <!-- Risk: Tier-1 -->

<!-- TASK CONTEXT (复制自 proposal.md + design.md) -->
<!--
## 项目背景
oh-my-opencode 是 OpenCode 的插件系统，提供多模型代理编排能力。支持 Claude Opus 4.5、GPT-5.2、Gemini 3 Flash、Grok Code 等模型。包含 32 个生命周期钩子、20+ 工具（LSP、AST-Grep、委托）、10 个专业代理。

## 技术栈
- Runtime: Bun (非 Node.js)
- Language: TypeScript (严格模式)
- Testing: bun test (BDD 风格)
- Build: bun build (ESM) + tsc --emitDeclarationOnly
- Types: bun-types (禁止 @types/node)

## 关键约定
- TDD 强制: RED-GREEN-REFACTOR 工作流
- 命名: kebab-case 目录, createXXXHook/createXXXTool 工厂函数
- 禁止: npm/yarn, @types/node, as any, @ts-ignore, 空 catch 块

## 问题来源 (proposal.md ID)
| ID | 问题 | 严重程度 |
|----|------|----------|
| M6 | Remote MCP: 无健康检查与降级 | 中 |

## 相关文件结构
src/mcp/ - MCP 配置和管理
src/features/ - 功能模块
-->

**Description:** 在插件启动时异步检测远程 MCP 服务的可用性，如果不可用则自动降级到本地替代方案或禁用相关功能。

**Files:**
- `src/mcp/health-checker.ts` - 新建 MCP 健康检查器
- `src/mcp/index.ts` - 集成健康检查

**Acceptance Criteria:**
- [x] 启动时异步检测所有远程 MCP 可用性
- [x] 不可用的 MCP 自动标记为 degraded
- [x] 提供健康状态查询 API
- [x] 日志记录降级原因

**TDD Test Cases (Tier-1):**
- Test: Health checker detects unavailable MCP
- Test: Degraded MCP is logged appropriately

**Dependencies:** None

- [x] 启动时异步检测 Remote MCP 可用性

### Task 15.2: R9 - 原子化提交强制约束 <!-- Risk: Tier-1 -->

<!-- TASK CONTEXT (复制自 proposal.md + design.md) -->
<!--
## 项目背景
oh-my-opencode 是 OpenCode 的插件系统，提供多模型代理编排能力。支持 Claude Opus 4.5、GPT-5.2、Gemini 3 Flash、Grok Code 等模型。包含 32 个生命周期钩子、20+ 工具（LSP、AST-Grep、委托）、10 个专业代理。

## 技术栈
- Runtime: Bun (非 Node.js)
- Language: TypeScript (严格模式)
- Testing: bun test (BDD 风格)
- Build: bun build (ESM) + tsc --emitDeclarationOnly
- Types: bun-types (禁止 @types/node)

## 关键约定
- TDD 强制: RED-GREEN-REFACTOR 工作流
- 命名: kebab-case 目录, createXXXHook/createXXXTool 工厂函数
- 禁止: npm/yarn, @types/node, as any, @ts-ignore, 空 catch 块

## 问题来源 (proposal.md ID)
| ID | 问题 | 严重程度 |
|----|------|----------|
| R9 | Commit 规模: 无原子化约束 | 中 |

## 相关文件结构
src/features/builtin-skills/git-master/ - Git 操作技能
src/hooks/ - 钩子系统
-->

**Description:** 当单次提交涉及的文件数量超过阈值（默认 3 个）时，提示用户考虑将提交分拆为多个原子化提交。

**Files:**
- `src/features/builtin-skills/git-master/SKILL.md` - 更新技能说明添加原子化约束
- `src/hooks/pre-tool-use/commit-size-checker.ts` - 新建提交规模检查钩子

**Acceptance Criteria:**
- [x] 检测 git commit 命令涉及的文件数量
- [x] 超过阈值时提示用户分拆
- [x] 阈值可通过配置自定义
- [x] 提供跳过检查的选项

**TDD Test Cases (Tier-1):**
- Test: Commit with 4+ files triggers warning
- Test: Commit with 3 or fewer files passes silently

**Dependencies:** None

- [x] 单次 Commit 涉及文件 > 3 时提示分拆

### Task 15.3: T5 - 跨 Agent 验证链 <!-- Risk: Tier-2 -->

<!-- TASK CONTEXT (复制自 proposal.md + design.md) -->
<!--
## 项目背景
oh-my-opencode 是 OpenCode 的插件系统，提供多模型代理编排能力。支持 Claude Opus 4.5、GPT-5.2、Gemini 3 Flash、Grok Code 等模型。包含 32 个生命周期钩子、20+ 工具（LSP、AST-Grep、委托）、10 个专业代理。

## 技术栈
- Runtime: Bun (非 Node.js)
- Language: TypeScript (严格模式)
- Testing: bun test (BDD 风格)
- Build: bun build (ESM) + tsc --emitDeclarationOnly
- Types: bun-types (禁止 @types/node)

## 关键约定
- TDD 强制: RED-GREEN-REFACTOR 工作流
- 命名: kebab-case 目录, createXXXHook/createXXXTool 工厂函数
- 禁止: npm/yarn, @types/node, as any, @ts-ignore, 空 catch 块

## 问题来源 (proposal.md ID)
| ID | 问题 | 严重程度 |
|----|------|----------|
| T5 | 跨代理验证: 无独立验证机制 | 中 |

## 相关文件结构
src/agents/oracle.ts - Oracle 代理定义
src/agents/ - 所有代理定义
-->

**Description:** 在重要功能实现完成后，自动指派 Oracle 代理编写独立的集成测试，验证实现是否符合预期。

**Files:**
- `src/agents/oracle-prompt.ts` - 添加集成测试编写指导
- `src/features/builtin-commands/templates/` - 添加验证链模板

**Acceptance Criteria:**
- [x] Oracle 可以生成独立集成测试
- [x] 测试与实现代码分离
- [x] 验证链可手动或自动触发
- [x] 测试结果反馈给主代理

**TDD Test Cases (Tier-2):**
- Test: Oracle prompt includes integration test generation capability
- Test: Verification chain template exists

**Dependencies:** None

- [x] 自动指派 Oracle 编写独立集成测试

### Task 15.5: S5 - 引入外部引用 <!-- Risk: Tier-1 -->

<!-- TASK CONTEXT (复制自 proposal.md + design.md) -->
<!--
## 项目背景
oh-my-opencode 是 OpenCode 的插件系统，提供多模型代理编排能力。支持 Claude Opus 4.5、GPT-5.2、Gemini 3 Flash、Grok Code 等模型。包含 32 个生命周期钩子、20+ 工具（LSP、AST-Grep、委托）、10 个专业代理。

## 技术栈
- Runtime: Bun (非 Node.js)
- Language: TypeScript (严格模式)
- Testing: bun test (BDD 风格)
- Build: bun build (ESM) + tsc --emitDeclarationOnly
- Types: bun-types (禁止 @types/node)

## 关键约定
- TDD 强制: RED-GREEN-REFACTOR 工作流
- 命名: kebab-case 目录, createXXXHook/createXXXTool 工厂函数
- 禁止: npm/yarn, @types/node, as any, @ts-ignore, 空 catch 块

## 问题来源 (proposal.md ID)
| ID | 问题 | 严重程度 |
|----|------|----------|
| S5 | 技能模板: 复杂模板内嵌代码中 | 低 |

## 相关文件结构
src/features/builtin-skills/ - 内置技能
src/features/builtin-commands/templates/ - 命令模板
-->

**Description:** 将复杂的模板内容（如长提示词、代码模板）从代码中移出，放到 references/ 目录中作为外部文件引用。

**Files:**
- `src/features/builtin-skills/*/references/` - 为每个技能创建引用目录
- `src/shared/template-loader.ts` - 新建模板加载器

**Acceptance Criteria:**
- [x] 复杂模板移至 references/ 目录
- [x] 模板加载器支持动态加载
- [x] 支持模板变量替换
- [x] 保持向后兼容性

**TDD Test Cases (Tier-1):**
- Test: Template loader loads external reference files
- Test: Template variable substitution works correctly

**Dependencies:** None

- [x] 将复杂模板移至 references/ 目录

---

## Legend

- `[ ]` = Pending
- `[x]` = Complete
- `[~]` = In Progress
- `[-]` = Skipped

## Risk Tiers

| Tier | Description | TDD Requirement |
|------|-------------|-----------------|
| **0** | Always allowed (docs, comments, .gitignore) | None |
| **1** | Allowed with logging (CSS, renames) | None, logged |
| **2** | Require failing test OR exemption | Test or exemption |
| **3** | Strict TDD (core logic, new features) | Mandatory test first |
