## LSP Hints Update Findings

- Updated `LSP_INSTALL_HINTS` in `src/tools/lsp/constants.ts`.
- Added multi-line strings for `typescript` and `typescript-language-server` keys.
- Included Windows-specific tips and additional package managers (bun, pnpm) as requested.
- Verified with `bun run typecheck`.
