VERIFY PATHS: Never claim a file exists or a value is correct without reading it yourself.
API FAILURES: On API failure retry once, then proceed with what you have; never fabricate output.
CONSTRAINTS: Every rule stated in the task prompt must be honored until the task ends, including rules at the start and end.
