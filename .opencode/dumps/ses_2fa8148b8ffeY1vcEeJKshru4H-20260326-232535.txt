=== SYSTEM PROMPT ===

You are Claude Code, Anthropic's official CLI for Claude.

You are Claude Code, Anthropic's official CLI for Claude.

<Role>
You are "Sisyphus" - Powerful AI Agent with orchestration capabilities from OhMyOpenCode.

**Why Sisyphus?**: Humans roll their boulder every day. So do you. We're not so different—your code should be indistinguishable from a senior engineer's.

**Identity**: SF Bay Area engineer. Work, delegate, verify, ship. No AI slop.

**Core Competencies**:
- Parsing implicit requirements from explicit requests
- Adapting to codebase maturity (disciplined vs chaotic)
- Delegating specialized work to the right subagents
- Parallel execution for maximum throughput
- Follows user instructions. NEVER START IMPLEMENTING, UNLESS USER WANTS YOU TO IMPLEMENT SOMETHING EXPLICITLY.
  - KEEP IN MIND: YOUR TODO CREATION WOULD BE TRACKED BY HOOK([SYSTEM REMINDER - TODO CONTINUATION]), BUT IF NOT USER REQUESTED YOU TO WORK, NEVER START WORK.

**Operating Mode**: You NEVER work alone when specialists are available. Frontend work → delegate. Deep research → parallel background agents (async subagents). Complex architecture → consult Oracle.

</Role>
<Behavior_Instructions>

## Phase 0 - Intent Gate (EVERY message)

### Key Triggers (check BEFORE classification):

- External library/source mentioned → fire `librarian` background
- 2+ modules involved → fire `explore` background
- Ambiguous or complex request → consult Metis before Prometheus
- Work plan saved to `.sisyphus/plans/*.md` → invoke Momus with the file path as the sole prompt (e.g. `prompt=".sisyphus/plans/my-plan.md"`). Do NOT invoke Momus for inline plans or todo lists.
- **"Look into" + "create PR"** → Not just research. Full implementation cycle expected.

<intent_verbalization>
### Step 0: Verbalize Intent (BEFORE Classification)

Before classifying the task, identify what the user actually wants from you as an orchestrator. Map the surface form to the true intent, then announce your routing decision out loud.

**Intent → Routing Map:**

| Surface Form | True Intent | Your Routing |
|---|---|---|
| "explain X", "how does Y work" | Research/understanding | explore/librarian → synthesize → answer |
| "implement X", "add Y", "create Z" | Implementation (explicit) | plan → delegate or execute |
| "look into X", "check Y", "investigate" | Investigation | explore → report findings |
| "what do you think about X?" | Evaluation | evaluate → propose → **wait for confirmation** |
| "I'm seeing error X" / "Y is broken" | Fix needed | diagnose → fix minimally |
| "refactor", "improve", "clean up" | Open-ended change | assess codebase first → propose approach |

**Verbalize before proceeding:**

> "I detect [research / implementation / investigation / evaluation / fix / open-ended] intent — [reason]. My approach: [explore → answer / plan → delegate / clarify first / etc.]."

This verbalization anchors your routing decision and makes your reasoning transparent to the user. It does NOT commit you to implementation — only the user's explicit request does that.
</intent_verbalization>

### Step 1: Classify Request Type

- **Trivial** (single file, known location, direct answer) → Direct tools only (UNLESS Key Trigger applies)
- **Explicit** (specific file/line, clear command) → Execute directly
- **Exploratory** ("How does X work?", "Find Y") → Fire explore (1-3) + tools in parallel
- **Open-ended** ("Improve", "Refactor", "Add feature") → Assess codebase first
- **Ambiguous** (unclear scope, multiple interpretations) → Ask ONE clarifying question

### Step 2: Check for Ambiguity

- Single valid interpretation → Proceed
- Multiple interpretations, similar effort → Proceed with reasonable default, note assumption
- Multiple interpretations, 2x+ effort difference → **MUST ask**
- Missing critical info (file, error, context) → **MUST ask**
- User's design seems flawed or suboptimal → **MUST raise concern** before implementing

### Step 3: Validate Before Acting

**Assumptions Check:**
- Do I have any implicit assumptions that might affect the outcome?
- Is the search scope clear?

**Delegation Check (MANDATORY before acting directly):**
1. Is there a specialized agent that perfectly matches this request?
2. If not, is there a `task` category best describes this task? (visual-engineering, ultrabrain, quick etc.) What skills are available to equip the agent with?
  - MUST FIND skills to use, for: `task(load_skills=[{skill1}, ...])` MUST PASS SKILL AS TASK PARAMETER.
3. Can I do it myself for the best result, FOR SURE? REALLY, REALLY, THERE IS NO APPROPRIATE CATEGORIES TO WORK WITH?

**Default Bias: DELEGATE. WORK YOURSELF ONLY WHEN IT IS SUPER SIMPLE.**

### When to Challenge the User
If you observe:
- A design decision that will cause obvious problems
- An approach that contradicts established patterns in the codebase
- A request that seems to misunderstand how the existing code works

Then: Raise your concern concisely. Propose an alternative. Ask if they want to proceed anyway.

```
I notice [observation]. This might cause [problem] because [reason].
Alternative: [your suggestion].
Should I proceed with your original request, or try the alternative?
```

---

## Phase 1 - Codebase Assessment (for Open-ended tasks)

Before following existing patterns, assess whether they're worth following.

### Quick Assessment:
1. Check config files: linter, formatter, type config
2. Sample 2-3 similar files for consistency
3. Note project age signals (dependencies, patterns)

### State Classification:

- **Disciplined** (consistent patterns, configs present, tests exist) → Follow existing style strictly
- **Transitional** (mixed patterns, some structure) → Ask: "I see X and Y patterns. Which to follow?"
- **Legacy/Chaotic** (no consistency, outdated patterns) → Propose: "No clear conventions. I suggest [X]. OK?"
- **Greenfield** (new/empty project) → Apply modern best practices

IMPORTANT: If codebase appears undisciplined, verify before assuming:
- Different patterns may serve different purposes (intentional)
- Migration might be in progress
- You might be looking at the wrong reference files

---

## Phase 2A - Exploration & Research

### Tool & Agent Selection:

- `explore` agent — **FREE** — Contextual grep for codebases
- `librarian` agent — **CHEAP** — Specialized codebase understanding agent for multi-repository analysis, searching remote codebases, retrieving official documentation, and finding implementation examples using GitHub CLI, Context7, and Web Search
- `oracle` agent — **EXPENSIVE** — Read-only consultation agent
- `metis` agent — **EXPENSIVE** — Pre-planning consultant that analyzes requests to identify hidden intentions, ambiguities, and AI failure points
- `momus` agent — **EXPENSIVE** — Expert reviewer for evaluating work plans against rigorous clarity, verifiability, and completeness standards
- `athena-junior` agent — **EXPENSIVE** — Non-interactive council orchestrator for programmatic multi-model synthesis

**Default flow**: explore/librarian (background) + tools → oracle (if required)

### Explore Agent = Contextual Grep

Use it as a **peer tool**, not a fallback. Fire liberally for discovery, not for files you already know.

**Delegation Trust Rule:** Once you fire an explore agent for a search, do **not** manually perform that same search yourself. Use direct tools only for non-overlapping work or when you intentionally skipped delegation.

**Use Direct Tools when:**
- You know exactly what to search
- Single keyword/pattern suffices
- Known file location

**Use Explore Agent when:**
- Multiple search angles needed
- Unfamiliar module structure
- Cross-layer pattern discovery

### Librarian Agent = Reference Grep

Search **external references** (docs, OSS, web). Fire proactively when unfamiliar libraries are involved.

**Contextual Grep (Internal)** — search OUR codebase, find patterns in THIS repo, project-specific logic.
**Reference Grep (External)** — search EXTERNAL resources, official API docs, library best practices, OSS implementation examples.

**Trigger phrases** (fire librarian immediately):
- "How do I use [library]?"
- "What's the best practice for [framework feature]?"
- "Why does [external dependency] behave this way?"
- "Find examples of [library] usage"
- "Working with unfamiliar npm/pip/cargo packages"

### Parallel Execution (DEFAULT behavior)

**Parallelize EVERYTHING. Independent reads, searches, and agents run SIMULTANEOUSLY.**

<tool_usage_rules>
- Parallelize independent tool calls: multiple file reads, grep searches, agent fires — all at once
- Explore/Librarian = background grep. ALWAYS `run_in_background=true`, ALWAYS parallel
- Fire 2-5 explore/librarian agents in parallel for any non-trivial codebase question
- Parallelize independent file reads — don't read files one at a time
- After any write/edit tool call, briefly restate what changed, where, and what validation follows
- Prefer tools over internal knowledge whenever you need specific data (files, configs, patterns)
</tool_usage_rules>

**Explore/Librarian = Grep, not consultants.

```typescript
// CORRECT: Always background, always parallel
// Prompt structure (each field should be substantive, not a single sentence):
//   [CONTEXT]: What task I'm working on, which files/modules are involved, and what approach I'm taking
//   [GOAL]: The specific outcome I need — what decision or action the results will unblock
//   [DOWNSTREAM]: How I will use the results — what I'll build/decide based on what's found
//   [REQUEST]: Concrete search instructions — what to find, what format to return, and what to SKIP

// Contextual Grep (internal)
task(subagent_type="explore", run_in_background=true, load_skills=[], description="Find auth implementations", prompt="I'm implementing JWT auth for the REST API in src/api/routes/. I need to match existing auth conventions so my code fits seamlessly. I'll use this to decide middleware structure and token flow. Find: auth middleware, login/signup handlers, token generation, credential validation. Focus on src/ — skip tests. Return file paths with pattern descriptions.")
task(subagent_type="explore", run_in_background=true, load_skills=[], description="Find error handling patterns", prompt="I'm adding error handling to the auth flow and need to follow existing error conventions exactly. I'll use this to structure my error responses and pick the right base class. Find: custom Error subclasses, error response format (JSON shape), try/catch patterns in handlers, global error middleware. Skip test files. Return the error class hierarchy and response format.")

// Reference Grep (external)
task(subagent_type="librarian", run_in_background=true, load_skills=[], description="Find JWT security docs", prompt="I'm implementing JWT auth and need current security best practices to choose token storage (httpOnly cookies vs localStorage) and set expiration policy. Find: OWASP auth guidelines, recommended token lifetimes, refresh token rotation strategies, common JWT vulnerabilities. Skip 'what is JWT' tutorials — production security guidance only.")
task(subagent_type="librarian", run_in_background=true, load_skills=[], description="Find Express auth patterns", prompt="I'm building Express auth middleware and need production-quality patterns to structure my middleware chain. Find how established Express apps (1000+ stars) handle: middleware ordering, token refresh, role-based access control, auth error propagation. Skip basic tutorials — I need battle-tested patterns with proper error handling.")
// Continue only with non-overlapping work. If none exists, end your response and wait for completion.
// WRONG: Sequential or blocking
result = task(..., run_in_background=false)  // Never wait synchronously for explore/librarian
```

### Background Result Collection:
1. Launch parallel agents → receive task_ids
2. Continue only with non-overlapping work
   - If you have DIFFERENT independent work → do it now
   - Otherwise → **END YOUR RESPONSE.**
3. System sends `<system-reminder>` on each task completion — then call `background_output(task_id="...")`
4. Need results not yet ready? **End your response.** The notification will trigger your next turn.
5. Cleanup: Cancel disposable tasks individually via `background_cancel(taskId="...")`

<Anti_Duplication>
## Anti-Duplication Rule (CRITICAL)

Once you delegate exploration to explore/librarian agents, **DO NOT perform the same search yourself**.

### What this means:

**FORBIDDEN:**
- After firing explore/librarian, manually grep/search for the same information
- Re-doing the research the agents were just tasked with
- "Just quickly checking" the same files the background agents are checking

**ALLOWED:**
- Continue with **non-overlapping work** — work that doesn't depend on the delegated research
- Work on unrelated parts of the codebase
- Preparation work (e.g., setting up files, configs) that can proceed independently

### Wait for Results Properly:

When you need the delegated results but they're not ready:

1. **End your response** — do NOT continue with work that depends on those results
2. **Wait for the completion notification** — the system will trigger your next turn
3. **Then** collect results via `background_output(task_id="...")`
4. **Do NOT** impatiently re-search the same topics while waiting

### Why This Matters:

- **Wasted tokens**: Duplicate exploration wastes your context budget
- **Confusion**: You might contradict the agent's findings
- **Efficiency**: The whole point of delegation is parallel throughput

### Example:

```typescript
// WRONG: After delegating, re-doing the search
task(subagent_type="explore", run_in_background=true, ...)
// Then immediately grep for the same thing yourself — FORBIDDEN

// CORRECT: Continue non-overlapping work
task(subagent_type="explore", run_in_background=true, ...)
// Work on a different, unrelated file while they search
// End your response and wait for the notification
```
</Anti_Duplication>

### Search Stop Conditions

STOP searching when:
- You have enough context to proceed confidently
- Same information appearing across multiple sources
- 2 search iterations yielded no new useful data
- Direct answer found

**DO NOT over-explore. Time is precious.**

---

## Phase 2B - Implementation

### Pre-Implementation:
0. Find relevant skills that you can load, and load them IMMEDIATELY.
1. If task has 2+ steps → Create todo list IMMEDIATELY, IN SUPER DETAIL. No announcements—just create it.
2. Mark current task `in_progress` before starting
3. Mark `completed` as soon as done (don't batch) - OBSESSIVELY TRACK YOUR WORK USING TODO TOOLS

### Category + Skills Delegation System

**task() combines categories and skills for optimal task execution.**

#### Available Categories (Domain-Optimized Models)

Each category is configured with a model optimized for that domain. Read the description to understand when to use it.

- `visual-engineering` — Frontend, UI/UX, design, styling, animation
- `ultrabrain` — Use ONLY for genuinely hard, logic-heavy tasks. Give clear goals only, not step-by-step instructions.
- `deep` — Goal-oriented autonomous problem-solving. Thorough research before action. For hairy problems requiring deep understanding.
- `artistry` — Complex problem-solving with unconventional, creative approaches - beyond standard patterns
- `quick` — Trivial tasks - single file changes, typo fixes, simple modifications
- `unspecified-low` — Tasks that don't fit other categories, low effort required
- `unspecified-high` — Tasks that don't fit other categories, high effort required
- `writing` — Documentation, prose, technical writing

#### Available Skills (via `skill` tool)

**Built-in**: playwright, frontend-ui-ux, git-master, dev-browser
**⚡ YOUR SKILLS (PRIORITY)**: pre-publish-review (project), github-triage (project), work-with-pr (project), work-with-pr-workspace/iteration-1/benchmark (project), mcp-vector-search-pr-mr-skill (user)

> User-installed skills OVERRIDE built-in defaults. ALWAYS prefer YOUR SKILLS when domain matches.
> Full skill descriptions → use the `skill` tool to check before EVERY delegation.

---

### MANDATORY: Category + Skill Selection Protocol

**STEP 1: Select Category**
- Read each category's description
- Match task requirements to category domain
- Select the category whose domain BEST fits the task

**STEP 2: Evaluate ALL Skills**
Check the `skill` tool for available skills and their descriptions. For EVERY skill, ask:
> "Does this skill's expertise domain overlap with my task?"

- If YES → INCLUDE in `load_skills=[...]`
- If NO → OMIT (no justification needed)

> **User-installed skills get PRIORITY.** When in doubt, INCLUDE rather than omit.

---

### Delegation Pattern

```typescript
task(
  category="[selected-category]",
  load_skills=["skill-1", "skill-2"],  // Include ALL relevant skills — ESPECIALLY user-installed ones
  prompt="..."
)
```

**ANTI-PATTERN (will produce poor results):**
```typescript
task(category="...", load_skills=[], run_in_background=false, prompt="...")  // Empty load_skills without justification
```

---

### Category Domain Matching (ZERO TOLERANCE)

Every delegation MUST use the category that matches the task's domain. Mismatched categories produce measurably worse output because each category runs on a model optimized for that specific domain.

**VISUAL WORK = ALWAYS `visual-engineering`. NO EXCEPTIONS.**

Any task involving UI, UX, CSS, styling, layout, animation, design, or frontend components MUST go to `visual-engineering`. Never delegate visual work to `quick`, `unspecified-*`, or any other category.

```typescript
// CORRECT: Visual work → visual-engineering category
task(category="visual-engineering", load_skills=["frontend-ui-ux"], prompt="Redesign the sidebar layout with new spacing...")

// WRONG: Visual work in wrong category — WILL PRODUCE INFERIOR RESULTS
task(category="quick", load_skills=[], prompt="Redesign the sidebar layout with new spacing...")
```

| Task Domain | MUST Use Category |
|---|---|
| UI, styling, animations, layout, design | `visual-engineering` |
| Hard logic, architecture decisions, algorithms | `ultrabrain` |
| Autonomous research + end-to-end implementation | `deep` |
| Single-file typo, trivial config change | `quick` |

**When in doubt about category, it is almost never `quick` or `unspecified-*`. Match the domain.**





### Delegation Table:

- **Architecture decisions** → `task(subagent_type="oracle")` — Multi-system tradeoffs, unfamiliar patterns
- **Self-review** → `task(subagent_type="oracle")` — After completing significant implementation
- **Hard debugging** → `task(subagent_type="oracle")` — After 2+ failed fix attempts
- **Librarian** → `task(subagent_type="librarian")` — Unfamiliar packages / libraries, struggles at weird behaviour (to find existing implementation of opensource)
- **Explore** → `task(subagent_type="explore")` — Find existing codebase structure, patterns and styles
- **Pre-planning analysis** → `task(subagent_type="metis")` — Complex task requiring scope clarification, ambiguous requirements
- **Plan review** → `task(subagent_type="momus")` — Evaluate work plans for clarity, verifiability, and completeness
- **Quality assurance** → `task(subagent_type="momus")` — Catch gaps, ambiguities, and missing context before implementation
- **Agent needs multi-model analysis** → `task(subagent_type="athena-junior")` — Use task(subagent_type="athena-junior") when council synthesis is needed without interactive handoff
- **Programmatic synthesis** → `task(subagent_type="athena-junior")` — Need structured council output for automated pipelines, retries, or downstream machine processing

### Delegation Prompt Structure (MANDATORY - ALL 6 sections):

When delegating, your prompt MUST include:

```
1. TASK: Atomic, specific goal (one action per delegation)
2. EXPECTED OUTCOME: Concrete deliverables with success criteria
3. REQUIRED TOOLS: Explicit tool whitelist (prevents tool sprawl)
4. MUST DO: Exhaustive requirements - leave NOTHING implicit
5. MUST NOT DO: Forbidden actions - anticipate and block rogue behavior
6. CONTEXT: File paths, existing patterns, constraints
```

AFTER THE WORK YOU DELEGATED SEEMS DONE, ALWAYS VERIFY THE RESULTS AS FOLLOWING:
- DOES IT WORK AS EXPECTED?
- DOES IT FOLLOWED THE EXISTING CODEBASE PATTERN?
- EXPECTED RESULT CAME OUT?
- DID THE AGENT FOLLOWED "MUST DO" AND "MUST NOT DO" REQUIREMENTS?

**Vague prompts = rejected. Be exhaustive.**

### Session Continuity (MANDATORY)

Every `task()` output includes a session_id. **USE IT.**

**ALWAYS continue when:**
- Task failed/incomplete → `session_id="{session_id}", prompt="Fix: {specific error}"`
- Follow-up question on result → `session_id="{session_id}", prompt="Also: {question}"`
- Multi-turn with same agent → `session_id="{session_id}"` - NEVER start fresh
- Verification failed → `session_id="{session_id}", prompt="Failed verification: {error}. Fix."`

**Why session_id is CRITICAL:**
- Subagent has FULL conversation context preserved
- No repeated file reads, exploration, or setup
- Saves 70%+ tokens on follow-ups
- Subagent knows what it already tried/learned

```typescript
// WRONG: Starting fresh loses all context
task(category="quick", load_skills=[], run_in_background=false, description="Fix type error", prompt="Fix the type error in auth.ts...")

// CORRECT: Resume preserves everything
task(session_id="ses_abc123", load_skills=[], run_in_background=false, description="Fix type error", prompt="Fix: Type error on line 42")
```

**After EVERY delegation, STORE the session_id for potential continuation.**

### Code Changes:
- Match existing patterns (if codebase is disciplined)
- Propose approach first (if codebase is chaotic)
- Never suppress type errors with `as any`, `@ts-ignore`, `@ts-expect-error`
- Never commit unless explicitly requested
- When refactoring, use various tools to ensure safe refactorings
- **Bugfix Rule**: Fix minimally. NEVER refactor while fixing.

### Verification:

Run `lsp_diagnostics` on changed files at:
- End of a logical task unit
- Before marking a todo item complete
- Before reporting completion to user

If project has build/test commands, run them at task completion.

### Evidence Requirements (task NOT complete without these):

- **File edit** → `lsp_diagnostics` clean on changed files
- **Build command** → Exit code 0
- **Test run** → Pass (or explicit note of pre-existing failures)
- **Delegation** → Agent result received and verified

**NO EVIDENCE = NOT COMPLETE.**

---

## Phase 2C - Failure Recovery

### When Fixes Fail:

1. Fix root causes, not symptoms
2. Re-verify after EVERY fix attempt
3. Never shotgun debug (random changes hoping something works)

### After 3 Consecutive Failures:

1. **STOP** all further edits immediately
2. **REVERT** to last known working state (git checkout / undo edits)
3. **DOCUMENT** what was attempted and what failed
4. **CONSULT** Oracle with full failure context
5. If Oracle cannot resolve → **ASK USER** before proceeding

**Never**: Leave code in broken state, continue hoping it'll work, delete failing tests to "pass"

---

## Phase 3 - Completion

A task is complete when:
- [ ] All planned todo items marked done
- [ ] Diagnostics clean on changed files
- [ ] Build passes (if applicable)
- [ ] User's original request fully addressed

If verification fails:
1. Fix issues caused by your changes
2. Do NOT fix pre-existing issues unless asked
3. Report: "Done. Note: found N pre-existing lint errors unrelated to my changes."

### Before Delivering Final Answer:
- If Oracle is running: **end your response** and wait for the completion notification first.
- Cancel disposable background tasks individually via `background_cancel(taskId="...")`.
</Behavior_Instructions>

<Oracle_Usage>
## Oracle — Read-Only High-IQ Consultant

Oracle is a read-only, expensive, high-quality reasoning model for debugging and architecture. Consultation only.

### WHEN to Consult (Oracle FIRST, then implement):

- Complex architecture design
- After completing significant work
- 2+ failed fix attempts
- Unfamiliar code patterns
- Security/performance concerns
- Multi-system tradeoffs

### WHEN NOT to Consult:

- Simple file operations (use direct tools)
- First attempt at any fix (try yourself first)
- Questions answerable from code you've read
- Trivial decisions (variable names, formatting)
- Things you can infer from existing code patterns

### Usage Pattern:
Briefly announce "Consulting Oracle for [reason]" before invocation.

**Exception**: This is the ONLY case where you announce before acting. For all other work, start immediately without status updates.

### Oracle Background Task Policy:

**Collect Oracle results before your final answer. No exceptions.**

- Oracle takes minutes. When done with your own work: **end your response** — wait for the `<system-reminder>`.
- Do NOT poll `background_output` on a running Oracle. The notification will come.
- Never cancel Oracle.
</Oracle_Usage>

<Task_Management>
## Todo Management (CRITICAL)

**DEFAULT BEHAVIOR**: Create todos BEFORE starting any non-trivial task. This is your PRIMARY coordination mechanism.

### When to Create Todos (MANDATORY)

- Multi-step task (2+ steps) → ALWAYS create todos first
- Uncertain scope → ALWAYS (todos clarify thinking)
- User request with multiple items → ALWAYS
- Complex single task → Create todos to break down

### Workflow (NON-NEGOTIABLE)

1. **IMMEDIATELY on receiving request**: `todowrite` to plan atomic steps.
   - ONLY ADD TODOS TO IMPLEMENT SOMETHING, ONLY WHEN USER WANTS YOU TO IMPLEMENT SOMETHING.
2. **Before starting each step**: Mark `in_progress` (only ONE at a time)
3. **After completing each step**: Mark `completed` IMMEDIATELY (NEVER batch)
4. **If scope changes**: Update todos before proceeding

### Why This Is Non-Negotiable

- **User visibility**: User sees real-time progress, not a black box
- **Prevents drift**: Todos anchor you to the actual request
- **Recovery**: If interrupted, todos enable seamless continuation
- **Accountability**: Each todo = explicit commitment

### Anti-Patterns (BLOCKING)

- Skipping todos on multi-step tasks — user has no visibility, steps get forgotten
- Batch-completing multiple todos — defeats real-time tracking purpose
- Proceeding without marking in_progress — no indication of what you're working on
- Finishing without completing todos — task appears incomplete to user

**FAILURE TO USE TODOS ON NON-TRIVIAL TASKS = INCOMPLETE WORK.**

### Clarification Protocol (when asking):

```
I want to make sure I understand correctly.

**What I understood**: [Your interpretation]
**What I'm unsure about**: [Specific ambiguity]
**Options I see**:
1. [Option A] - [effort/implications]
2. [Option B] - [effort/implications]

**My recommendation**: [suggestion with reasoning]

Should I proceed with [recommendation], or would you prefer differently?
```
</Task_Management>

<Tone_and_Style>
## Communication Style

### Be Concise
- Start work immediately. No acknowledgments ("I'm on it", "Let me...", "I'll start...")
- Answer directly without preamble
- Don't summarize what you did unless asked
- Don't explain your code unless asked
- One word answers are acceptable when appropriate

### No Flattery
Never start responses with:
- "Great question!"
- "That's a really good idea!"
- "Excellent choice!"
- Any praise of the user's input

Just respond directly to the substance.

### No Status Updates
Never start responses with casual acknowledgments:
- "Hey I'm on it..."
- "I'm working on this..."
- "Let me start by..."
- "I'll get to work on..."
- "I'm going to..."

Just start working. Use todos for progress tracking—that's what they're for.

### When User is Wrong
If the user's approach seems problematic:
- Don't blindly implement it
- Don't lecture or be preachy
- Concisely state your concern and alternative
- Ask if they want to proceed anyway

### Match User's Style
- If user is terse, be terse
- If user wants detail, provide detail
- Adapt to their communication preference
</Tone_and_Style>

<Constraints>
## Hard Blocks (NEVER violate)

- Type error suppression (`as any`, `@ts-ignore`) — **Never**
- Commit without explicit request — **Never**
- Speculate about unread code — **Never**
- Leave code in broken state after failures — **Never**
- `background_cancel(all=true)` — **Never.** Always cancel individually by taskId.
- Delivering final answer before collecting Oracle result — **Never.**

## Anti-Patterns (BLOCKING violations)

- **Type Safety**: `as any`, `@ts-ignore`, `@ts-expect-error`
- **Error Handling**: Empty catch blocks `catch(e) {}`
- **Testing**: Deleting failing tests to "pass"
- **Search**: Firing agents for single-line typos or obvious syntax errors
- **Debugging**: Shotgun debugging, random changes
- **Background Tasks**: Polling `background_output` on running tasks — end response and wait for notification
- **Delegation Duplication**: Delegating exploration to explore/librarian and then manually doing the same search yourself
- **Oracle**: Delivering answer without collecting Oracle results

## Soft Guidelines

- Prefer existing libraries over new dependencies
- Prefer small, focused changes over large refactors
- When uncertain about scope, ask
</Constraints>

You are powered by the model named claude-opus-4-6. The exact model ID is anthropic/claude-opus-4-6
Here is some useful information about the environment you are running in:
<env>
  Working directory: /Users/ufukaltinok/Work/OSS/oh-my-opencode
  Workspace root folder: /Users/ufukaltinok/Work/OSS/oh-my-opencode
  Is directory a git repo: yes
  Platform: darwin
  Today's date: Thu Mar 26 2026
</env>
<directories>
  
</directories>
Instructions from: /Users/ufukaltinok/Work/OSS/oh-my-opencode/AGENTS.md
# oh-my-opencode — O P E N C O D E Plugin

**Generated:** 2026-03-06 | **Commit:** 7fe44024 | **Branch:** dev

## OVERVIEW

OpenCode plugin (npm: `oh-my-opencode`) that extends Claude Code (OpenCode fork) with multi-agent orchestration, 48 lifecycle hooks, 26 tools, skill/command/MCP systems, and Claude Code compatibility. 1268 TypeScript files, 160k LOC.

## STRUCTURE

```
oh-my-opencode/
├── src/
│   ├── index.ts              # Plugin entry: loadConfig → createManagers → createTools → createHooks → createPluginInterface
│   ├── plugin-config.ts      # JSONC multi-level config: user → project → defaults (Zod v4)
│   ├── agents/               # 11 agents (Sisyphus, Hephaestus, Oracle, Librarian, Explore, Atlas, Prometheus, Metis, Momus, Multimodal-Looker, Sisyphus-Junior)
│   ├── hooks/                # 48 lifecycle hooks across dedicated modules and standalone files
│   ├── tools/                # 26 tools across 15 directories
│   ├── features/             # 19 feature modules (background-agent, skill-loader, tmux, MCP-OAuth, etc.)
│   ├── shared/               # 95+ utility files in 13 categories
│   ├── config/               # Zod v4 schema system (24 files)
│   ├── cli/                  # CLI: install, run, doctor, mcp-oauth (Commander.js)
│   ├── mcp/                  # 3 built-in remote MCPs (websearch, context7, grep_app)
│   ├── plugin/               # 8 OpenCode hook handlers + 48 hook composition
│   └── plugin-handlers/      # 6-phase config loading pipeline
├── packages/                 # Monorepo: cli-runner, 12 platform binaries
└── local-ignore/             # Dev-only test fixtures
```

## INITIALIZATION FLOW

```
OhMyOpenCodePlugin(ctx)
  ├─→ loadPluginConfig()         # JSONC parse → project/user merge → Zod validate → migrate
  ├─→ createManagers()           # TmuxSessionManager, BackgroundManager, SkillMcpManager, ConfigHandler
  ├─→ createTools()              # SkillContext + AvailableCategories + ToolRegistry (26 tools)
  ├─→ createHooks()              # 3-tier: Core(39) + Continuation(7) + Skill(2) = 48 hooks
  └─→ createPluginInterface()    # 8 OpenCode hook handlers → PluginInterface
```

## 8 OPENCODE HOOK HANDLERS

| Handler | Purpose |
|---------|---------|
| `config` | 6-phase: provider → plugin-components → agents → tools → MCPs → commands |
| `tool` | 26 registered tools |
| `chat.message` | First-message variant, session setup, keyword detection |
| `chat.params` | Anthropic effort level adjustment |
| `chat.headers` | Copilot x-initiator header injection |
| `event` | Session lifecycle (created, deleted, idle, error) |
| `tool.execute.before` | Pre-tool hooks (file guard, label truncator, rules injector) |
| `tool.execute.after` | Post-tool hooks (output truncation, metadata store) |
| `experimental.chat.messages.transform` | Context injection, thinking block validation |

## WHERE TO LOOK

| Task | Location | Notes |
|------|----------|-------|
| Add new agent | `src/agents/` + `src/agents/builtin-agents/` | Follow createXXXAgent factory pattern |
| Add new hook | `src/hooks/{name}/` + register in `src/plugin/hooks/create-*-hooks.ts` | Match event type to tier |
| Add new tool | `src/tools/{name}/` + register in `src/plugin/tool-registry.ts` | Follow createXXXTool factory |
| Add new feature module | `src/features/{name}/` | Standalone module, wire in plugin/ |
| Add new MCP | `src/mcp/` + register in `createBuiltinMcps()` | Remote HTTP only |
| Add new skill | `src/features/builtin-skills/skills/` | Implement BuiltinSkill interface |
| Add new command | `src/features/builtin-commands/` | Template in templates/ |
| Add new CLI command | `src/cli/cli-program.ts` | Commander.js subcommand |
| Add new doctor check | `src/cli/doctor/checks/` | Register in checks/index.ts |
| Modify config schema | `src/config/schema/` + update root schema | Zod v4, add to OhMyOpenCodeConfigSchema |
| Add new category | `src/tools/delegate-task/constants.ts` | DEFAULT_CATEGORIES + CATEGORY_MODEL_REQUIREMENTS |

## MULTI-LEVEL CONFIG

```
Project (.opencode/oh-my-opencode.jsonc)  →  User (~/.config/opencode/oh-my-opencode.jsonc)  →  Defaults
```

- `agents`, `categories`, `claude_code`: deep merged recursively
- `disabled_*` arrays: Set union (concatenated + deduplicated)
- All other fields: override replaces base value
- Zod `safeParse()` fills defaults for omitted fields
- `migrateConfigFile()` transforms legacy keys automatically

Fields: agents (14 overridable, 21 fields each), categories (8 built-in + custom), disabled_* arrays (agents, hooks, mcps, skills, commands, tools), 19 feature-specific configs.

## THREE-TIER MCP SYSTEM

| Tier | Source | Mechanism |
|------|--------|-----------|
| Built-in | `src/mcp/` | 3 remote HTTP: websearch (Exa/Tavily), context7, grep_app |
| Claude Code | `.mcp.json` | `${VAR}` env expansion via claude-code-mcp-loader |
| Skill-embedded | SKILL.md YAML | Managed by SkillMcpManager (stdio + HTTP) |

## CONVENTIONS

- **Runtime**: Bun only — never use npm/yarn
- **TypeScript**: strict mode, ESNext, bundler moduleResolution, `bun-types` (never `@types/node`)
- **Test pattern**: Bun test (`bun:test`), co-located `*.test.ts`, given/when/then style (nested describe with `#given`/`#when`/`#then` prefixes)
- **CI test split**: mock-heavy tests run in isolation (separate `bun test` processes), rest in batch
- **Factory pattern**: `createXXX()` for all tools, hooks, agents
- **Hook tiers**: Session (23) → Tool-Guard (12) → Transform (4) → Continuation (7) → Skill (2)
- **Agent modes**: `primary` (respects UI model) vs `subagent` (own fallback chain) vs `all`
- **Model resolution**: 4-step: override → category-default → provider-fallback → system-default
- **Config format**: JSONC with comments, Zod v4 validation, snake_case keys
- **File naming**: kebab-case for all files/directories
- **Module structure**: index.ts barrel exports, no catch-all files (utils.ts, helpers.ts banned), 200 LOC soft limit
- **Imports**: relative within module, barrel imports across modules (`import { log } from "./shared"`)
- **No path aliases**: no `@/` — relative imports only

## ANTI-PATTERNS

- Never use `as any`, `@ts-ignore`, `@ts-expect-error`
- Never suppress lint/type errors
- Never add emojis to code/comments unless user explicitly asks
- Never commit unless explicitly requested
- Never run `bun publish` directly — use GitHub Actions
- Never modify `package.json` version locally
- Test: given/when/then — never use Arrange-Act-Assert comments
- Comments: avoid AI-generated comment patterns (enforced by comment-checker hook)
- Never create catch-all files (`utils.ts`, `helpers.ts`, `service.ts`)
- Empty catch blocks `catch(e) {}` — always handle errors
- Never use em dashes (—), en dashes (–), or AI filler phrases in generated content
- index.ts is entry point ONLY — never dump business logic there

## COMMANDS

```bash
bun test                    # Bun test suite
bun run build              # Build plugin (ESM + declarations + schema)
bun run build:all          # Build + platform binaries
bun run typecheck           # tsc --noEmit
bunx oh-my-opencode install # Interactive setup
bunx oh-my-opencode doctor  # Health diagnostics
bunx oh-my-opencode run     # Non-interactive session
```

## CI/CD

| Workflow | Trigger | Purpose |
|----------|---------|---------|
| ci.yml | push/PR to master/dev | Tests (split: mock-heavy isolated + batch), typecheck, build, schema auto-commit |
| publish.yml | manual dispatch | Version bump, npm publish, platform binaries, GitHub release, merge to master |
| publish-platform.yml | called by publish | 12 platform binaries via bun compile (darwin/linux/windows) |
| sisyphus-agent.yml | @mention / dispatch | AI agent handles issues/PRs |
| cla.yml | issue_comment/PR | CLA assistant for contributors |
| lint-workflows.yml | push to .github/ | actionlint + shellcheck on workflow files |

## NOTES

- Logger writes to `/tmp/oh-my-opencode.log` — check there for debugging
- Background tasks: 5 concurrent per model/provider (configurable)
- Plugin load timeout: 10s for Claude Code plugins
- Model fallback priority: Claude > OpenAI > Gemini > Copilot > OpenCode Zen > Z.ai > Kimi
- Config migration runs automatically on legacy keys (agent names, hook names, model versions)
- Build: bun build (ESM) + tsc --emitDeclarationOnly, externals: @ast-grep/napi
- Test setup: `test-setup.ts` preloaded via bunfig.toml, mock-heavy tests run in isolation in CI
- 98 barrel export files (index.ts) establish module boundaries
- Architecture rules enforced via `.sisyphus/rules/modular-code-enforcement.md`


## Magic Context

Messages and tool outputs are tagged with §N§ identifiers (e.g., §1§, §42§).
Use `ctx_reduce` to manage context size. It supports one operation:
- `drop`: Remove entirely (best for tool outputs you already acted on).
Syntax: "3-5", "1,2,9", or "1-5,8,12-15". Last 20 tags are protected.
Use `ctx_note` for deferred intentions — things to tackle later, not right now. NOT for task tracking (use todos). Notes survive context compression and you'll be reminded at natural work boundaries (after commits, historian runs, todo completion).
Use `ctx_memory` to manage cross-session project memories. Write new memories, delete stale ones, or search stored memories by category. Memories persist across sessions and are automatically injected into new sessions.
Use `ctx_expand` to decompress a compartment range to see the original conversation transcript. Use `start`/`end` from `<compartment start=N end=M>` attributes. Returns the compacted U:/A: transcript for that message range, capped at ~15K tokens.
NEVER drop large ranges blindly (e.g., "1-50"). Review each tag before deciding.
NEVER drop user messages — they are short and will be summarized by compartmentalization automatically. Dropping them loses context the historian needs.
NEVER drop assistant text messages unless they are exceptionally large. Your conversation messages are lightweight; only large tool outputs are worth dropping.
Before your turn finishes, consider using `ctx_reduce` to drop large tool outputs you no longer need.

### Reduction Triggers
- After file reads used for implementation — drop once you acted on the content.
- After search results processed — drop raw grep/glob outputs.
- After each logical implementation step completed.

### What to Drop
- Large tool outputs (file reads, grep, build logs) you already acted on.
- NEVER drop your task prompt or initial requirements.

### What to Keep
- ALL user messages and assistant conversation text — these are cheap and compartmentalized automatically.
- Your task requirements (initial prompt).
- Current implementation context and recent edits.
- Recent errors and test results.

Prefer many small targeted operations over one large blanket operation. Compress early and often — don't wait for warnings.


=== MESSAGES ===


[Message 0] role: user
[
  {
    "type": "text",
    "text": "§1§ We’re currently in feat/context-management branch, we need to change to feat/athena branch which is in its own worktree"
  }
]

[Message 1] role: assistant
[
  {
    "type": "text",
    "text": "§3§ The `feat/athena` worktree is at:\n\n```\n/Users/ufukaltinok/Work/OSS/oh-my-opencode-worktrees/feat-athena\n```\n\nYou'll need to `cd` into that directory in your terminal to work there. Want me to do anything in that worktree?"
  }
]

[Message 2] role: user
[
  {
    "type": "text",
    "text": "§4§ Let’s detach the branch from that worktree and checkout here, we gonna continue here. "
  }
]

[Message 3] role: assistant
[
  {
    "type": "text",
    "text": "§7§ Done. Worktree removed and now on `feat/athena`, up to date with origin."
  }
]

[Message 4] role: user
[
  {
    "type": "text",
    "text": "§8§ Context dumped to `/Users/ufukaltinok/Work/OSS/oh-my-opencode/.opencode/dumps/ses_2fa8148b8ffeY1vcEeJKshru4H-20260319-105753.txt`"
  }
]

[Message 5] role: user
[
  {
    "type": "text",
    "text": "§9§ Now our feat/athena branch is a bit behind of the upstream. (https://github.com/code-yeongyu/oh-my-openagent) We need to get up to date on tag v3.12.3 "
  }
]

[Message 6] role: assistant
[
  {
    "type": "text",
    "text": "§11§ Upstream already configured. Let me fetch and rebase onto that tag."
  }
]

[Message 7] role: assistant
[
  {
    "type": "text",
    "text": "§17§ 3 conflicts to resolve. Let me check them."
  }
]

[Message 8] role: assistant
[
  {
    "type": "text",
    "text": "§22§ Now let me resolve these conflicts. The schema.json is auto-generated so I'll take the upstream version. For the code files, I need to keep our feat/athena additions while incorporating upstream improvements."
  }
]

[Message 9] role: assistant
[
  {
    "type": "text",
    "text": "§24§ Now let me resolve the two code conflicts. Let me read the full conflict areas."
  }
]

[Message 10] role: assistant
[
  {
    "type": "text",
    "text": "§28§ Now I'll check if `isActiveSessionStatus` is available in our branch to use the upstream's improved check:"
  }
]

[Message 11] role: assistant
[
  {
    "type": "text",
    "text": "§30§ Good, `isActiveSessionStatus` is available. Now I'll resolve both conflicts:\n\n**manager.ts** — Keep our feat/athena idle-handling logic, but use upstream's `isActiveSessionStatus` for the active check:"
  }
]

[Message 12] role: assistant
[
  {
    "type": "text",
    "text": "§32§ **agent-config-handler.ts** — Keep upstream's `pluginAgents` (migrated) + null filter, and keep our athena args:"
  }
]

[Message 13] role: assistant
[
  {
    "type": "text",
    "text": "§34§ Now add the resolved files and complete the merge:"
  }
]

[Message 14] role: assistant
[
  {
    "type": "text",
    "text": "§36§ No remaining conflicts. Let me finalize the merge commit:"
  }
]

[Message 15] role: assistant
[
  {
    "type": "text",
    "text": "§38§ Done. `feat/athena` is now merged with upstream `v3.12.3`. Three conflicts resolved:\n\n- **`assets/oh-my-opencode.schema.json`** — took upstream (auto-generated)\n- **`src/features/background-agent/manager.ts`** — kept our idle-handling logic, adopted upstream's `isActiveSessionStatus()` for the active-session check\n- **`src/plugin-handlers/agent-config-handler.ts`** — adopted upstream's `pluginAgents` (migrated) + null filter, kept our athena args"
  }
]

[Message 16] role: user
[
  {
    "type": "text",
    "text": "§39§ Did we actually create this: src/features/background-agent/manager.ts — kept our idle-handling logic or was it actually from upstream from older commits. We need to check this it’s important. "
  }
]

[Message 17] role: assistant
[
  {
    "type": "text",
    "text": "§46§ Here's the answer:\n\n**The idle-handling logic (`validateSessionHasOutput`, `checkSessionTodos`) already exists in upstream v3.12.3** — it's part of the event-based path via `session-idle-event-handler`. It was introduced around v3.5.0-v3.8.2 upstream.\n\n**`nudgeCouncilMemberIfNeeded` is the only thing that's ours** (feat/athena) — it was introduced in the athena council PR (`df2bd752`). It does NOT exist in v3.12.3.\n\nSo in the conflict I resolved, the block I kept from \"ours\" was mostly **duplicating upstream's existing idle logic in the polling path**, while upstream had already refactored it to be event-driven. The only genuinely new piece was `nudgeCouncilMemberIfNeeded`.\n\nLet me check what upstream's polling path actually does at v3.12.3 for idle to see if we introduced a redundancy:"
  }
]

[Message 18] role: assistant
[
  {
    "type": "text",
    "text": "§48§ This confirms it. In upstream v3.12.3, the polling path does **not** have inline idle-handling — it relies on the event-based `session-idle-event-handler` for that. The polling path just checks `isActiveSessionStatus()` and skips if running.\n\n**Summary:**\n- The idle-handling block we kept in the polling path (`validateSessionHasOutput`, `checkSessionTodos`, `tryCompleteTask`) is **redundant with upstream's event-driven approach**. It's not \"ours\" — it's older upstream code that was already refactored out.\n- The **only genuinely feat/athena addition** is `nudgeCouncilMemberIfNeeded`.\n\nWe should clean this up: remove the redundant inline idle logic from the polling path and only keep `nudgeCouncilMemberIfNeeded` wired into the event-based flow where it belongs. Want me to do that?"
  }
]

[Message 19] role: user
[
  {
    "type": "text",
    "text": "§49§ Yes"
  }
]

[Message 20] role: assistant
[
  {
    "type": "text",
    "text": "§55§ `nudgeCouncilMemberIfNeeded` is already wired into the event-based `session-idle-event-handler.ts` (line 16, 98-99) and passed from manager.ts at line 1011. So the polling path duplicate is purely redundant. Let me also check what the rest of the polling path looks like after the block to remove, so I get the edit right:"
  }
]

[Message 21] role: assistant
[
  {
    "type": "text",
    "text": "§57§ Now I see the full picture. Lines 1842-1871 is the redundant inline idle block. The existing code at lines 1903-1922 already handles \"idle or gone\" as a fallback at the end. I just need to remove the duplicate block (1842-1871)."
  }
]

[Message 22] role: assistant
[
  {
    "type": "text",
    "text": "§62§ Done. Zero type errors.\n\n**What changed:** Removed the redundant 30-line inline idle-handling block (lines 1842-1871) from the polling path in `manager.ts`. This block duplicated `validateSessionHasOutput`, `checkSessionTodos`, and `nudgeCouncilMemberIfNeeded` calls that are already handled by:\n\n1. **Event-based path** — `session-idle-event-handler.ts` handles `session.idle` events with all three checks including `nudgeCouncilMemberIfNeeded` (already wired at line 1011)\n2. **Fallback polling path** — the existing idle/gone block at lines 1903-1922 already covers `validateSessionHasOutput` and `checkSessionTodos` for the polling fallback\n\nThe polling path now matches upstream v3.12.3's structure: `isActiveSessionStatus` → skip if running, terminal → complete immediately, otherwise fall through to the idle/gone validation path."
  }
]

[Message 23] role: user
[
  {
    "type": "text",
    "text": "§63§ Okay great now, someone found a bug:\n\nBug: switch-agent TUI navigation silently fails\nFound while merging feat/athena with v3.12.0.\n\nIssue\nnavigateTuiToSession in src/tools/switch-agent/tools.ts uses the wrong call signature for selectSession, so TUI navigation after an agent switch silently does nothing. The user stays in the current session while the target agent (e.g. Prometheus) runs in a detached session nobody sees.\n\nRoot cause\nWrong type (line 16):\n\n// Current\ntype TuiService = {\n  selectSession: (input?: { sessionID?: string }) => Promise<unknown>\n}\n\n// Should be (matches ralph-loop's working SelectSessionApi)\ntype TuiService = {\n  selectSession: (input: { body: { sessionID: string } }) => Promise<unknown>\n}\nWrong call (line 64):\n\n// Current\nawait client.tui.selectSession({ sessionID })\n\n// Should be\nawait client.tui.selectSession({ body: { sessionID } })\nThe correct pattern is confirmed by src/hooks/ralph-loop/session-reset-strategy.ts (line 39, 50) which works correctly.\n\nSilent failure: When hasTuiService returns false or selectSession throws, the function returns false with no logging. The output says \"Agent switch initiated\" without any indication that TUI navigation failed.\n\nFix\nTwo lines + logging: fix the type, fix the call arg shape, add log() on both failure paths. I've applied this in my fork — happy to open a PR if helpful."
  }
]

[Message 24] role: assistant
[
  {
    "type": "text",
    "text": "§67§ Bug is confirmed. The ralph-loop uses `{ body: { sessionID } }` (line 39) which works. The switch-agent tool uses `{ sessionID }` which silently fails."
  }
]

[Message 25] role: assistant
[
  {
    "type": "text",
    "text": "§69§ The `log` call needs fixing — it takes 2 args max."
  }
]

[Message 26] role: assistant
[
  {
    "type": "text",
    "text": "§72§ Clean. Three changes made to `src/tools/switch-agent/tools.ts`:\n\n1. **Fixed type** — `selectSession` now expects `{ body: { sessionID: string } }` matching the actual API (confirmed by ralph-loop's working implementation)\n2. **Fixed call** — `{ sessionID }` → `{ body: { sessionID } }`\n3. **Added logging** — both the \"TUI not available\" and \"navigation failed\" paths now log instead of silently returning false"
  }
]

[Message 27] role: user
[
  {
    "type": "text",
    "text": "§73§ Good let’s commit the changes and push. "
  }
]

[Message 28] role: assistant
[
  {
    "type": "text",
    "text": "§79§ Committed and pushed. 130 commits ahead of origin now synced."
  }
]

[Message 29] role: user
[
  {
    "type": "text",
    "text": "§80§ This happened while trying to use switch agent \n[2026-03-19T10:13:25.689Z] [switch-agent] TUI service not available, cannot navigate to session: \"ses_2fa6a0fc9ffesMPCQ0eHN3O7Hc\"\n[2026-03-19T10:13:25.689Z] [switch-agent] Agent switch applied via fresh session {\"sourceSessionID\":\"ses_2fa6a2cdcffeMbYhygVJ7SmV5z\",\"newSessionID\":\"ses_2fa6a0fc9ffesMPCQ0eHN3O7Hc\",\"agent\":\"Atlas (Plan Executor)\",\"tuiNavigated\":false,\"promptDelivered\":true}"
  }
]

[Message 30] role: assistant
[
  {
    "type": "text",
    "text": "§81§ The fix landed the call signature, but the client passed to `createSwitchAgentTool` doesn't have a `tui` property at all. Let me compare how ralph-loop gets its client vs how switch-agent gets its."
  }
]

[Message 31] role: assistant
[
  {
    "type": "text",
    "text": "§85§ Now I see the issue. Both get `ctx.client`, so the client object is the same. The difference is **how they access `tui`**.\n\nRalph-loop uses `isRecord(client)` then accesses `client.tui` as a generic record property — it works because it doesn't constrain the client type. Switch-agent uses `Reflect.get` which should also work... but the `SessionClient` type only declares `session`, not `tui`. The `hasTuiService` type guard narrows from `SessionClient` to `SessionClient & { tui: TuiService }` — but `ctx.client` might have `tui` as a getter or prototype property that `Reflect.get` should pick up.\n\nLet me check if `hasTuiService` actually works correctly:"
  }
]

[Message 32] role: assistant
[
  {
    "type": "text",
    "text": "§88§ The `Reflect.get` approach in `hasTuiService` should work the same as ralph-loop's record access. The client really doesn't have `tui` at runtime. Let me check why:"
  }
]

[Message 33] role: assistant
[
  {
    "type": "text",
    "text": "§94§ Good — the OpenCode source is at `~/Work/OSS/opencode`. Let me check how the client's `tui` property is actually exposed, since `hasTuiService` is returning false at runtime."
  }
]

[Message 34] role: assistant
[
  {
    "type": "text",
    "text": "§114§ Found it. **`selectSession` doesn't exist in the v1 SDK at all.** It's only in v2. Let me check which one OpenCode actually exports:"
  }
]

[Message 35] role: assistant
[
  {
    "type": "text",
    "text": "§120§ Root cause identified.\n\n**The v1 SDK (`@opencode-ai/sdk` → `./src/index.ts`) doesn't have `selectSession` on the `Tui` class at all.** It only exists in the v2 SDK (`@opencode-ai/sdk/v2`). \n\nOpenCode's plugin loader (`packages/opencode/src/plugin/index.ts` line 5) imports from `@opencode-ai/sdk` (v1), so the client passed to plugins has a `tui` property but it lacks `selectSession`. That's why `hasTuiService` returns false — the method doesn't exist.\n\nNow the question: **ralph-loop's `getSelectSessionApi` has the same check** — it looks for `tuiValue.selectSession` and returns null if it's not a function. So ralph-loop would also silently fail on v1 SDK. \n\nThis means either:\n1. The OpenCode version you're running has been updated to v2 SDK and ralph-loop works, or  \n2. Ralph-loop has the same silent failure and nobody noticed because it's a less-used path\n\nEither way, the fix for switch-agent is the same: we need to match ralph-loop's approach. But the deeper issue is that **`selectSession` needs to be on the SDK the plugin actually receives**.\n\nLet me check if there's a newer version of OpenCode that uses v2:"
  }
]

[Message 36] role: assistant
[
  {
    "type": "text",
    "text": "§125§ Good point. Let me check how this actually works in the current OpenCode the user is running:"
  }
]

[Message 37] role: assistant
[
  {
    "type": "text",
    "text": "§136§ OK so the v2 SDK commit didn't change the import — OpenCode still uses v1 SDK (`@opencode-ai/sdk`), and v1 Tui class **has no `selectSession` method**.\n\nThat means both our switch-agent AND ralph-loop's `selectSessionInTui` would fail on the current OpenCode HEAD — unless OpenCode in TUI mode exposes something different.\n\nLet me check your point about TUI mode directly:"
  }
]

[Message 38] role: assistant
[
  {
    "type": "text",
    "text": "§140§ Here's the full picture:\n\n- **v1 SDK** (what both OpenCode and our plugin use) → `Tui` class has NO `selectSession` method\n- **v2 SDK** → has `selectSession` but nobody imports it yet\n- **Server route** `/tui/select-session` exists and works — publishes a bus event the TUI subscribes to\n- **Ralph-loop** dynamically checks for `selectSession` and silently returns false if missing — same failure\n\nThe SDK client doesn't have the method, but the HTTP endpoint exists. We need to bypass the SDK and call the endpoint directly. The plugin has access to `serverUrl`."
  }
]

[Message 39] role: assistant
[
  {
    "type": "text",
    "text": "§142§ Good question. Let me trace the regression."
  }
]

[Message 40] role: assistant
[
  {
    "type": "text",
    "text": "§148§ **Found the regression.** Let me see the breaking refactor:"
  }
]

[Message 41] role: assistant
[
  {
    "type": "text",
    "text": "§152§ The regression chain:\n\n1. **`96972694` (Mar 1)** — Original working code: `(client as any)._client.post({ url: \"/tui/select-session\", body: { sessionID } })`. Direct HTTP POST bypassing SDK. **Worked.**\n\n2. **`15dc2209` (Mar 14)** — First refactor: replaced `as any` with a type guard checking `client._client.post`. Still used `_client.post()` direct HTTP. **Still worked.**\n\n3. **`4150e26d` (Mar 14, 2h later)** — **THE BREAKING COMMIT**: replaced `_client.post()` with `client.tui.selectSession({ sessionID })`. Assumed the v1 SDK `Tui` class has `selectSession` — **it doesn't**. This is when it broke.\n\n**Fix:** Revert to the approach from step 1/2 — use `_client.post()` direct HTTP call to `/tui/select-session`. That's exactly what you were saying about TUI mode not using the SDK."
  }
]

[Message 42] role: assistant
[
  {
    "type": "text",
    "text": "§155§ Clean. Here's the regression summary and fix:\n\n**Root cause:** Commit `4150e26d` (Mar 14) replaced the working `_client.post()` direct HTTP call with `client.tui.selectSession()` — assuming the v1 SDK `Tui` class has `selectSession`. It doesn't. That method only exists in v2 SDK which nobody imports yet.\n\n**Fix:** Reverted to the direct HTTP approach — `_client.post({ url: \"/tui/select-session\", body: { sessionID } })`. This bypasses the SDK entirely and hits the server route directly, which publishes the bus event the TUI subscribes to. Works regardless of SDK version.\n\nWant me to commit and push?"
  }
]

[Message 43] role: user
[
  {
    "type": "text",
    "text": "§156§ Yes this fixed it. what was the commit message for that change? "
  }
]

[Message 44] role: assistant
[
  {
    "type": "text",
    "text": "§158§ The last commit message was:\n\n```\nfix(switch-agent): correct selectSession call signature for TUI navigation\n```\n\nThat message is now inaccurate — we didn't just fix the call signature, we replaced the SDK approach entirely with a direct HTTP call. Want me to amend it to something like `fix(switch-agent): bypass SDK, use direct HTTP for TUI session navigation`?"
  }
]

[Message 45] role: user
[
  {
    "type": "text",
    "text": "§159§ Okay let’s commit and push and please write me a reply draft that I can send to the original bug report "
  }
]

[Message 46] role: assistant
[
  {
    "type": "text",
    "text": "§163§ Pushed. Here's the reply draft:\n\n---\n\nThanks for the detailed report -- this was spot on.\n\n**Root cause:** A refactor on Mar 14 (`4150e26d`) replaced a working direct HTTP call (`_client.post(\"/tui/select-session\")`) with `client.tui.selectSession()`, assuming the method exists on the SDK's `Tui` class. It doesn't -- `selectSession` only exists in the v2 SDK (`@opencode-ai/sdk/v2`), and both OpenCode and our plugin import the v1 SDK. The type guard (`hasTuiService`) returns false, so TUI navigation silently no-ops while the agent switch itself succeeds in a detached session.\n\n**Fix:** Reverted to bypassing the SDK and POSTing directly to `/tui/select-session` via the SDK client's internal HTTP client. This hits the server route that publishes the bus event the TUI subscribes to, and works regardless of SDK version.\n\nYour suggested fix (changing the call signature to `{ body: { sessionID } }`) was close but wouldn't have resolved it -- the underlying issue is that the method doesn't exist at all on v1 SDK, so the type guard rejects it before the call is ever made.\n\nFixed in `3a30ccf4` on `feat/athena`."
  }
]

[Message 47] role: user
[
  {
    "type": "text",
    "text": "§164§ \nCheck if these issues are valid — if so, understand the root cause of each and fix them. If appropriate, use sub-agents to investigate and fix each issue separately.\n\n\n<file name=\"src/tools/switch-agent/tools.ts\">\n\n<violation number=\"1\" location=\"src/tools/switch-agent/tools.ts:73\">\nP2: `navigateTuiToSession` treats any resolved `post` as success, so HTTP error responses can be misreported as successful navigation when the client does not throw on 4xx/5xx.</violation>\n</file>"
  }
]

[Message 48] role: assistant
[
  {
    "type": "text",
    "text": "§165§ I detect **investigation + fix** intent. Let me check the current code and how the SDK's `_client.post()` behaves on error responses."
  }
]

[Message 49] role: assistant
[
  {
    "type": "text",
    "text": "§174§ **Violation is valid.** The v1 SDK client's `post()` does NOT throw on 4xx/5xx by default. On error, it resolves with `{ error, request, response }` instead of throwing. Our catch block never fires, and we return `true`.\n\nThe fix: check the response for an `error` property."
  }
]

[Message 50] role: assistant
[
  {
    "type": "text",
    "text": "§178§ **Valid.** The v1 SDK client does NOT throw on 4xx/5xx by default -- it resolves with `{ error, request, response }`. Our `navigateTuiToSession` was treating any resolved promise as success.\n\nFixed by checking the response for an `error` property before returning `true`. Now 4xx/5xx responses are caught and logged."
  }
]

[Message 51] role: user
[
  {
    "type": "text",
    "text": "§179§ Let’s commit and push"
  }
]

[Message 52] role: assistant
[
  {
    "type": "text",
    "text": "§181§ Pushed `438f8690`."
  }
]

[Message 53] role: user
[
  {
    "type": "text",
    "text": "§182§ \nCheck if these issues are valid — if so, understand the root cause of each and fix them. If appropriate, use sub-agents to investigate and fix each issue separately.\n\n\n<file name=\"src/tools/switch-agent/tools.ts\">\n\n<violation number=\"1\" location=\"src/tools/switch-agent/tools.ts:78\">\nP2: The `\"error\" in result` check may incorrectly flag successful responses as errors if the API returns objects with falsy error fields like `{ error: null }`. The `in` operator checks for key presence, not value truthiness. Change the check to verify the error value is actually meaningful, such as `result.error != null`.</violation>\n</file>"
  }
]

[Message 54] role: assistant
[
  {
    "type": "text",
    "text": "§184§ **Invalid.** The SDK client's behavior makes this a non-issue:\n\n- **Success path** (`response.ok`): returns `{ data, request, response }` — no `error` key exists\n- **Error path** (`!response.ok`): returns `{ error: finalError, ...result }` where `finalError` is always truthy (`finalError || ({} as string)` — at minimum an empty object)\n\nThe `error` key is only ever present on error responses, and its value is never null/falsy. `\"error\" in result` correctly discriminates between the two shapes. No change needed."
  }
]

[Message 55] role: user
[
  {
    "type": "text",
    "text": "§185§ These tools:\n\n    \"prepare_council_prompt\",\n    \"council_finalize”,\n\nShould be only available to athena and athena-junior but it’s registered to all agents."
  }
]

[Message 56] role: assistant
[
  {
    "type": "text",
    "text": "§186§ I detect implementation intent — you want the tool exposure narrowed so only `athena` and `athena-junior` can use those council tools. My approach: inspect where the tools are registered and where agent tool visibility is filtered, then patch the restriction at the source."
  }
]

[Message 57] role: assistant
[
  {
    "type": "text",
    "text": "§189§ I’m waiting on the codebase scan for the exact registration/filter path, then I’ll patch the restriction cleanly."
  }
]

[Message 58] role: assistant
[
  {
    "type": "text",
    "text": "§192§ I found the registration path. The issue looks valid: the tools are globally registered in `src/plugin/tool-registry.ts`, and the current gating in `src/plugin-handlers/tool-config-handler.ts` appears to control permissions, not actual tool visibility. I’m patching the config handler so only `athena` and `athena-junior` get these tool definitions in their visible tool list."
  }
]

[Message 59] role: assistant
[
  {
    "type": "text",
    "text": "§211§ I changed `src/plugin-handlers/tool-config-handler.ts` so `athena` and `athena-junior` now get explicit `tools` visibility overrides for `prepare_council_prompt`, `council_finalize`, and `athena_council`, while the global config still hides them by default for everyone else. Next I’m adding a targeted test to lock this behavior in."
  }
]

[Message 60] role: assistant
[
  {
    "type": "text",
    "text": "§216§ The behavior change is in. `src/plugin-handlers/tool-config-handler.ts` now explicitly grants tool visibility only to `athena` and `athena-junior`, and the new test covers both the positive and negative cases. I’m doing one more verification pass because the language server is flagging the usual `bun:test` ambient-types issue on the test file even though the test itself passes under Bun."
  }
]

[Message 61] role: assistant
[
  {
    "type": "text",
    "text": "§221§ The issue was valid. `src/plugin/tool-registry.ts` registers `prepare_council_prompt`, `council_finalize`, and `athena_council` globally, and `src/plugin-handlers/tool-config-handler.ts` was only adjusting permissions. That meant the tools could still appear in other agents’ tool lists even though execution was denied.\n\nI fixed it in `src/plugin-handlers/tool-config-handler.ts` by:\n- keeping the global hide defaults for those three tools\n- adding explicit per-agent `tools` visibility overrides only for `athena` and `athena-junior`\n- preserving the existing per-agent permission allows for those same tools\n\nI also added coverage in `src/plugin-handlers/tool-config-handler.test.ts` for both:\n- `athena` / `athena-junior` can see the council tools\n- non-Athena agents keep them hidden\n\nVerification:\n- `bun test src/plugin-handlers/tool-config-handler.test.ts` passes\n- `bun run typecheck` passes\n\nNext step if you want:\n1. `commit and push`"
  }
]

[Message 62] role: user
[
  {
    "type": "text",
    "text": "§222§ We need to first get up to date with upstream dev"
  }
]

[Message 63] role: assistant
[
  {
    "type": "text",
    "text": "§228§ Need to stash our uncommitted changes first, merge, then reapply."
  }
]

[Message 64] role: assistant
[
  {
    "type": "text",
    "text": "§235§ Both conflicts are straightforward — each side added non-overlapping code. Let me resolve them:"
  }
]

[Message 65] role: assistant
[
  {
    "type": "text",
    "text": "§238§ Both resolved. Now add and complete the merge:"
  }
]

[Message 66] role: assistant
[
  {
    "type": "text",
    "text": "§246§ First let me resolve the test file conflict — need to keep both upstream's `disabled_tools` tests and our council tool gating tests:"
  }
]

[Message 67] role: assistant
[
  {
    "type": "text",
    "text": "§249§ All 41 tests pass (upstream's new `disabled_tools` tests + our council tool gating tests). Now commit and push:"
  }
]

[Message 68] role: assistant
[
  {
    "type": "text",
    "text": "§251§ Done. Merged upstream dev, resolved 2 conflicts (idle-event imports + tool-execute-before keeping both null byte stripping and council member guard), then committed the council tool gating on top. Pushed `12691ee9`."
  }
]

[Message 69] role: user
[
  {
    "type": "text",
    "text": "§252§ Wait now athena-junior is not able to call council-finalize, I think we did the reverse of what we supposed to do"
  }
]

[Message 70] role: assistant
[
  {
    "type": "text",
    "text": "§253§ Let me check what we actually did vs what we should have done."
  }
]

[Message 71] role: assistant
[
  {
    "type": "text",
    "text": "§257§ I see the problem. We're doing **both**:\n\n1. **Line 43-45**: `tools: { prepare_council_prompt: false, council_finalize: false, athena_council: false }` — globally hides the tools from all agents\n2. **Line 129-150**: Per-agent `tools: { ...: true }` — tries to re-enable for athena/athena-junior\n3. **Line 166-168**: `permission: { ...: \"deny\" }` — globally denies permission\n\nThe global `tools: false` completely removes the tool definition before the agent-level override gets a chance. We should **not** hide them globally — just deny permission globally and allow per-agent. The tools need to be *visible* to athena/athena-junior but *denied* for everyone else."
  }
]

[Message 72] role: assistant
[
  {
    "type": "text",
    "text": "§259§ Now the tools are no longer globally hidden (`tools: false`). They're still visible to all agents, but **permission is denied** globally (line 166-168) and only **allowed** per-agent for athena/athena-junior. Need to update the test too — the non-athena test was asserting `tools.prepare_council_prompt === false` which no longer applies:"
  }
]

[Message 73] role: assistant
[
  {
    "type": "text",
    "text": "§262§ All 41 pass. The fix: removed `tools: false` for council tools (which was nuking them entirely), kept only the `permission: \"deny\"` global default + per-agent `permission: \"allow\"` + `tools: true` for athena/athena-junior. Now the tools are visible to athena agents but permission-denied for everyone else.\n\nWant me to commit and push?"
  }
]

[Message 74] role: user
[
  {
    "type": "text",
    "text": "§263§ Context dumped to `/Users/ufukaltinok/Work/OSS/oh-my-opencode/.opencode/dumps/ses_2fa8148b8ffeY1vcEeJKshru4H-20260326-181307.txt`"
  }
]

[Message 75] role: user
[
  {
    "type": "text",
    "text": "§264§ Yes but is it really fixed because you currently have all these tools for example which normally you shouldn’t have:\n\n    \"prepare_council_prompt\",\n    \"council_finalize\",\n    \"athena_council\","
  }
]

[Message 76] role: assistant
[
  {
    "type": "text",
    "text": "§266§ You're right — removing `tools: false` just made them visible to everyone. Permission deny blocks execution but doesn't hide the tool definition. We need them completely invisible to non-athena agents.\n\nThe core issue is: `config.tools.X = false` globally hides the tool, and `agent.tools.X = true` apparently doesn't override that. Let me trace how OpenCode resolves tool visibility."
  }
]

[Message 77] role: assistant
[
  {
    "type": "text",
    "text": "§311§ I've been tracing through the OpenCode source extensively. Here's what I found:\n\n**The `config.tools` field is legacy compatibility** — OpenCode converts it to permission rules during Zod parsing (config.ts line 229-241). But our plugin mutates the config AFTER parsing, so our `config.tools.X = false` never triggers the conversion. It's a dead field.\n\n**Similarly, `agent.tools`** is also legacy compat (config.ts line 772-783) — converted during parsing, not at runtime. Our per-agent `tools: true` overrides never take effect.\n\n**The only real mechanism** is `config.permission` → feeds into agent permission rulesets → `PermissionNext.disabled()` removes tools in `resolveTools()`.\n\nBut there's a potential **timing issue**: if `Agent.state()` initializes before `Plugin.init()` finishes mutating the config, agents won't see our permission changes.\n\nLet me verify the init order:"
  }
]

[Message 78] role: assistant
[
  {
    "type": "text",
    "text": "§324§ Let me check how our plugin's tools actually get registered vs OpenCode's ToolRegistry:"
  }
]

[Message 79] role: assistant
[
  {
    "type": "reasoning",
    "text": "Line 55-60: Plugin tools are loaded from `Plugin.list()` which returns all loaded plugins. Each plugin's `tool` property is iterated, and each tool definition is wrapped via `fromPlugin()` and pushed into the `custom` array.\n\nSo our plugin's tools (council_finalize, prepare_council_prompt, athena_council) are registered in the global ToolRegistry alongside all built-in tools. When `ToolRegistry.tools(model, agent)` is called, ALL tools are returned (with model-specific filtering for edit/websearch/codesearch only).\n\nThen in prompt.ts, ALL tools go into the `tools` map. Then LLM.stream's resolveTools filters by permission.\n\n\nSo the permission-based filtering is the ONLY mechanism. And based on my analysis, it should work if our config.permission changes are visible to agent.ts.\n\nLet me try a completely different approach. Instead of permission-based filtering (which theoretically works but seemingly doesn't in practice), let me filter the tools AT REGISTRATION TIME. I'm looking at how `ToolRegistry.tools()` works and thinking about whether I can intercept the agent parameter there to apply permission-based filtering earlier in the pipeline.\n\nThe `agent` gets passed to `t.init()` during tool initialization, so theoretically I could make council tools return null or skip themselves when the agent isn't athena or athena-junior. But looking at how `fromPlugin` constructs tools, the `init` function always returns a tool object—there's no mechanism to signal \"skip this tool.\" I'm examining the filter logic in `tools()` to see if I can add a condition there that checks the agent and filters out council tools accordingly.\n\nNow I'm mapping out what mechanisms are actually available in my plugin to control tool visibility per agent. The legacy config approaches like `config.tools.X = false` and `agent.tools.X = true/false` both get converted during Zod parsing before my mutations run, so they're not viable. But `config.permission.X = \"deny\"` and `agent.permission.X = \"allow/deny\"` should work since those get read by agent.ts as permissions that can be applied after parsing.\n\nThe user says it's not working though, so I'm wondering if there's a type mismatch happening. When my plugin sets `config.permission`, it's assigning a plain object, but the Zod schema might define `permission` as a specific typed structure that doesn't accept my assignments the way I expect.\n\nActually, I realize the real issue might be simpler — maybe I shouldn't be relying on the global `config.tools` and `config.permission` at all, and instead find a more direct way to pass the filtered tools through the system.",
    "providerOptions": {
      "anthropic": {
        "signature": ""
      }
    }
  },
  {
    "type": "text",
    "text": "§331§ Let me check how our council tools are actually registered:"
  }
]

[Message 80] role: assistant
[
  {
    "type": "tool-call",
    "toolCallId": "toolu_01SwNDNWaCGeHs8p2yeQKEcG",
    "toolName": "read",
    "input": {
      "filePath": "src/shared/agent-tool-restrictions.ts"
    }
  }
]

[Message 81] role: tool
[
  {
    "type": "tool-result",
    "toolCallId": "toolu_01SwNDNWaCGeHs8p2yeQKEcG",
    "toolName": "read",
    "output": {
      "type": "text",
      "value": "§336§   1: /**\n  2:  * Agent tool restrictions for session.prompt calls.\n  3:  * OpenCode SDK's session.prompt `tools` parameter expects boolean values.\n  4:  * true = tool allowed, false = tool denied.\n  5:  */\n  6: \n  7: import { COUNCIL_MEMBER_KEY_PREFIX } from \"../agents/builtin-agents/council-member-agents\"\n  8: \n  9: const EXPLORATION_AGENT_DENYLIST: Record<string, boolean> = {\n 10:   write: false,\n 11:   edit: false,\n 12:   task: false,\n 13:   call_omo_agent: false,\n 14:   switch_agent: false,\n 15: }\n 16: \n 17: const AGENT_RESTRICTIONS: Record<string, Record<string, boolean>> = {\n 18:   explore: EXPLORATION_AGENT_DENYLIST,\n 19: \n 20:   librarian: EXPLORATION_AGENT_DENYLIST,\n 21: \n 22:   oracle: {\n 23:     write: false,\n 24:     edit: false,\n 25:     task: false,\n 26:     call_omo_agent: false,\n 27:     switch_agent: false,\n 28:   },\n 29: \n 30:   metis: {\n 31:     write: false,\n 32:     edit: false,\n 33:     task: false,\n 34:     switch_agent: false,\n 35:   },\n 36: \n 37:   momus: {\n 38:     write: false,\n 39:     edit: false,\n 40:     task: false,\n 41:     switch_agent: false,\n 42:   },\n 43: \n 44:   \"multimodal-looker\": {\n 45:     read: true,\n 46:   },\n 47: \n 48:   \"sisyphus-junior\": {\n 49:     task: false,\n 50:     switch_agent: false,\n 51:   },\n 52: \n 53:   athena: {\n 54:     call_omo_agent: false,\n 55:   },\n 56: \n 57:   \"athena-junior\": {\n 58:     call_omo_agent: false,\n 59:     question: false,\n 60:     switch_agent: false,\n 61:   },\n 62: \n 63:   // NOTE: Athena/council tool restrictions are also defined in:\n 64:   // - src/agents/athena/agent.ts (AgentConfig permission format)\n 65:   // - src/agents/athena/council-member-agent.ts (AgentConfig permission format — allow-list)\n 66:   // - src/plugin-handlers/tool-config-handler.ts (allow/deny string format)\n 67:   // Keep all three in sync when modifying.\n 68:   // Council members use an allow-list: read-only analysis + optional call_omo_agent delegation.\n 69:   // TodoWrite/TodoRead explicitly denied to prevent uncompletable todo loops.\n 70:   // Prompt file lives in .sisyphus/tmp/ (inside project) so no external_directory needed.\n 71:   \"council-member\": {\n 72:     \"*\": false,\n 73:     read: true,\n 74:     grep: true,\n 75:     glob: true,\n 76:     lsp_goto_definition: true,\n 77:     lsp_find_references: true,\n 78:     lsp_symbols: true,\n 79:     lsp_diagnostics: true,\n 80:     ast_grep_search: true,\n 81:     call_omo_agent: true,\n 82:     background_output: true,\n 83:     background_wait: true,\n 84:     background_cancel: true,\n 85:     todowrite: false,\n 86:     todoread: false,\n 87:   },\n 88: }\n 89: \n 90: export function getAgentToolRestrictions(agentName: string): Record<string, boolean> {\n 91:   if (agentName.startsWith(COUNCIL_MEMBER_KEY_PREFIX)) {\n 92:     return AGENT_RESTRICTIONS[\"council-member\"] ?? {}\n 93:   }\n 94: \n 95:   return AGENT_RESTRICTIONS[agentName]\n 96:     ?? Object.entries(AGENT_RESTRICTIONS).find(([key]) => key.toLowerCase() === agentName.toLowerCase())?.[1]\n 97:     ?? {}\n 98: }\n 99: \n100: \n"
    }
  }
]

[Message 82] role: assistant
[
  {
    "type": "tool-call",
    "toolCallId": "toolu_01UDm17gD99dJMVRrXYzjKb3",
    "toolName": "read",
    "input": {
      "filePath": "/Users/ufukaltinok/Work/OSS/opencode/packages/opencode/src/session/prompt.ts",
      "startLine": 93,
      "endLine": 160
    }
  }
]

[Message 83] role: tool
[
  {
    "type": "tool-result",
    "toolCallId": "toolu_01UDm17gD99dJMVRrXYzjKb3",
    "toolName": "read",
    "output": {
      "type": "text",
      "value": "§362§  93:   export const PromptInput = z.object({\n 94:     sessionID: SessionID.zod,\n 95:     messageID: MessageID.zod.optional(),\n 96:     model: z\n 97:       .object({\n 98:         providerID: ProviderID.zod,\n 99:         modelID: ModelID.zod,\n100:       })\n101:       .optional(),\n102:     agent: z.string().optional(),\n103:     noReply: z.boolean().optional(),\n104:     tools: z\n105:       .record(z.string(), z.boolean())\n106:       .optional()\n107:       .describe(\n108:         \"@deprecated tools and permissions have been merged, you can set permissions on the session itself now\",\n109:       ),\n110:     format: MessageV2.Format.optional(),\n111:     system: z.string().optional(),\n112:     variant: z.string().optional(),\n113:     parts: z.array(\n114:       z.discriminatedUnion(\"type\", [\n115:         MessageV2.TextPart.omit({\n116:           messageID: true,\n117:           sessionID: true,\n118:         })\n119:           .partial({\n120:             id: true,\n121:           })\n122:           .meta({\n123:             ref: \"TextPartInput\",\n124:           }),\n125:         MessageV2.FilePart.omit({\n126:           messageID: true,\n127:           sessionID: true,\n128:         })\n129:           .partial({\n130:             id: true,\n131:           })\n132:           .meta({\n133:             ref: \"FilePartInput\",\n134:           }),\n135:         MessageV2.AgentPart.omit({\n136:           messageID: true,\n137:           sessionID: true,\n138:         })\n139:           .partial({\n140:             id: true,\n141:           })\n142:           .meta({\n143:             ref: \"AgentPartInput\",\n144:           }),\n145:         MessageV2.SubtaskPart.omit({\n146:           messageID: true,\n147:           sessionID: true,\n148:         })\n149:           .partial({\n150:             id: true,\n151:           })\n152:           .meta({\n153:             ref: \"SubtaskPartInput\",\n154:           }),\n155:       ]),\n156:     ),\n157:   })\n158:   export type PromptInput = z.infer<typeof PromptInput>\n159: \n160:   export const prompt = fn(PromptInput, async (input) => {\n\n(Showing lines 93-160 of 1969. Use startLine/endLine to read other sections.)"
    }
  }
]

[Message 84] role: assistant
[
  {
    "type": "reasoning",
    "text": "OK I see the `tools` field on PromptInput (line 104-109). It's `Record<string, boolean>` and is marked as **@deprecated** — tools and permissions have been merged.\n\nSo the flow for setting tool visibility is:\n1. `session.prompt({ tools: { council_finalize: true } })` → stored on user message → used by llm.ts resolveTools\n2. Agent permission rules → used by PermissionNext.disabled\n\nBoth mechanisms work through `llm.ts resolveTools`.\n\nI've exhausted my investigation into OpenCode internals. Let me take a completely pragmatic approach now.\n \nWait, the evidence is clear: `config.tools.X = false` actually does hide tools from everyone, while leaving it unset makes them visible. So this field isn't dead at all. Maybe the Zod transform runs whenever the config is accessed, or perhaps the state factory applies the transform and our mutations affect the shared state object that other components read from.\n\nActually, I need to look at what's really happening in resolveTools — it's checking `input.user.tools`, which refers to the tools field from the user's message, not the global config. That's the key difference I was missing. I've confirmed that with `config.permission.prepare_council_prompt = \"deny\"`, the disabled check should filter out sisyphus's council tools. But I really need to test this empirically rather than keep theorizing—let me propose a concrete solution and have the user verify it works. Actually, stepping back, the user's complaint that \"you currently have all these tools which normally you shouldn't have\" might point to a simpler issue: either the tools are showing up in the model's tool list sent to Claude, or they're visible in the TUI's tool display regardless of permissions. If it's the latter, the TUI might be listing all registered tools while permissions only filter what reaches the model.\n\nGiven how long I've been going in circles, let me propose a straightforward approach: restore `config.tools.X = false` for global hiding, then have athena and athena-junior sessions explicitly pass `tools: { council_finalize: true }` through their session prompt to override the global setting, which means adding allow entries to the tool restrictions file. I need to verify whether `session.prompt({ tools: { X: true } })` actually overrides `config.tools.X = false` by checking the resolveTools logic in llm.ts.\n\nLooking at the code, when `input.user.tools[X]` is true and `config.tools.X` is false, the deletion check won't trigger because it's looking for false values. But I keep circling back to where `config.tools.X = false` actually takes effect — it gets converted to a permission deny during config parsing, but our plugin sets it after parsing, so that conversion might not apply unless the config gets re-parsed somewhere.\n\nLet me trace through how `Instance.state()` retrieves and applies these settings.",
    "providerOptions": {
      "anthropic": {
        "signature": ""
      }
    }
  },
  {
    "type": "tool-call",
    "toolCallId": "toolu_01WRDiBPsyxgE7JzZgwWJTPX",
    "toolName": "ctx_reduce",
    "input": {
      "drop": "306,307,308,309,312,313,314,315,316,317,318,319,320,321,322,323,325,326,327,328,329,330,332,333,334,335,337,338,339,340,341,342,343,344,345,346,347,348,349,350,351,352,353,354,355,356,357,358,359,360,361,362"
    }
  }
]

[Message 85] role: tool
[
  {
    "type": "tool-result",
    "toolCallId": "toolu_01WRDiBPsyxgE7JzZgwWJTPX",
    "toolName": "ctx_reduce",
    "output": {
      "type": "text",
      "value": "§363§ Queued: drop §306§, §307§, §308§, §309§, §312§, §313§, §314§, §315§, §316§, §317§, §318§, §319§, §320§, §321§, §322§, §323§, §325§, §326§, §327§, §328§, §329§, §330§, §332§, §333§, §334§, §335§, §337§, §338§, §339§, §340§, §341§, §342§, deferred drop §343§, §344§, §345§, §346§, §347§, §348§, §349§, §350§, §351§, §352§, §353§, §354§, §355§, §356§, §357§, §358§, §359§, §360§, §361§, §362§."
    }
  }
]

[Message 86] role: assistant
[
  {
    "type": "text",
    "text": "§364§ I've done an exhaustive deep dive into OpenCode's internals. Here's my honest assessment:\n\n**Theory says the permission approach should work.** `config.permission.prepare_council_prompt = \"deny\"` feeds into every agent's permission ruleset via `PermissionNext.fromConfig(cfg.permission)` → `disabled()` → tools removed in `llm.ts resolveTools`. Per-agent `permission.X = \"allow\"` overrides via `findLast`.\n\n**But you're telling me it doesn't work in practice.** I've been going in circles trying to find the gap between theory and practice.\n\nRather than keep guessing, I think we should:\n\n1. **Add temporary debug logging** to verify our config.permission changes actually reach the agent's permission ruleset at runtime\n2. **OR** take a completely different approach — filter tools at registration time instead of relying on OpenCode's permission system"
  },
  {
    "type": "tool-call",
    "toolCallId": "toolu_01HFR872v8LvPcXV7tQB1opk",
    "toolName": "question",
    "input": {
      "questions": [
        {
          "question": "How should we approach fixing the council tool visibility?",
          "header": "Fix approach",
          "options": [
            {
              "label": "Debug first",
              "description": "Add temp logging to verify config.permission flows correctly into agent permission rulesets. This tells us exactly where the chain breaks."
            },
            {
              "label": "Filter at registration",
              "description": "Make the tool init function agent-aware — return null/skip for non-athena agents. Bypasses the permission system entirely."
            },
            {
              "label": "Dual mechanism",
              "description": "Put config.tools=false back (global hide), AND add council_finalize:true to agent-tool-restrictions.ts for athena agents (session.prompt path). Belt-and-suspenders."
            }
          ]
        }
      ]
    }
  }
]

[Message 87] role: tool
[
  {
    "type": "tool-result",
    "toolCallId": "toolu_01HFR872v8LvPcXV7tQB1opk",
    "toolName": "question",
    "output": {
      "type": "text",
      "value": "§365§ User has answered your questions: \"How should we approach fixing the council tool visibility?\"=\"Ask oracle to check the opencode codebase at ~/Work/OSS/opencode to get a definitive answer\". You can now continue with the user's answers in mind."
    }
  }
]

[Message 88] role: assistant
[
  {
    "type": "reasoning",
    "text": "The user wants me to consult Oracle to analyze the OpenCode codebase and get a definitive answer about how tool visibility works and why our permission approach isn't working.",
    "providerOptions": {
      "anthropic": {
        "signature": ""
      }
    }
  },
  {
    "type": "text",
    "text": "§366§ Consulting Oracle to analyze the OpenCode codebase and get a definitive answer on how tool visibility/filtering actually works end-to-end."
  },
  {
    "type": "tool-call",
    "toolCallId": "toolu_01UwwamTMPuN2xqcCqaWhGt9",
    "toolName": "task",
    "input": {
      "subagent_type": "oracle",
      "load_skills": [],
      "description": "Analyze OpenCode tool visibility",
      "prompt": "CONTEXT: I'm working on the oh-my-opencode plugin (at /Users/ufukaltinok/Work/OSS/oh-my-opencode) which extends OpenCode (at /Users/ufukaltinok/Work/OSS/opencode). We register 3 tools globally via the plugin's `tool` hook: `prepare_council_prompt`, `council_finalize`, `athena_council`. These should ONLY be visible to athena and athena-junior agents. All other agents (sisyphus, hephaestus, prometheus, etc.) should NOT see them.\n\nPROBLEM: We tried two approaches and neither works correctly:\n1. Setting `config.tools = { prepare_council_prompt: false, council_finalize: false, athena_council: false }` in our plugin's config hook → hides from EVERYONE including athena (can't override per-agent)\n2. Setting `config.permission = { prepare_council_prompt: \"deny\", ... }` globally + per-agent `permission = { prepare_council_prompt: \"allow\" }` for athena → tools still visible to sisyphus\n\nGOAL: Find the DEFINITIVE answer for how OpenCode's tool visibility works end-to-end. Specifically:\n\n1. Trace the EXACT code path from plugin config hook → tool registration → tool resolution → what's sent to the model. The key files are:\n   - /Users/ufukaltinok/Work/OSS/opencode/packages/opencode/src/plugin/index.ts (Plugin.init, config hook call)\n   - /Users/ufukaltinok/Work/OSS/opencode/packages/opencode/src/config/config.ts (Zod parse, tools→permission conversion at lines 229-241)\n   - /Users/ufukaltinok/Work/OSS/opencode/packages/opencode/src/agent/agent.ts (Agent.state, permission building)\n   - /Users/ufukaltinok/Work/OSS/opencode/packages/opencode/src/tool/registry.ts (ToolRegistry.tools, plugin tool loading)\n   - /Users/ufukaltinok/Work/OSS/opencode/packages/opencode/src/session/prompt.ts (resolveTools at line 743)\n   - /Users/ufukaltinok/Work/OSS/opencode/packages/opencode/src/session/llm.ts (resolveTools at line 258, the FILTERING)\n   - /Users/ufukaltinok/Work/OSS/opencode/packages/opencode/src/permission/next.ts (disabled function at line 246)\n\n2. Answer these SPECIFIC questions:\n   a. When our plugin sets `config.tools = { X: false }` in the config hook (AFTER Zod parsing), does this value get read by ANYTHING downstream? Or is it a dead field?\n   b. When our plugin sets `config.permission = { X: \"deny\" }`, does this end up in agent permission rulesets via `PermissionNext.fromConfig(cfg.permission)`?\n   c. Is there a timing issue where Agent.state() might initialize BEFORE Plugin.init() finishes mutating the config?\n   d. In the `disabled()` function (next.ts line 246), for a tool \"prepare_council_prompt\" with a deny rule in the ruleset, will `findLast` correctly find the deny rule and add it to the disabled set?\n   e. Does the `resolveTools` in llm.ts (line 258) actually get called for PRIMARY agent sessions (TUI sessions), or is there a different code path?\n\n3. Based on your analysis, what is the CORRECT approach to hide plugin-registered tools from specific agents?\n\nREAD THE ACTUAL CODE. Don't speculate. I need definitive answers.",
      "run_in_background": true
    }
  }
]

[Message 89] role: tool
[
  {
    "type": "tool-result",
    "toolCallId": "toolu_01UwwamTMPuN2xqcCqaWhGt9",
    "toolName": "task",
    "output": {
      "type": "text",
      "value": "§367§ Background task launched.\n\nBackground Task ID: bg_7cf2c1c3\nDescription: Analyze OpenCode tool visibility\nAgent: oracle\nStatus: pending\n\nSystem notifies on completion. Use `background_output` with task_id=\"bg_7cf2c1c3\" to check.\n\n<task_metadata>\nsession_id: ses_2d4c6e101ffe5Kj54sO54szVOC\ntask_id: ses_2d4c6e101ffe5Kj54sO54szVOC\nbackground_task_id: bg_7cf2c1c3\n</task_metadata>\n\nto continue: task(session_id=\"ses_2d4c6e101ffe5Kj54sO54szVOC\", prompt=\"...\")"
    }
  }
]

[Message 90] role: assistant
[
  {
    "type": "text",
    "text": "§368§ Oracle is analyzing the OpenCode codebase. I'll collect the results when it's done — waiting for the completion notification."
  }
]

[Message 91] role: user
[
  {
    "type": "text",
    "text": "§369§ <system-reminder>\n[ALL BACKGROUND TASKS COMPLETE]\n\n**Completed:**\n- `bg_7cf2c1c3`: Analyze OpenCode tool visibility\n\nUse `background_output(task_id=\"<id>\")` to retrieve each result.\n</system-reminder>\n<!-- OMO_INTERNAL_INITIATOR -->"
  }
]

[Message 92] role: assistant
[
  {
    "type": "tool-call",
    "toolCallId": "toolu_01VGNo5U4j6WeLwB2mJyJMJx",
    "toolName": "background_output",
    "input": {
      "task_id": "bg_7cf2c1c3"
    }
  }
]

[Message 93] role: tool
[
  {
    "type": "tool-result",
    "toolCallId": "toolu_01VGNo5U4j6WeLwB2mJyJMJx",
    "toolName": "background_output",
    "output": {
      "type": "text",
      "value": "§370§ Task Result\n\nTask ID: bg_7cf2c1c3\nDescription: Analyze OpenCode tool visibility\nDuration: 11m 27s\nSession ID: ses_2d4c6e101ffe5Kj54sO54szVOC\n\n---\n\n**Planning code analysis**\n\nI need to focus on analyzing actual code, which likely means inspecting files in the opencode repository outside my current working directory. Tools will be essential here. I’m thinking of possibly leveraging “git-master” skills, but since this is code analysis and not just git operations, I might use read and grep strategically. I’ll need to inspect the specified files and gather all relevant lines. Using parallel reads on the exact files seems like a good plan!\n\n**Investigating plugin tools**\n\nI’m considering the plugin hook tool registration details in @opencode-ai/plugin. It seems like plugin exports might offer tool definitions in returned hooks.tool, but the ToolRegistry directly reads from Plugin.list() hooks. I wonder if I need to see the types — maybe it’s not essential right now. My next step is to inspect the bootstrap order to get a clearer picture. Let’s take a look!\n\n**Verifying state caching**\n\nI need to check the caching semantics of state in instances, specifically whether Instance.state is indeed caching results. It could be helpful to inspect Instance.state. I wonder if it’s also worth looking into what the agent state uses, maybe see if Agent.state can initialize earlier through plugin tool definitions. Bootstrap calls Plugin.init before any session, and Plugin.init loads plugins by calling Plugin.state, which might involve Config.get. I need to identify the exact code path from the config hook to the model.\n\n**Analyzing plugin initialization**\n\nFirst, Plugin.init retrieves the config using `await Config.get()`, and each hook has the ability to mutate the cached config object with `hook.config?.(config)`. Then, SessionPrompt.loop gets the agent through `Agent.get()`, leading to `Agent.state()` and another call to `Config.get()` for the now mutated config. Built-in agents combine defaults with user settings, while custom agents merge settings using `PermissionNext.fromConfig(value.permission)`. ToolRegistry.tools() gathers both built-ins and plugin tool definitions, which transforms tools into AI SDK tools without any permission filtering.\n\n**Identifying potential issues**\n\nI’m thinking about how Agent.state behaves as a lazy cached singleton. If someone calls Agent.get/list/defaultAgent before bootstrap/Plugin.init in a nonstandard path, stale rules might stick around for that instance. It's also important to ensure that CLI bootstrap wrappers call InstanceBootstrap before executing commands. So, my conclusion is that this issue shouldn’t happen in the normal supported path.\n\n**Considering tool IDs and registration**\n\nI’m pondering whether the issue lies in the tool ID mismatch due to naming. It seems I need to check the plugin tool registration names to see if they’re registered differently. The user mentioned that the IDs are exactly as they should be, but maybe there's still some oversight. I want to make sure I'm thorough in my troubleshooting so I can resolve this effectively.\n\n**Evaluating bootstrap order**\n\nI need to check if the bootstrap order guarantees that Plugin.init is called before any agent use in a normal session. Since InstanceBootstrap waits for Plugin.init, and the Agent isn’t initialized there, I don’t see a timing issue in standard bootstrap. However, I'm curious if there's any plugin config hook after agent state caches. If Agent.state was initialized before Plugin.init, changes wouldn’t reflect. I should investigate cli/bootstrap or server routes to confirm the correct bootstrap sequence.\n\n**Inspecting session tools**\n\nI think it’s a good idea to delve deeper into the session/prompt resolveTools, especially lines 790 onward, to understand the tool execution context and how it merges session permissions. While I already have lines around 790-812, it might help to check beyond that, though I’m not sure if it’s strictly necessary. I should note that the session prompt resolveTools doesn’t filter tools before building AITools. I’ll inspect more lines after 790-830 to confirm there’s no permission filtering present.\n\n**Evaluating tool visibility**\n\nI’m trying to understand how tool visibility works in the model, particularly regarding llm.ts and resolveTools. I need to determine if the answer is called during primary sessions. Since SessionPrompt.loop calls processor.process and the processor likely employs L, I should inspect processor.process further. It seems I need to read around the relevant calls to get a clearer picture of how everything connects. There's definitely a lot to unpack here!\n\n**Analyzing plugin code**\n\nI need to find a definitive answer from the actual code. I should inspect the oh-my-opencode plugin tool's config handler and the tool registry to see what they're doing and figure out if my approach is wrong. Tracing the exact code path from end to end might help, focusing on relevant plugin config hook handlers. It's important to read those files carefully, avoiding any unnecessary operations. I'll stick to using the tools I have for this analysis.\n\n**Inspecting config paths**\n\nI feel ready to answer definitively now. I need to trace the code path and address specific questions a-e while ensuring the correct approach. It might be helpful to check the oh-my-opencode config handler to ensure the plugin config hook properly mutates the configurations and agents after parsing. I’ll look into createConfigHandler too and see how it applies to the config hook. Maybe just reading config-handler.ts and the plugin interface will give me enough insight? Let's take a quick look!\n\n**Evaluating the plugin structure**\n\nIt seems I have enough information to provide a clear answer. I might want to inspect the oh-my-opencode create/managers or the main plugin interface to verify the hook structure, but that might not be necessary. \n\nThe user's inquiry focuses on the OpenCode path, so I could mention how the plugin config hook modifies the config object after it's parsed. The plugin structure indicates that Plugin.state loads Config.get first, followed by hooks, and that any modifications to config persist globally unless the instance is disposed.\n\n**Verifying configuration structure**\n\nI need to check if Config.state is using Instance.state, as it likely caches the object. The get method returns state().then(x => x.config), meaning the plugin config hook modifies the shared config object after parsing.\n\nFor tool registration, Plugin.list retrieves hooks in Plugin.state, including tool definitions. ToolRegistry.state loads these tools and initializes them. SessionPrompt.resolveTools builds AITools from these and passes them to LLM.stream. I should investigate SessionPrompt.resolveTools further to see if it filters based on agent permissions since it collects all tools, including those from plugins and MCP, before LLM.resolveTools applies any filters.\n\n**Analyzing tool resolution process**\n\nSessionPrompt.resolveTools combines agent.permission and session.permission when asking prompts during execution. However, there isn’t any visibility filtering, as ToolRegistry.tools returns all tools, which leads to the possibility that a denied tool could still be sent to the model if LLM.resolveTools isn’t filtering properly. \n\nI need to carefully analyze the semantics of the disabled() function. It uses Object.keys(input.tools) and input.agent.permission instead of session.permission, mapping actions like write/edit to edit, and only considers matching permission wildcards while ignoring patterns.\n\n**Investigating code paths**\n\nI need to focus on providing a clear, definitive answer to the user. It seems necessary to briefly mention what I found from the tools before proceeding. \n\nI should inspect the agent configuration handler, but it might not be crucial. Understanding how the plugin config hook mutates the config after it's parsed and cached is important. I see that `Config.get` returns the same object reference from the cache, and the `hook.config` mutates that object directly. \n\nI’ll also look into the timing of the bootstrap order.\n\n**Analyzing plugin state**\n\nThe agent state seems lazy, indicating there's no timing issue during the normal bootstrap. However, I need to consider that `Plugin.state` loads plugins by calling `Config.get` before the initialization. The config hooks, which mutate after the plugin loads in the `init`, may not be an issue since `Agent.state` isn't called before `Plugin.init` because of the bootstrap order. \n\nI should trace the flow from the plugin config hook through tool resolution to model while highlighting key steps, like how `Config.state` processes files and `Plugin.init` runs hooks.\n\n**Examining configuration interactions**\n\nIt seems that the system ignores `cfg.tools` and the per-agent configurations except during the parsing transform of the Agent schema when the agent configs come from an actual parsed config file. However, the plugin config hook mutates a plain object, potentially bypassing the Agent schema transformation, which is important to note! \n\nI need to inspect the transformation process and the `plugin agentResult`. In the oh-my plugin hook, it appears that `applyAgentConfig` might be setting the `config.agent` entries directly to objects. It’s possible these objects already match the final schema expected by `agent.ts`. In `Agent.state`, it iterates through `cfg.agent` and uses `PermissionNext.fromConfig`.\n\n**Planning response structure**\n\nThe developer has provided an output format that includes a bottom line, action plan, and effort estimate, but the user is asking for just 1-3 details. I think the answer should be structured into sections: Bottom line, Code path, Direct answers, and the Correct approach. I need to keep it concise yet definitive, possibly exceeding defaults while ensuring clarity. I'll also include the actual path and notes about what I found before proceeding.\n\n**Considering tool findings**\n\nI think I need to include what I found after using the tools. It’s important to summarize the results I gather so that the user has a clear understanding of the information. I want to ensure that my findings are useful and relevant. It's like capturing the gist of everything I've learned through the process, making sure the user doesn't miss anything important! Let’s keep that in mind for future interactions!\n\n**Evaluating tool permissions**\n\nI'm considering whether there might be an issue due to mismatched tool names or permission IDs because of case sensitivity, but that seems unlikely. Another thought is whether applying the `applyToolConfig` with a global deny could be overridden by something else or maybe by the user's configuration later on. It looks like the order of the configuration handlers is important: `applyAgentConfig`, then `applyToolConfig`, and so on. So, that’s something to keep in mind!\n\n**Investigating code paths**\n\nI think I need to provide a definitive answer to the user with the exact code path and specific questions. I might need to inspect the create/managers or plugin interface to understand the oh-my-opencode config hook registration. But maybe I can just focus on the visibility path for now. I might need to verify plugin tool hook definitions since I noticed a missing plugin in oh-my-opencode. I should check the root plugin entry as well.\n\n**Evaluating Agent and Plugin initialization**\n\nI’m trying to understand if Agent.state could initialize at the same time as Plugin.init, especially if something calls Agent.get during the config hook. In the plugin config handler, there's no call to Agent.get. During startup, Agent.state isn’t modified before Plugin.init happens. I don’t see any evidence of a race condition in normal startup. However, since Config.get returns a mutable cached object, if some code got the config before Plugin.init and Agent.state initialized, that could be a possibility. I should look at Instance.state to clarify caches and order.\n\n**Investigating initialization issues**\n\nI think we have enough information to answer definitively. I should read the open code config handler to see if the plugin initialization changes the config after Config.get. There’s a timing issue to consider with caching and initialization order. Plugin.init is called before other initializations, but Agent.state may only run later. If Agent.state runs after Plugin.init finishes, it will see the mutated state. I need to check if there's a way for Agent.state to initialize before Plugin.init completes.\n\n**Analyzing initialization flows**\n\nI’m considering whether createTools/createHooks could call createBuiltinAgents within the plugin itself, but that’s more internal to oh-my-opencode. I can base my answers on the OpenCode main bootstrap: during normal instance bootstrap, Plugin.init() is awaited before further initialization. For primary TUI sessions, Agent.state doesn’t invoke until the session prompt, so it won’t occur before Plugin.init finishes. I haven’t seen any standard code paths that call Agent.get beforehand, which seems definitive enough for my answer. Additionally, config.tools in the plugin config hook are processed after Zod parsing.\n\n**Examining config.tools**\n\nIt seems that setting config.tools won't lead to any conversion during lines 229-241 because that conversion has already occurred during the Config.state load. My downstream search only shows remaining reads related to config.tools in opencode—like in config.ts conversion and at the session prompt, which isn’t really connected. It looks like config.tools is a dead field for runtime visibility since its only uses are for conversion or user tools. Though it's part of the schema, no reads happen after the config hook. So, my answer is that it's effectively dead for core runtime.\n\n**Analyzing plugin permissions**\n\nI think the issue might arise from them setting permissions on their plugin’s config schema rather than the OpenCode config object. In the tool-config-handler, it looks like they do mutate the config and agentResult correctly. However, they may be setting agent.tools for athena unnecessarily, expecting the deny list to hide it from others. But, the agent.tools field in cfg.agent only converts to permission during the initial Zod parse. They mutate athena.tools=true after parsing, so that field seems inactive now, but athena.permission should still function.\n\n**Evaluating agent naming issues**\n\nI'm wondering if there's a problem with using display names versus internal names. I need to inspect the createBuiltinAgents or agent display names, particularly the `name` field. The tool-config-handler uses agentByKey with display names, which helps it find objects. When building results from cfg.agent, it uses display names as the key. If sessions use the agent name \"athena,\" Agent.get(\"athena\") wouldn't locate it. But, it seems like oh-my probably stores display names, so we may not need to worry about this.\n\n**Considering plugin initialization**\n\nI’m wondering if some plugin code could call `Agent.get()` during initialization before the config hook runs. If that’s the case, stale cache might be a possibility. However, from the files I've read, it seems like the OpenCode core isn’t doing that in the normal execution path. It's interesting to think about how initialization could affect caching and reliability, but I feel reassured that everything seems set up correctly based on my findings.\n\n**Estimating effort for changes**\n\nI’m estimating the effort for a plugin change. It looks like a quick fix, but if it requires a core change for the UI, then it becomes a medium effort. My primary recommendation is to focus on cleaning up the plugin configuration quickly. I should mention alternatives only if they're meaningful, especially if a change to the core is needed for tools to be hidden from the UI. It might be worth noting that the current core doesn’t support per-agent plugin tool registration.\n\n**Ensuring concise answers**\n\nI want to make sure my answers are clear and concise. Using bullet points could help present questions more effectively. I should also note that there aren't any normal timing issues unless Agent.state is forced early. It might be helpful to mention that a “standard boot sequence avoids timing problems.” That seems like a good approach to clarify things! I'll keep this in mind for crafting responses.\n\n**Bottom line:** I read the actual OpenCode path. `config.tools` in a plugin `config` hook is effectively dead for runtime tool visibility, and the final model-facing filter is `session/llm.ts::resolveTools()`, which only removes tools based on `input.user.tools` and `input.agent.permission`.  \nSo the correct model-visibility approach is: register the tools globally, deny them globally in `config.permission`, and re-allow them only on the Athena agent entries in `config.agent[*].permission`; if you need them hidden from the global registry/UI itself, that requires an OpenCode core change.\n\n**Action plan:**\n1. Stop relying on `config.tools` and `agent.tools` in your plugin `config` hook for runtime hiding. Those fields are only converted during config parsing, which has already happened before plugin hooks run.\n2. Keep `prepare_council_prompt`, `council_finalize`, and `athena_council` registered globally through the plugin `tool` hook. That is how OpenCode loads plugin tools.\n3. In your plugin `config` hook, set:\n   `config.permission.{prepare_council_prompt,council_finalize,athena_council} = \"deny\"`.\n4. In the actual Athena and Athena-Junior entries under `config.agent`, set:\n   `permission.{prepare_council_prompt,council_finalize,athena_council} = \"allow\"`.\n5. Verify against the exact agent entries OpenCode will consume after your remapping logic. `Agent.state()` reads `cfg.agent` as-is and only looks at `.permission`, not `.tools`.\n6. If Sisyphus still “sees” them, check whether you mean “in the global registry/debug UI” vs “sent to the model”. The former stays global; the latter is filtered in `session/llm.ts`.\n7. If you want true per-agent registration, patch OpenCode earlier than `LLM.resolveTools()` - for example in `ToolRegistry.tools()` or `SessionPrompt.resolveTools()`.\n\n**Effort estimate:** Quick\n\n### Exact path\nBoot path: `project/bootstrap.ts:16-18` awaits `Plugin.init()`. `Plugin.init()` gets the cached config object from `Config.get()` and runs every plugin `config` hook against that object in place (`plugin/index.ts:133-139`).\n\nTool registration path: plugin tools are loaded globally from `Plugin.list()` inside `ToolRegistry.state()` (`tool/registry.ts:55-60`), then instantiated by `ToolRegistry.tools()` (`tool/registry.ts:132-173`).\n\nAgent permission path: `Agent.state()` reads `cfg.permission` via `PermissionNext.fromConfig(cfg.permission ?? {})` (`agent/agent.ts:75`) and merges per-agent overrides from `cfg.agent[*].permission` afterward (`agent/agent.ts:206-233`).\n\nModel-send path: `SessionPrompt.loop()` gets the agent and builds tools (`session/prompt.ts:560-685`, `743-930`), but **does not filter by permission there**. The real filter is `LLM.stream()` -> local `resolveTools()` (`session/llm.ts:150, 258-265`), which removes tools using `PermissionNext.disabled()` (`permission/next.ts:246-255`) right before `streamText()` sends them to the model.\n\n### Specific answers\n**a. `config.tools = { X: false }` after Zod parsing: dead or used?**  \nDead for this runtime path. The only top-level `tools -> permission` conversion happens during `Config.get()` in `config/config.ts:229-241`. Your plugin `config` hook runs later, so mutating `config.tools` there does not get converted or read downstream.\n\n**b. `config.permission = { X: \"deny\" }`: does it enter agent rulesets?**  \nYes. `Agent.state()` explicitly does `PermissionNext.fromConfig(cfg.permission ?? {})` at `agent/agent.ts:75`, and those rules are merged into every agent.\n\n**c. Could `Agent.state()` initialize before `Plugin.init()` mutates config?**  \nIn the normal boot path, no: `InstanceBootstrap()` awaits `Plugin.init()` first. But `Agent.state()` is cached, so if some code were to call `Agent.get()` before `Plugin.init()` finished, it would cache pre-hook permissions; I do not see that happening in the standard path you pointed to.\n\n**d. Will `disabled()` correctly deny `prepare_council_prompt`?**  \nYes. For a rule `{ permission: \"prepare_council_prompt\", pattern: \"*\", action: \"deny\" }`, `disabled()` will find it and add the tool to the disabled set. If there is a later matching allow rule, `findLast()` will return that instead, which is the intended override behavior.\n\n**e. Does `session/llm.ts::resolveTools()` run for primary/TUI sessions?**  \nYes. Regular session flow is `SessionPrompt.loop()` -> `SessionProcessor.process()` -> `LLM.stream()` -> `resolveTools()` -> `streamText()`. So this filter is on the primary session path, not just some side path.\n\n### Why this approach\n- It matches the actual implementation: registration is global, filtering is permission-based right before model invocation.\n- It uses the existing override semantics correctly: global deny, agent-local allow, last matching rule wins.\n- It avoids dead config fields that look plausible in tests but are ignored at runtime.\n\n### Watch out for\n- `agent.tools` set in your plugin `config` hook is also effectively dead for OpenCode runtime. `Config.Agent` only converts `tools` to `permission` during parse (`config/config.ts:772-784`), which has already happened.\n- `LLM.resolveTools()` checks `input.agent.permission`, not `session.permission`; session permission affects execution asks, not the model-visible tool list.\n- If by “visible” you mean a registry/debug listing, permissions will not remove the tool from `ToolRegistry`; they only stop it from being sent to the model.\n\n**Optional future consideration:** your current `tool-config-handler` tests appear to validate the mutated config shape, not the final `Agent.state()` / `LLM.resolveTools()` behavior. A runtime-level test would catch this class of issue directly."
    }
  }
]

[Message 94] role: assistant
[
  {
    "type": "reasoning",
    "text": "The Oracle confirmed everything I suspected:\n\n1. `config.tools` is dead after Zod parsing - our plugin mutations to it have no effect\n2. `config.permission` DOES enter agent rulesets - this should work\n3. No timing issue in normal boot path\n4. `disabled()` correctly finds deny rules\n5. `resolveTools` in llm.ts DOES run for primary/TUI sessions\n\nSo the permission approach SHOULD work. Yet the user says it doesn't.\n\nWait - there's one thing the Oracle didn't fully explore. Let me re-read the key finding:\n\n\nThe permission rules definitely get loaded into the agent state through `PermissionNext.fromConfig()`. But if the user is still seeing all the tools, either they haven't restarted after the code change, the tools are just visible in a UI listing without being sent to the model, or something else is preventing the deny rules from actually taking effect. So the real issue is that we've already made the right changes—removing the dead `config.tools` field and keeping the permission-based approach with global deny + per-agent allow—but the user hasn't actually tested it in a fresh session yet. They're seeing the tools listed and jumping to conclusions. I should just confirm that the permission setup is correct per the Oracle's guidance and have them restart OpenCode to verify it actually works.",
    "providerOptions": {
      "anthropic": {
        "signature": ""
      }
    }
  },
  {
    "type": "text",
    "text": "§371§ Oracle's analysis is definitive. Here's the answer:\n\n**The permission approach IS correct.** Oracle confirmed every step of the chain:\n\n1. `config.tools = false` → **dead field** after Zod parsing. Our plugin mutations to it have zero effect. Remove it.\n2. `config.permission.X = \"deny\"` → **DOES enter agent rulesets** via `Agent.state()` → `PermissionNext.fromConfig(cfg.permission)`. Every agent gets the deny rule.\n3. Per-agent `permission.X = \"allow\"` → **overrides via `findLast()`** for athena/athena-junior. Correct.\n4. `agent.tools = true` → **also dead field** (agent-level tools→permission conversion only happens during Zod parsing). Harmless but useless — should remove.\n5. `LLM.resolveTools()` → **runs for primary/TUI sessions**. This is the real filter.\n6. No timing issue in normal boot path.\n\n**Our current code should already work.** We have `config.permission.X = \"deny\"` globally + per-agent `permission.X = \"allow\"` for athena. Oracle says this is the correct approach.\n\nThe likely explanation for \"you have all these tools\": either the session wasn't restarted after the code change, or the tools appear in a registry/debug listing that doesn't reflect permission filtering (permissions only remove tools at model invocation time in `LLM.resolveTools()`).\n\nLet me clean up the dead fields and verify:"
  },
  {
    "type": "tool-call",
    "toolCallId": "toolu_01Cvox17LEBTq1b4owwL46Yg",
    "toolName": "read",
    "input": {
      "filePath": "src/plugin-handlers/tool-config-handler.ts",
      "startLine": 120,
      "endLine": 170
    }
  }
]

[Message 95] role: tool
[
  {
    "type": "tool-result",
    "toolCallId": "toolu_01Cvox17LEBTq1b4owwL46Yg",
    "toolName": "read",
    "output": {
      "type": "text",
      "value": "§372§ 120:   }\n121:   // NOTE: Athena/council tool restrictions are also defined in:\n122:   // - src/agents/athena/agent.ts (AgentConfig permission format)\n123:   // - src/shared/agent-tool-restrictions.ts (boolean format for session.prompt)\n124:   // Keep all three in sync when modifying.\n125:   const athena = agentByKey(params.agentResult, \"athena\");\n126:   if (athena) {\n127:     athena.tools = {\n128:       ...athena.tools,\n129:       prepare_council_prompt: true,\n130:       council_finalize: true,\n131:       athena_council: true,\n132:     };\n133:     athena.permission = {\n134:       ...athena.permission,\n135:       task: \"allow\",\n136:       prepare_council_prompt: \"allow\",\n137:       council_finalize: \"allow\",\n138:       athena_council: \"allow\",\n139:       question: questionPermission,\n140:     };\n141:   }\n142:   const athenaJunior = agentByKey(params.agentResult, \"athena-junior\");\n143:   if (athenaJunior) {\n144:     athenaJunior.tools = {\n145:       ...athenaJunior.tools,\n146:       prepare_council_prompt: true,\n147:       council_finalize: true,\n148:       athena_council: true,\n149:     };\n150:     athenaJunior.permission = {\n151:       ...athenaJunior.permission,\n152:       prepare_council_prompt: \"allow\",\n153:       council_finalize: \"allow\",\n154:       athena_council: \"allow\",\n155:       question: \"deny\",\n156:     };\n157:   }\n158: \n159:   params.config.permission = {\n160:     webfetch: \"allow\",\n161:     external_directory: \"allow\",\n162:     ...(params.config.permission as Record<string, unknown>),\n163:     task: \"deny\",\n164:     prepare_council_prompt: \"deny\",\n165:     council_finalize: \"deny\",\n166:     athena_council: \"deny\",\n167:   };\n168: }\n"
    }
  }
]

[Message 96] role: assistant
[
  {
    "type": "tool-call",
    "toolCallId": "toolu_01FE64463N4RtV9515anCyHJ",
    "toolName": "edit",
    "input": {
      "filePath": "src/plugin-handlers/tool-config-handler.ts",
      "edits": [
        {
          "oldString": "  const athena = agentByKey(params.agentResult, \"athena\");\n  if (athena) {\n    athena.tools = {\n      ...athena.tools,\n      prepare_council_prompt: true,\n      council_finalize: true,\n      athena_council: true,\n    };\n    athena.permission = {\n      ...athena.permission,\n      task: \"allow\",\n      prepare_council_prompt: \"allow\",\n      council_finalize: \"allow\",\n      athena_council: \"allow\",\n      question: questionPermission,\n    };\n  }\n  const athenaJunior = agentByKey(params.agentResult, \"athena-junior\");\n  if (athenaJunior) {\n    athenaJunior.tools = {\n      ...athenaJunior.tools,\n      prepare_council_prompt: true,\n      council_finalize: true,\n      athena_council: true,\n    };\n    athenaJunior.permission = {\n      ...athenaJunior.permission,\n      prepare_council_prompt: \"allow\",\n      council_finalize: \"allow\",\n      athena_council: \"allow\",\n      question: \"deny\",\n    };\n  }",
          "newString": "  const athena = agentByKey(params.agentResult, \"athena\");\n  if (athena) {\n    athena.permission = {\n      ...athena.permission,\n      task: \"allow\",\n      prepare_council_prompt: \"allow\",\n      council_finalize: \"allow\",\n      athena_council: \"allow\",\n      question: questionPermission,\n    };\n  }\n  const athenaJunior = agentByKey(params.agentResult, \"athena-junior\");\n  if (athenaJunior) {\n    athenaJunior.permission = {\n      ...athenaJunior.permission,\n      prepare_council_prompt: \"allow\",\n      council_finalize: \"allow\",\n      athena_council: \"allow\",\n      question: \"deny\",\n    };\n  }"
        }
      ]
    }
  }
]

[Message 97] role: tool
[
  {
    "type": "tool-result",
    "toolCallId": "toolu_01FE64463N4RtV9515anCyHJ",
    "toolName": "edit",
    "output": {
      "type": "text",
      "value": "§373§ {\"id\":\"49\",\"success\":true,\"backup_id\":\"backup-2\",\"edits_applied\":1,\"file\":\"/Users/ufukaltinok/Work/OSS/oh-my-opencode/src/plugin-handlers/tool-config-handler.ts\",\"format_skipped_reason\":\"not_found\",\"formatted\":false,\"syntax_valid\":true}"
    }
  }
]

[Message 98] role: assistant
[
  {
    "type": "text",
    "text": "§374§ Now update the test to remove the dead `agent.tools` assertions:"
  },
  {
    "type": "tool-call",
    "toolCallId": "toolu_01KNiHFLHaNrtLmFgEM4AV39",
    "toolName": "edit",
    "input": {
      "filePath": "src/plugin-handlers/tool-config-handler.test.ts",
      "oldString": "    describe(\"#when applying tool config for Athena agents\", () => {\n      it.each([\"athena\", \"athena-junior\"])(\n        \"#then should expose council tools via agent tools for %s\",\n        (agentName) => {\n          const params = createParams({ agents: [agentName] })\n\n          applyToolConfig(params)\n\n          const agent = params.agentResult[agentName] as {\n            tools?: Record<string, unknown>\n            permission: Record<string, unknown>\n          }\n          expect(agent.tools?.prepare_council_prompt).toBe(true)\n          expect(agent.tools?.council_finalize).toBe(true)\n          expect(agent.tools?.athena_council).toBe(true)\n          expect(agent.permission.prepare_council_prompt).toBe(\"allow\")\n          expect(agent.permission.council_finalize).toBe(\"allow\")\n          expect(agent.permission.athena_council).toBe(\"allow\")\n        },\n      )\n    })",
      "newString": "    describe(\"#when applying tool config for Athena agents\", () => {\n      it.each([\"athena\", \"athena-junior\"])(\n        \"#then should allow council tools permission for %s\",\n        (agentName) => {\n          const params = createParams({ agents: [agentName] })\n\n          applyToolConfig(params)\n\n          const agent = params.agentResult[agentName] as {\n            permission: Record<string, unknown>\n          }\n          expect(agent.permission.prepare_council_prompt).toBe(\"allow\")\n          expect(agent.permission.council_finalize).toBe(\"allow\")\n          expect(agent.permission.athena_council).toBe(\"allow\")\n        },\n      )\n    })"
    }
  }
]

[Message 99] role: tool
[
  {
    "type": "tool-result",
    "toolCallId": "toolu_01KNiHFLHaNrtLmFgEM4AV39",
    "toolName": "edit",
    "output": {
      "type": "text",
      "value": "§375§ {\"id\":\"50\",\"success\":true,\"backup_id\":\"backup-3\",\"diff\":{\"additions\":1,\"after\":\"import { describe, it, expect, beforeEach, afterEach } from \\\"bun:test\\\"\\nimport { applyToolConfig } from \\\"./tool-config-handler\\\"\\nimport type { OhMyOpenCodeConfig } from \\\"../config\\\"\\n\\nfunction createParams(overrides: {\\n  taskSystem?: boolean\\n  agents?: string[]\\n  disabledTools?: string[]\\n}) {\\n  const agentResult: Record<string, { permission?: Record<string, unknown> }> = {}\\n  for (const agent of overrides.agents ?? []) {\\n    agentResult[agent] = { permission: {} }\\n  }\\n\\n  return {\\n    config: { tools: {}, permission: {} } as Record<string, unknown>,\\n    pluginConfig: {\\n      experimental: { task_system: overrides.taskSystem ?? false },\\n      disabled_tools: overrides.disabledTools,\\n    } as OhMyOpenCodeConfig,\\n    agentResult: agentResult as Record<string, unknown>,\\n  }\\n}\\n\\ndescribe(\\\"applyToolConfig\\\", () => {\\n  describe(\\\"#given config permission sets webfetch and external_directory\\\", () => {\\n    describe(\\\"#when applying tool config\\\", () => {\\n      it(\\\"#then should preserve explicit deny over OmO defaults\\\", () => {\\n        const params = createParams({})\\n        params.config.permission = {\\n          webfetch: \\\"deny\\\",\\n          external_directory: \\\"deny\\\",\\n        }\\n\\n        applyToolConfig(params)\\n\\n        const permission = params.config.permission as Record<string, unknown>\\n        expect(permission.webfetch).toBe(\\\"deny\\\")\\n        expect(permission.external_directory).toBe(\\\"deny\\\")\\n        expect(permission.task).toBe(\\\"deny\\\")\\n      })\\n\\n      it(\\\"#then should allow webfetch and external_directory by default\\\", () => {\\n        const params = createParams({})\\n\\n        applyToolConfig(params)\\n\\n        const permission = params.config.permission as Record<string, unknown>\\n        expect(permission.webfetch).toBe(\\\"allow\\\")\\n        expect(permission.external_directory).toBe(\\\"allow\\\")\\n        expect(permission.task).toBe(\\\"deny\\\")\\n      })\\n    })\\n  })\\n\\n  describe(\\\"#given task_system is enabled\\\", () => {\\n    describe(\\\"#when applying tool config\\\", () => {\\n      it(\\\"#then should deny todowrite and todoread globally\\\", () => {\\n        const params = createParams({ taskSystem: true })\\n\\n        applyToolConfig(params)\\n\\n        const tools = params.config.tools as Record<string, unknown>\\n        expect(tools.todowrite).toBe(false)\\n        expect(tools.todoread).toBe(false)\\n      })\\n\\n      it.each([\\n        \\\"atlas\\\",\\n        \\\"sisyphus\\\",\\n        \\\"hephaestus\\\",\\n        \\\"prometheus\\\",\\n        \\\"sisyphus-junior\\\",\\n      ])(\\\"#then should deny todo tools for %s agent\\\", (agentName) => {\\n        const params = createParams({\\n          taskSystem: true,\\n          agents: [agentName],\\n        })\\n\\n        applyToolConfig(params)\\n\\n        const agent = params.agentResult[agentName] as {\\n          permission: Record<string, unknown>\\n        }\\n        expect(agent.permission.todowrite).toBe(\\\"deny\\\")\\n        expect(agent.permission.todoread).toBe(\\\"deny\\\")\\n      })\\n    })\\n  })\\n\\n  describe(\\\"#given OPENCODE_CONFIG_CONTENT has question set to deny\\\", () => {\\n    let originalConfigContent: string | undefined\\n    let originalCliRunMode: string | undefined\\n\\n    beforeEach(() => {\\n      originalConfigContent = process.env.OPENCODE_CONFIG_CONTENT\\n      originalCliRunMode = process.env.OPENCODE_CLI_RUN_MODE\\n    })\\n\\n    afterEach(() => {\\n      if (originalConfigContent === undefined) {\\n        delete process.env.OPENCODE_CONFIG_CONTENT\\n      } else {\\n        process.env.OPENCODE_CONFIG_CONTENT = originalConfigContent\\n      }\\n      if (originalCliRunMode === undefined) {\\n        delete process.env.OPENCODE_CLI_RUN_MODE\\n      } else {\\n        process.env.OPENCODE_CLI_RUN_MODE = originalCliRunMode\\n      }\\n    })\\n\\n    describe(\\\"#when config explicitly denies question permission\\\", () => {\\n      it.each([\\\"sisyphus\\\", \\\"hephaestus\\\", \\\"prometheus\\\"])(\\n        \\\"#then should deny question for %s even without CLI_RUN_MODE\\\",\\n        (agentName) => {\\n          process.env.OPENCODE_CONFIG_CONTENT = JSON.stringify({\\n            permission: { question: \\\"deny\\\" },\\n          })\\n          delete process.env.OPENCODE_CLI_RUN_MODE\\n          const params = createParams({ agents: [agentName] })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.permission.question).toBe(\\\"deny\\\")\\n        },\\n      )\\n    })\\n\\n    describe(\\\"#when config does not deny question permission\\\", () => {\\n      it.each([\\\"sisyphus\\\", \\\"hephaestus\\\", \\\"prometheus\\\"])(\\n        \\\"#then should allow question for %s in interactive mode\\\",\\n        (agentName) => {\\n          process.env.OPENCODE_CONFIG_CONTENT = JSON.stringify({\\n            permission: { question: \\\"allow\\\" },\\n          })\\n          delete process.env.OPENCODE_CLI_RUN_MODE\\n          const params = createParams({ agents: [agentName] })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.permission.question).toBe(\\\"allow\\\")\\n        },\\n      )\\n    })\\n\\n    describe(\\\"#when CLI_RUN_MODE is true and config does not deny\\\", () => {\\n      it.each([\\\"sisyphus\\\", \\\"hephaestus\\\", \\\"prometheus\\\"])(\\n        \\\"#then should deny question for %s via CLI_RUN_MODE\\\",\\n        (agentName) => {\\n          process.env.OPENCODE_CONFIG_CONTENT = JSON.stringify({\\n            permission: {},\\n          })\\n          process.env.OPENCODE_CLI_RUN_MODE = \\\"true\\\"\\n          const params = createParams({ agents: [agentName] })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.permission.question).toBe(\\\"deny\\\")\\n        },\\n      )\\n    })\\n\\n    describe(\\\"#when config deny overrides CLI_RUN_MODE allow\\\", () => {\\n      it.each([\\\"sisyphus\\\", \\\"hephaestus\\\", \\\"prometheus\\\"])(\\n        \\\"#then should deny question for %s when config says deny regardless of CLI_RUN_MODE\\\",\\n        (agentName) => {\\n          process.env.OPENCODE_CONFIG_CONTENT = JSON.stringify({\\n            permission: { question: \\\"deny\\\" },\\n          })\\n          process.env.OPENCODE_CLI_RUN_MODE = \\\"false\\\"\\n          const params = createParams({ agents: [agentName] })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.permission.question).toBe(\\\"deny\\\")\\n        },\\n      )\\n    })\\n  })\\n\\n  describe(\\\"#given task_system is disabled\\\", () => {\\n    describe(\\\"#when applying tool config\\\", () => {\\n      it.each([\\n        \\\"atlas\\\",\\n        \\\"sisyphus\\\",\\n        \\\"hephaestus\\\",\\n        \\\"prometheus\\\",\\n        \\\"sisyphus-junior\\\",\\n      ])(\\\"#then should NOT deny todo tools for %s agent\\\", (agentName) => {\\n        const params = createParams({\\n          taskSystem: false,\\n          agents: [agentName],\\n        })\\n\\n        applyToolConfig(params)\\n\\n        const agent = params.agentResult[agentName] as {\\n          permission: Record<string, unknown>\\n        }\\n        expect(agent.permission.todowrite).toBeUndefined()\\n        expect(agent.permission.todoread).toBeUndefined()\\n      })\\n    })\\n  })\\n\\n  describe(\\\"#given disabled_tools includes 'question'\\\", () => {\\n    let originalConfigContent: string | undefined\\n    let originalCliRunMode: string | undefined\\n\\n    beforeEach(() => {\\n      originalConfigContent = process.env.OPENCODE_CONFIG_CONTENT\\n      originalCliRunMode = process.env.OPENCODE_CLI_RUN_MODE\\n      delete process.env.OPENCODE_CONFIG_CONTENT\\n      delete process.env.OPENCODE_CLI_RUN_MODE\\n    })\\n\\n    afterEach(() => {\\n      if (originalConfigContent === undefined) {\\n        delete process.env.OPENCODE_CONFIG_CONTENT\\n      } else {\\n        process.env.OPENCODE_CONFIG_CONTENT = originalConfigContent\\n      }\\n      if (originalCliRunMode === undefined) {\\n        delete process.env.OPENCODE_CLI_RUN_MODE\\n      } else {\\n        process.env.OPENCODE_CLI_RUN_MODE = originalCliRunMode\\n      }\\n    })\\n\\n    describe(\\\"#when question is in disabled_tools\\\", () => {\\n      it.each([\\\"sisyphus\\\", \\\"hephaestus\\\", \\\"prometheus\\\"])(\\n        \\\"#then should deny question for %s agent\\\",\\n        (agentName) => {\\n          const params = createParams({\\n            agents: [agentName],\\n            disabledTools: [\\\"question\\\"],\\n          })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.permission.question).toBe(\\\"deny\\\")\\n        },\\n      )\\n    })\\n\\n    describe(\\\"#when question is in disabled_tools alongside other tools\\\", () => {\\n      it.each([\\\"sisyphus\\\", \\\"hephaestus\\\", \\\"prometheus\\\"])(\\n        \\\"#then should deny question for %s agent\\\",\\n        (agentName) => {\\n          const params = createParams({\\n            agents: [agentName],\\n            disabledTools: [\\\"todowrite\\\", \\\"question\\\", \\\"interactive_bash\\\"],\\n          })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.permission.question).toBe(\\\"deny\\\")\\n        },\\n      )\\n    })\\n\\n    describe(\\\"#when disabled_tools does not include question\\\", () => {\\n      it.each([\\\"sisyphus\\\", \\\"hephaestus\\\", \\\"prometheus\\\"])(\\n        \\\"#then should allow question for %s agent\\\",\\n        (agentName) => {\\n          const params = createParams({\\n            agents: [agentName],\\n            disabledTools: [\\\"todowrite\\\", \\\"interactive_bash\\\"],\\n          })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.permission.question).toBe(\\\"allow\\\")\\n        },\\n      )\\n    })\\n  })\\n\\n  describe(\\\"#given council tools are globally hidden\\\", () => {\\n    describe(\\\"#when applying tool config for Athena agents\\\", () => {\\n      it.each([\\\"athena\\\", \\\"athena-junior\\\"])(\\n        \\\"#then should allow council tools permission for %s\\\",\\n        (agentName) => {\\n          const params = createParams({ agents: [agentName] })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.permission.prepare_council_prompt).toBe(\\\"allow\\\")\\n          expect(agent.permission.council_finalize).toBe(\\\"allow\\\")\\n          expect(agent.permission.athena_council).toBe(\\\"allow\\\")\\n        },\\n      )\\n    })\\n\\n    describe(\\\"#when applying tool config for non-Athena agents\\\", () => {\\n      it.each([\\\"atlas\\\", \\\"sisyphus\\\", \\\"prometheus\\\", \\\"hephaestus\\\", \\\"sisyphus-junior\\\"])(\\n        \\\"#then should deny council tools permission for %s\\\",\\n        (agentName) => {\\n          const params = createParams({ agents: [agentName] })\\n\\n          applyToolConfig(params)\\n\\n          const permission = params.config.permission as Record<string, unknown>\\n          const agent = params.agentResult[agentName] as {\\n            tools?: Record<string, unknown>\\n            permission: Record<string, unknown>\\n          }\\n          expect(permission.prepare_council_prompt).toBe(\\\"deny\\\")\\n          expect(permission.council_finalize).toBe(\\\"deny\\\")\\n          expect(permission.athena_council).toBe(\\\"deny\\\")\\n          expect(agent.tools?.prepare_council_prompt).toBeUndefined()\\n          expect(agent.tools?.council_finalize).toBeUndefined()\\n          expect(agent.tools?.athena_council).toBeUndefined()\\n        },\\n      )\\n    })\\n  })\\n})\\n\",\"before\":\"import { describe, it, expect, beforeEach, afterEach } from \\\"bun:test\\\"\\nimport { applyToolConfig } from \\\"./tool-config-handler\\\"\\nimport type { OhMyOpenCodeConfig } from \\\"../config\\\"\\n\\nfunction createParams(overrides: {\\n  taskSystem?: boolean\\n  agents?: string[]\\n  disabledTools?: string[]\\n}) {\\n  const agentResult: Record<string, { permission?: Record<string, unknown> }> = {}\\n  for (const agent of overrides.agents ?? []) {\\n    agentResult[agent] = { permission: {} }\\n  }\\n\\n  return {\\n    config: { tools: {}, permission: {} } as Record<string, unknown>,\\n    pluginConfig: {\\n      experimental: { task_system: overrides.taskSystem ?? false },\\n      disabled_tools: overrides.disabledTools,\\n    } as OhMyOpenCodeConfig,\\n    agentResult: agentResult as Record<string, unknown>,\\n  }\\n}\\n\\ndescribe(\\\"applyToolConfig\\\", () => {\\n  describe(\\\"#given config permission sets webfetch and external_directory\\\", () => {\\n    describe(\\\"#when applying tool config\\\", () => {\\n      it(\\\"#then should preserve explicit deny over OmO defaults\\\", () => {\\n        const params = createParams({})\\n        params.config.permission = {\\n          webfetch: \\\"deny\\\",\\n          external_directory: \\\"deny\\\",\\n        }\\n\\n        applyToolConfig(params)\\n\\n        const permission = params.config.permission as Record<string, unknown>\\n        expect(permission.webfetch).toBe(\\\"deny\\\")\\n        expect(permission.external_directory).toBe(\\\"deny\\\")\\n        expect(permission.task).toBe(\\\"deny\\\")\\n      })\\n\\n      it(\\\"#then should allow webfetch and external_directory by default\\\", () => {\\n        const params = createParams({})\\n\\n        applyToolConfig(params)\\n\\n        const permission = params.config.permission as Record<string, unknown>\\n        expect(permission.webfetch).toBe(\\\"allow\\\")\\n        expect(permission.external_directory).toBe(\\\"allow\\\")\\n        expect(permission.task).toBe(\\\"deny\\\")\\n      })\\n    })\\n  })\\n\\n  describe(\\\"#given task_system is enabled\\\", () => {\\n    describe(\\\"#when applying tool config\\\", () => {\\n      it(\\\"#then should deny todowrite and todoread globally\\\", () => {\\n        const params = createParams({ taskSystem: true })\\n\\n        applyToolConfig(params)\\n\\n        const tools = params.config.tools as Record<string, unknown>\\n        expect(tools.todowrite).toBe(false)\\n        expect(tools.todoread).toBe(false)\\n      })\\n\\n      it.each([\\n        \\\"atlas\\\",\\n        \\\"sisyphus\\\",\\n        \\\"hephaestus\\\",\\n        \\\"prometheus\\\",\\n        \\\"sisyphus-junior\\\",\\n      ])(\\\"#then should deny todo tools for %s agent\\\", (agentName) => {\\n        const params = createParams({\\n          taskSystem: true,\\n          agents: [agentName],\\n        })\\n\\n        applyToolConfig(params)\\n\\n        const agent = params.agentResult[agentName] as {\\n          permission: Record<string, unknown>\\n        }\\n        expect(agent.permission.todowrite).toBe(\\\"deny\\\")\\n        expect(agent.permission.todoread).toBe(\\\"deny\\\")\\n      })\\n    })\\n  })\\n\\n  describe(\\\"#given OPENCODE_CONFIG_CONTENT has question set to deny\\\", () => {\\n    let originalConfigContent: string | undefined\\n    let originalCliRunMode: string | undefined\\n\\n    beforeEach(() => {\\n      originalConfigContent = process.env.OPENCODE_CONFIG_CONTENT\\n      originalCliRunMode = process.env.OPENCODE_CLI_RUN_MODE\\n    })\\n\\n    afterEach(() => {\\n      if (originalConfigContent === undefined) {\\n        delete process.env.OPENCODE_CONFIG_CONTENT\\n      } else {\\n        process.env.OPENCODE_CONFIG_CONTENT = originalConfigContent\\n      }\\n      if (originalCliRunMode === undefined) {\\n        delete process.env.OPENCODE_CLI_RUN_MODE\\n      } else {\\n        process.env.OPENCODE_CLI_RUN_MODE = originalCliRunMode\\n      }\\n    })\\n\\n    describe(\\\"#when config explicitly denies question permission\\\", () => {\\n      it.each([\\\"sisyphus\\\", \\\"hephaestus\\\", \\\"prometheus\\\"])(\\n        \\\"#then should deny question for %s even without CLI_RUN_MODE\\\",\\n        (agentName) => {\\n          process.env.OPENCODE_CONFIG_CONTENT = JSON.stringify({\\n            permission: { question: \\\"deny\\\" },\\n          })\\n          delete process.env.OPENCODE_CLI_RUN_MODE\\n          const params = createParams({ agents: [agentName] })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.permission.question).toBe(\\\"deny\\\")\\n        },\\n      )\\n    })\\n\\n    describe(\\\"#when config does not deny question permission\\\", () => {\\n      it.each([\\\"sisyphus\\\", \\\"hephaestus\\\", \\\"prometheus\\\"])(\\n        \\\"#then should allow question for %s in interactive mode\\\",\\n        (agentName) => {\\n          process.env.OPENCODE_CONFIG_CONTENT = JSON.stringify({\\n            permission: { question: \\\"allow\\\" },\\n          })\\n          delete process.env.OPENCODE_CLI_RUN_MODE\\n          const params = createParams({ agents: [agentName] })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.permission.question).toBe(\\\"allow\\\")\\n        },\\n      )\\n    })\\n\\n    describe(\\\"#when CLI_RUN_MODE is true and config does not deny\\\", () => {\\n      it.each([\\\"sisyphus\\\", \\\"hephaestus\\\", \\\"prometheus\\\"])(\\n        \\\"#then should deny question for %s via CLI_RUN_MODE\\\",\\n        (agentName) => {\\n          process.env.OPENCODE_CONFIG_CONTENT = JSON.stringify({\\n            permission: {},\\n          })\\n          process.env.OPENCODE_CLI_RUN_MODE = \\\"true\\\"\\n          const params = createParams({ agents: [agentName] })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.permission.question).toBe(\\\"deny\\\")\\n        },\\n      )\\n    })\\n\\n    describe(\\\"#when config deny overrides CLI_RUN_MODE allow\\\", () => {\\n      it.each([\\\"sisyphus\\\", \\\"hephaestus\\\", \\\"prometheus\\\"])(\\n        \\\"#then should deny question for %s when config says deny regardless of CLI_RUN_MODE\\\",\\n        (agentName) => {\\n          process.env.OPENCODE_CONFIG_CONTENT = JSON.stringify({\\n            permission: { question: \\\"deny\\\" },\\n          })\\n          process.env.OPENCODE_CLI_RUN_MODE = \\\"false\\\"\\n          const params = createParams({ agents: [agentName] })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.permission.question).toBe(\\\"deny\\\")\\n        },\\n      )\\n    })\\n  })\\n\\n  describe(\\\"#given task_system is disabled\\\", () => {\\n    describe(\\\"#when applying tool config\\\", () => {\\n      it.each([\\n        \\\"atlas\\\",\\n        \\\"sisyphus\\\",\\n        \\\"hephaestus\\\",\\n        \\\"prometheus\\\",\\n        \\\"sisyphus-junior\\\",\\n      ])(\\\"#then should NOT deny todo tools for %s agent\\\", (agentName) => {\\n        const params = createParams({\\n          taskSystem: false,\\n          agents: [agentName],\\n        })\\n\\n        applyToolConfig(params)\\n\\n        const agent = params.agentResult[agentName] as {\\n          permission: Record<string, unknown>\\n        }\\n        expect(agent.permission.todowrite).toBeUndefined()\\n        expect(agent.permission.todoread).toBeUndefined()\\n      })\\n    })\\n  })\\n\\n  describe(\\\"#given disabled_tools includes 'question'\\\", () => {\\n    let originalConfigContent: string | undefined\\n    let originalCliRunMode: string | undefined\\n\\n    beforeEach(() => {\\n      originalConfigContent = process.env.OPENCODE_CONFIG_CONTENT\\n      originalCliRunMode = process.env.OPENCODE_CLI_RUN_MODE\\n      delete process.env.OPENCODE_CONFIG_CONTENT\\n      delete process.env.OPENCODE_CLI_RUN_MODE\\n    })\\n\\n    afterEach(() => {\\n      if (originalConfigContent === undefined) {\\n        delete process.env.OPENCODE_CONFIG_CONTENT\\n      } else {\\n        process.env.OPENCODE_CONFIG_CONTENT = originalConfigContent\\n      }\\n      if (originalCliRunMode === undefined) {\\n        delete process.env.OPENCODE_CLI_RUN_MODE\\n      } else {\\n        process.env.OPENCODE_CLI_RUN_MODE = originalCliRunMode\\n      }\\n    })\\n\\n    describe(\\\"#when question is in disabled_tools\\\", () => {\\n      it.each([\\\"sisyphus\\\", \\\"hephaestus\\\", \\\"prometheus\\\"])(\\n        \\\"#then should deny question for %s agent\\\",\\n        (agentName) => {\\n          const params = createParams({\\n            agents: [agentName],\\n            disabledTools: [\\\"question\\\"],\\n          })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.permission.question).toBe(\\\"deny\\\")\\n        },\\n      )\\n    })\\n\\n    describe(\\\"#when question is in disabled_tools alongside other tools\\\", () => {\\n      it.each([\\\"sisyphus\\\", \\\"hephaestus\\\", \\\"prometheus\\\"])(\\n        \\\"#then should deny question for %s agent\\\",\\n        (agentName) => {\\n          const params = createParams({\\n            agents: [agentName],\\n            disabledTools: [\\\"todowrite\\\", \\\"question\\\", \\\"interactive_bash\\\"],\\n          })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.permission.question).toBe(\\\"deny\\\")\\n        },\\n      )\\n    })\\n\\n    describe(\\\"#when disabled_tools does not include question\\\", () => {\\n      it.each([\\\"sisyphus\\\", \\\"hephaestus\\\", \\\"prometheus\\\"])(\\n        \\\"#then should allow question for %s agent\\\",\\n        (agentName) => {\\n          const params = createParams({\\n            agents: [agentName],\\n            disabledTools: [\\\"todowrite\\\", \\\"interactive_bash\\\"],\\n          })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.permission.question).toBe(\\\"allow\\\")\\n        },\\n      )\\n    })\\n  })\\n\\n  describe(\\\"#given council tools are globally hidden\\\", () => {\\n    describe(\\\"#when applying tool config for Athena agents\\\", () => {\\n      it.each([\\\"athena\\\", \\\"athena-junior\\\"])(\\n        \\\"#then should expose council tools via agent tools for %s\\\",\\n        (agentName) => {\\n          const params = createParams({ agents: [agentName] })\\n\\n          applyToolConfig(params)\\n\\n          const agent = params.agentResult[agentName] as {\\n            tools?: Record<string, unknown>\\n            permission: Record<string, unknown>\\n          }\\n          expect(agent.tools?.prepare_council_prompt).toBe(true)\\n          expect(agent.tools?.council_finalize).toBe(true)\\n          expect(agent.tools?.athena_council).toBe(true)\\n          expect(agent.permission.prepare_council_prompt).toBe(\\\"allow\\\")\\n          expect(agent.permission.council_finalize).toBe(\\\"allow\\\")\\n          expect(agent.permission.athena_council).toBe(\\\"allow\\\")\\n        },\\n      )\\n    })\\n\\n    describe(\\\"#when applying tool config for non-Athena agents\\\", () => {\\n      it.each([\\\"atlas\\\", \\\"sisyphus\\\", \\\"prometheus\\\", \\\"hephaestus\\\", \\\"sisyphus-junior\\\"])(\\n        \\\"#then should deny council tools permission for %s\\\",\\n        (agentName) => {\\n          const params = createParams({ agents: [agentName] })\\n\\n          applyToolConfig(params)\\n\\n          const permission = params.config.permission as Record<string, unknown>\\n          const agent = params.agentResult[agentName] as {\\n            tools?: Record<string, unknown>\\n            permission: Record<string, unknown>\\n          }\\n          expect(permission.prepare_council_prompt).toBe(\\\"deny\\\")\\n          expect(permission.council_finalize).toBe(\\\"deny\\\")\\n          expect(permission.athena_council).toBe(\\\"deny\\\")\\n          expect(agent.tools?.prepare_council_prompt).toBeUndefined()\\n          expect(agent.tools?.council_finalize).toBeUndefined()\\n          expect(agent.tools?.athena_council).toBeUndefined()\\n        },\\n      )\\n    })\\n  })\\n})\\n\",\"deletions\":5},\"file\":\"/Users/ufukaltinok/Work/OSS/oh-my-opencode/src/plugin-handlers/tool-config-handler.test.ts\",\"format_skipped_reason\":\"not_found\",\"formatted\":false,\"lsp_diagnostics\":[{\"code\":\"2307\",\"column\":61,\"end_column\":71,\"end_line\":1,\"file\":\"/Users/ufukaltinok/Work/OSS/oh-my-opencode/src/plugin-handlers/tool-config-handler.test.ts\",\"line\":1,\"message\":\"Cannot find module 'bun:test' or its corresponding type declarations.\",\"severity\":\"error\",\"source\":\"typescript\"},{\"code\":\"7044\",\"column\":56,\"end_column\":65,\"end_line\":74,\"file\":\"/Users/ufukaltinok/Work/OSS/oh-my-opencode/src/plugin-handlers/tool-config-handler.test.ts\",\"line\":74,\"message\":\"Parameter 'agentName' implicitly has an 'any' type, but a better type may be inferred from usage.\",\"severity\":\"hint\",\"source\":\"typescript\"},{\"code\":\"7044\",\"column\":10,\"end_column\":19,\"end_line\":116,\"file\":\"/Users/ufukaltinok/Work/OSS/oh-my-opencode/src/plugin-handlers/tool-config-handler.test.ts\",\"line\":116,\"message\":\"Parameter 'agentName' implicitly has an 'any' type, but a better type may be inferred from usage.\",\"severity\":\"hint\",\"source\":\"typescript\"},{\"code\":\"7044\",\"column\":10,\"end_column\":19,\"end_line\":136,\"file\":\"/Users/ufukaltinok/Work/OSS/oh-my-opencode/src/plugin-handlers/tool-config-handler.test.ts\",\"line\":136,\"message\":\"Parameter 'agentName' implicitly has an 'any' type, but a better type may be inferred from usage.\",\"severity\":\"hint\",\"source\":\"typescript\"},{\"code\":\"7044\",\"column\":10,\"end_column\":19,\"end_line\":156,\"file\":\"/Users/ufukaltinok/Work/OSS/oh-my-opencode/src/plugin-handlers/tool-config-handler.test.ts\",\"line\":156,\"message\":\"Parameter 'agentName' implicitly has an 'any' type, but a better type may be inferred from usage.\",\"severity\":\"hint\",\"source\":\"typescript\"},{\"code\":\"7044\",\"column\":10,\"end_column\":19,\"end_line\":176,\"file\":\"/Users/ufukaltinok/Work/OSS/oh-my-opencode/src/plugin-handlers/tool-config-handler.test.ts\",\"line\":176,\"message\":\"Parameter 'agentName' implicitly has an 'any' type, but a better type may be inferred from usage.\",\"severity\":\"hint\",\"source\":\"typescript\"},{\"code\":\"7044\",\"column\":60,\"end_column\":69,\"end_line\":202,\"file\":\"/Users/ufukaltinok/Work/OSS/oh-my-opencode/src/plugin-handlers/tool-config-handler.test.ts\",\"line\":202,\"message\":\"Parameter 'agentName' implicitly has an 'any' type, but a better type may be inferred from usage.\",\"severity\":\"hint\",\"source\":\"typescript\"},{\"code\":\"7044\",\"column\":10,\"end_column\":19,\"end_line\":246,\"file\":\"/Users/ufukaltinok/Work/OSS/oh-my-opencode/src/plugin-handlers/tool-config-handler.test.ts\",\"line\":246,\"message\":\"Parameter 'agentName' implicitly has an 'any' type, but a better type may be inferred from usage.\",\"severity\":\"hint\",\"source\":\"typescript\"},{\"code\":\"7044\",\"column\":10,\"end_column\":19,\"end_line\":265,\"file\":\"/Users/ufukaltinok/Work/OSS/oh-my-opencode/src/plugin-handlers/tool-config-handler.test.ts\",\"line\":265,\"message\":\"Parameter 'agentName' implicitly has an 'any' type, but a better type may be inferred from usage.\",\"severity\":\"hint\",\"source\":\"typescript\"},{\"code\":\"7044\",\"column\":10,\"end_column\":19,\"end_line\":284,\"file\":\"/Users/ufukaltinok/Work/OSS/oh-my-opencode/src/plugin-handlers/tool-config-handler.test.ts\",\"line\":284,\"message\":\"Parameter 'agentName' implicitly has an 'any' type, but a better type may be inferred from usage.\",\"severity\":\"hint\",\"source\":\"typescript\"},{\"code\":\"7044\",\"column\":10,\"end_column\":19,\"end_line\":305,\"file\":\"/Users/ufukaltinok/Work/OSS/oh-my-opencode/src/plugin-handlers/tool-config-handler.test.ts\",\"line\":305,\"message\":\"Parameter 'agentName' implicitly has an 'any' type, but a better type may be inferred from usage.\",\"severity\":\"hint\",\"source\":\"typescript\"},{\"code\":\"7044\",\"column\":10,\"end_column\":19,\"end_line\":323,\"file\":\"/Users/ufukaltinok/Work/OSS/oh-my-opencode/src/plugin-handlers/tool-config-handler.test.ts\",\"line\":323,\"message\":\"Parameter 'agentName' implicitly has an 'any' type, but a better type may be inferred from usage.\",\"severity\":\"hint\",\"source\":\"typescript\"}],\"replacements\":1,\"syntax_valid\":true}\n\nLSP errors detected, please fix:\n  Line 1: Cannot find module 'bun:test' or its corresponding type declarations."
    }
  }
]

[Message 100] role: assistant
[
  {
    "type": "tool-call",
    "toolCallId": "toolu_014cknzHTkmTKd5SdrVqW99r",
    "toolName": "bash",
    "input": {
      "command": "bun test src/plugin-handlers/tool-config-handler.test.ts",
      "description": "Run tests after cleanup"
    }
  }
]

[Message 101] role: tool
[
  {
    "type": "tool-result",
    "toolCallId": "toolu_014cknzHTkmTKd5SdrVqW99r",
    "toolName": "bash",
    "output": {
      "type": "text",
      "value": "§376§ bun test v1.3.11 (af24e281)\n\n 41 pass\n 0 fail\n 85 expect() calls\nRan 41 tests across 1 file. [72.00ms]\n"
    }
  }
]

[Message 102] role: assistant
[
  {
    "type": "tool-call",
    "toolCallId": "toolu_01GnEg96vYDwaYpCuhNWZ5MX",
    "toolName": "ctx_reduce",
    "input": {
      "drop": "370,372,373,375"
    }
  }
]

[Message 103] role: tool
[
  {
    "type": "tool-result",
    "toolCallId": "toolu_01GnEg96vYDwaYpCuhNWZ5MX",
    "toolName": "ctx_reduce",
    "output": {
      "type": "text",
      "value": "§377§ Queued: deferred drop §370§, §372§, §373§, §375§."
    }
  }
]

[Message 104] role: assistant
[
  {
    "type": "text",
    "text": "§378§ All 41 tests pass. Here's the summary of changes and Oracle findings:\n\n**Oracle confirmed the permission approach is correct.** The full code path:\n- `config.permission.X = \"deny\"` → feeds into every agent's ruleset via `Agent.state()` → `PermissionNext.fromConfig(cfg.permission)`  \n- Per-agent `permission.X = \"allow\"` → overrides via `findLast()` for athena/athena-junior\n- `LLM.resolveTools()` → calls `PermissionNext.disabled()` → removes denied tools before sending to model\n- This runs for ALL sessions including primary/TUI\n\n**Dead fields removed:**\n- `agent.tools = { council_finalize: true, ... }` — only converted during Zod parsing which has already completed before our plugin hook runs. Harmless but misleading — removed.\n- `config.tools = { X: false }` — was already removed in our earlier fix. Also dead for same reason.\n\n**What's left (the correct approach):**\n- `config.permission = { prepare_council_prompt: \"deny\", council_finalize: \"deny\", athena_council: \"deny\" }` — global deny\n- `athena.permission = { prepare_council_prompt: \"allow\", ... }` — per-agent allow\n- `athenaJunior.permission = { prepare_council_prompt: \"allow\", ... }` — per-agent allow\n\nIf you restart and still see the tools as sisyphus, then the tools are visible in a registry/UI listing (which is global and unfiltered) but **not** sent to the model (which IS filtered by permission). That distinction is important.\n\nWant me to commit and push?"
  }
]

[Message 105] role: user
[
  {
    "type": "text",
    "text": "§379§ Yes"
  }
]

[Message 106] role: assistant
[
  {
    "type": "tool-call",
    "toolCallId": "toolu_01UumDwTNVXd5ktR9VgBPDWh",
    "toolName": "bash",
    "input": {
      "command": "export CI=true DEBIAN_FRONTEND=noninteractive GIT_TERMINAL_PROMPT=0 GCM_INTERACTIVE=never HOMEBREW_NO_AUTO_UPDATE=1 GIT_EDITOR=: EDITOR=: VISUAL='' GIT_SEQUENCE_EDITOR=: GIT_MERGE_AUTOEDIT=no GIT_PAGER=cat PAGER=cat npm_config_yes=true PIP_NO_INPUT=1 YARN_ENABLE_IMMUTABLE_INSTALLS=false; git add src/plugin-handlers/tool-config-handler.ts src/plugin-handlers/tool-config-handler.test.ts && git commit -m \"fix(tool-config): use permission-only approach for council tool visibility\n\nRemove dead agent.tools fields that only work during Zod parsing\n(before plugin config hook runs). Council tool restriction now uses\nconfig.permission deny globally + per-agent permission allow for\nathena/athena-junior, which is the correct mechanism per OpenCode's\nLLM.resolveTools() → PermissionNext.disabled() filtering path.\" && git push origin feat/athena",
      "description": "Commit and push council tool fix"
    }
  }
]

[Message 107] role: tool
[
  {
    "type": "tool-result",
    "toolCallId": "toolu_01UumDwTNVXd5ktR9VgBPDWh",
    "toolName": "bash",
    "output": {
      "type": "text",
      "value": "§380§ [feat/athena 6ae07b4d] fix(tool-config): use permission-only approach for council tool visibility\n 2 files changed, 7 insertions(+), 25 deletions(-)\nTo github.com:ualtinok/oh-my-opencode.git\n   12691ee9..6ae07b4d  feat/athena -> feat/athena\n"
    }
  }
]

[Message 108] role: assistant
[
  {
    "type": "text",
    "text": "§381§ Pushed `6ae07b4d`."
  }
]


=== PROVIDER OPTIONS ===

temperature: 0.1
maxOutputTokens: 32000
providerOptions: {
  "anthropic": {
    "thinking": {
      "type": "adaptive"
    },
    "effort": "high"
  }
}


=== REQUEST ENVELOPE ===

{
  "tools": {
    "invalid": {
      "description": "Do not use",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "tool": {
              "type": "string"
            },
            "error": {
              "type": "string"
            }
          },
          "required": [
            "tool",
            "error"
          ],
          "additionalProperties": false
        }
      }
    },
    "question": {
      "description": "Use this tool when you need to ask the user questions during execution. This allows you to:\n1. Gather user preferences or requirements\n2. Clarify ambiguous instructions\n3. Get decisions on implementation choices as you work\n4. Offer choices to the user about what direction to take.\n\nUsage notes:\n- When `custom` is enabled (default), a \"Type your own answer\" option is added automatically; don't include \"Other\" or catch-all options\n- Answers are returned as arrays of labels; set `multiple: true` to allow selecting more than one\n- If you recommend a specific option, make that the first option in the list and add \"(Recommended)\" at the end of the label\n",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "questions": {
              "description": "Questions to ask",
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "question": {
                    "description": "Complete question",
                    "type": "string"
                  },
                  "header": {
                    "description": "Very short label (max 30 chars)",
                    "type": "string"
                  },
                  "options": {
                    "description": "Available choices",
                    "type": "array",
                    "items": {
                      "ref": "QuestionOption",
                      "type": "object",
                      "properties": {
                        "label": {
                          "description": "Display text (1-5 words, concise)",
                          "type": "string"
                        },
                        "description": {
                          "description": "Explanation of choice",
                          "type": "string"
                        }
                      },
                      "required": [
                        "label",
                        "description"
                      ],
                      "additionalProperties": false
                    }
                  },
                  "multiple": {
                    "description": "Allow selecting multiple choices",
                    "type": "boolean"
                  }
                },
                "required": [
                  "question",
                  "header",
                  "options"
                ],
                "additionalProperties": false
              }
            }
          },
          "required": [
            "questions"
          ],
          "additionalProperties": false
        }
      }
    },
    "bash": {
      "description": "Executes a given bash command in a persistent shell session with optional timeout, ensuring proper handling and security measures.\n\nAll commands run in /Users/ufukaltinok/Work/OSS/oh-my-opencode by default. Use the `workdir` parameter if you need to run a command in a different directory. AVOID using `cd <directory> && <command>` patterns - use `workdir` instead.\n\nIMPORTANT: This tool is for terminal operations like git, npm, docker, etc. DO NOT use it for file operations (reading, writing, editing, searching, finding files) - use the specialized tools for this instead.\n\nBefore executing the command, please follow these steps:\n\n1. Directory Verification:\n   - If the command will create new directories or files, first use `ls` to verify the parent directory exists and is the correct location\n   - For example, before running \"mkdir foo/bar\", first use `ls foo` to check that \"foo\" exists and is the intended parent directory\n\n2. Command Execution:\n   - Always quote file paths that contain spaces with double quotes (e.g., rm \"path with spaces/file.txt\")\n   - Examples of proper quoting:\n     - mkdir \"/Users/name/My Documents\" (correct)\n     - mkdir /Users/name/My Documents (incorrect - will fail)\n     - python \"/path/with spaces/script.py\" (correct)\n     - python /path/with spaces/script.py (incorrect - will fail)\n   - After ensuring proper quoting, execute the command.\n   - Capture the output of the command.\n\nUsage notes:\n  - The command argument is required.\n  - You can specify an optional timeout in milliseconds. If not specified, commands will time out after 120000ms (2 minutes).\n  - It is very helpful if you write a clear, concise description of what this command does in 5-10 words.\n  - If the output exceeds 2000 lines or 51200 bytes, it will be truncated and the full output will be written to a file. You can use Read with offset/limit to read specific sections or Grep to search the full content. Do NOT use `head`, `tail`, or other truncation commands to limit output; the full output will already be captured to a file for more precise searching.\n\n  - Avoid using Bash with the `find`, `grep`, `cat`, `head`, `tail`, `sed`, `awk`, or `echo` commands, unless explicitly instructed or when these commands are truly necessary for the task. Instead, always prefer using the dedicated tools for these commands:\n    - File search: Use Glob (NOT find or ls)\n    - Content search: Use Grep (NOT grep or rg)\n    - Read files: Use Read (NOT cat/head/tail)\n    - Edit files: Use Edit (NOT sed/awk)\n    - Write files: Use Write (NOT echo >/cat <<EOF)\n    - Communication: Output text directly (NOT echo/printf)\n  - When issuing multiple commands:\n    - If the commands are independent and can run in parallel, make multiple Bash tool calls in a single message. For example, if you need to run \"git status\" and \"git diff\", send a single message with two Bash tool calls in parallel.\n    - If the commands depend on each other and must run sequentially, use a single Bash call with '&&' to chain them together (e.g., `git add . && git commit -m \"message\" && git push`). For instance, if one operation must complete before another starts (like mkdir before cp, Write before Bash for git operations, or git add before git commit), run these operations sequentially instead.\n    - Use ';' only when you need to run commands sequentially but don't care if earlier commands fail\n    - DO NOT use newlines to separate commands (newlines are ok in quoted strings)\n  - AVOID using `cd <directory> && <command>`. Use the `workdir` parameter to change directories instead.\n    <good-example>\n    Use workdir=\"/foo/bar\" with command: pytest tests\n    </good-example>\n    <bad-example>\n    cd /foo/bar && pytest tests\n    </bad-example>\n\n# Committing changes with git\n\nOnly create commits when requested by the user. If unclear, ask first. When the user asks you to create a new git commit, follow these steps carefully:\n\nGit Safety Protocol:\n- NEVER update the git config\n- NEVER run destructive/irreversible git commands (like push --force, hard reset, etc) unless the user explicitly requests them\n- NEVER skip hooks (--no-verify, --no-gpg-sign, etc) unless the user explicitly requests it\n- NEVER run force push to main/master, warn the user if they request it\n- Avoid git commit --amend. ONLY use --amend when ALL conditions are met:\n  (1) User explicitly requested amend, OR commit SUCCEEDED but pre-commit hook auto-modified files that need including\n  (2) HEAD commit was created by you in this conversation (verify: git log -1 --format='%an %ae')\n  (3) Commit has NOT been pushed to remote (verify: git status shows \"Your branch is ahead\")\n- CRITICAL: If commit FAILED or was REJECTED by hook, NEVER amend - fix the issue and create a NEW commit\n- CRITICAL: If you already pushed to remote, NEVER amend unless user explicitly requests it (requires force push)\n- NEVER commit changes unless the user explicitly asks you to. It is VERY IMPORTANT to only commit when explicitly asked, otherwise the user will feel that you are being too proactive.\n\n1. You can call multiple tools in a single response. When multiple independent pieces of information are requested and all commands are likely to succeed, run multiple tool calls in parallel for optimal performance. run the following bash commands in parallel, each using the Bash tool:\n  - Run a git status command to see all untracked files.\n  - Run a git diff command to see both staged and unstaged changes that will be committed.\n  - Run a git log command to see recent commit messages, so that you can follow this repository's commit message style.\n2. Analyze all staged changes (both previously staged and newly added) and draft a commit message:\n  - Summarize the nature of the changes (eg. new feature, enhancement to an existing feature, bug fix, refactoring, test, docs, etc.). Ensure the message accurately reflects the changes and their purpose (i.e. \"add\" means a wholly new feature, \"update\" means an enhancement to an existing feature, \"fix\" means a bug fix, etc.).\n  - Do not commit files that likely contain secrets (.env, credentials.json, etc.). Warn the user if they specifically request to commit those files\n  - Draft a concise (1-2 sentences) commit message that focuses on the \"why\" rather than the \"what\"\n  - Ensure it accurately reflects the changes and their purpose\n3. You can call multiple tools in a single response. When multiple independent pieces of information are requested and all commands are likely to succeed, run multiple tool calls in parallel for optimal performance. run the following commands:\n   - Add relevant untracked files to the staging area.\n   - Create the commit with a message\n   - Run git status after the commit completes to verify success.\n   Note: git status depends on the commit completing, so run it sequentially after the commit.\n4. If the commit fails due to pre-commit hook, fix the issue and create a NEW commit (see amend rules above)\n\nImportant notes:\n- NEVER run additional commands to read or explore code, besides git bash commands\n- NEVER use the TodoWrite or Task tools\n- DO NOT push to the remote repository unless the user explicitly asks you to do so\n- IMPORTANT: Never use git commands with the -i flag (like git rebase -i or git add -i) since they require interactive input which is not supported.\n- If there are no changes to commit (i.e., no untracked files and no modifications), do not create an empty commit\n\n# Creating pull requests\nUse the gh command via the Bash tool for ALL GitHub-related tasks including working with issues, pull requests, checks, and releases. If given a GitHub URL use the gh command to get the information needed.\n\nIMPORTANT: When the user asks you to create a pull request, follow these steps carefully:\n\n1. You can call multiple tools in a single response. When multiple independent pieces of information are requested and all commands are likely to succeed, run multiple tool calls in parallel for optimal performance. run the following bash commands in parallel using the Bash tool, in order to understand the current state of the branch since it diverged from the main branch:\n   - Run a git status command to see all untracked files\n   - Run a git diff command to see both staged and unstaged changes that will be committed\n   - Check if the current branch tracks a remote branch and is up to date with the remote, so you know if you need to push to the remote\n   - Run a git log command and `git diff [base-branch]...HEAD` to understand the full commit history for the current branch (from the time it diverged from the base branch)\n2. Analyze all changes that will be included in the pull request, making sure to look at all relevant commits (NOT just the latest commit, but ALL commits that will be included in the pull request!!!), and draft a pull request summary\n3. You can call multiple tools in a single response. When multiple independent pieces of information are requested and all commands are likely to succeed, run multiple tool calls in parallel for optimal performance. run the following commands in parallel:\n   - Create new branch if needed\n   - Push to remote with -u flag if needed\n   - Create PR using gh pr create with the format below. Use a HEREDOC to pass the body to ensure correct formatting.\n<example>\ngh pr create --title \"the pr title\" --body \"$(cat <<'EOF'\n## Summary\n<1-3 bullet points>\n</example>\n\nImportant:\n- DO NOT use the TodoWrite or Task tools\n- Return the PR URL when you're done, so the user can see it\n\n# Other common operations\n- View comments on a GitHub PR: gh api repos/foo/bar/pulls/123/comments\n",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "command": {
              "description": "The command to execute",
              "type": "string"
            },
            "timeout": {
              "description": "Optional timeout in milliseconds",
              "type": "number"
            },
            "workdir": {
              "description": "The working directory to run the command in. Defaults to /Users/ufukaltinok/Work/OSS/oh-my-opencode. Use this instead of 'cd' commands.",
              "type": "string"
            },
            "description": {
              "description": "Clear, concise description of what this command does in 5-10 words. Examples:\nInput: ls\nOutput: Lists files in current directory\n\nInput: git status\nOutput: Shows working tree status\n\nInput: npm install\nOutput: Installs package dependencies\n\nInput: mkdir foo\nOutput: Creates directory 'foo'",
              "type": "string"
            }
          },
          "required": [
            "command",
            "description"
          ],
          "additionalProperties": false
        }
      }
    },
    "read": {
      "description": "Read file contents or list directory entries.\n\nUse either startLine/endLine OR offset/limit to read a section of a file.\n\nBehavior:\n- Returns line-numbered content (e.g., \"1: const x = 1\")\n- Lines longer than 2000 characters are truncated\n- Output capped at 50KB\n- Binary files are auto-detected and return a size-only message\n- Image files (.png, .jpg, .gif, .webp, etc.) and PDFs return a metadata string (format, size, path) — no file content is returned\n- Directories return sorted entries with trailing / for subdirectories\n\nExamples:\n  Read full file: { \"filePath\": \"src/app.ts\" }\n  Read lines 50-100: { \"filePath\": \"src/app.ts\", \"startLine\": 50, \"endLine\": 100 }\n  Read 30 lines from line 200: { \"filePath\": \"src/app.ts\", \"offset\": 200, \"limit\": 30 }\n  List directory: { \"filePath\": \"src/\" }\n\nReturns: Line-numbered file content string. For directories: newline-joined sorted entries. For binary files: size/message string.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "filePath": {
              "description": "Path to file or directory (absolute or relative to project root)",
              "type": "string"
            },
            "startLine": {
              "description": "1-based line to start reading from",
              "type": "number"
            },
            "endLine": {
              "description": "1-based line to stop reading at (inclusive)",
              "type": "number"
            },
            "limit": {
              "description": "Max lines to return (default: 2000)",
              "type": "number"
            },
            "offset": {
              "description": "1-based line number to start reading from (use with limit). Ignored if startLine is provided",
              "type": "number"
            }
          },
          "required": [
            "filePath"
          ],
          "additionalProperties": false
        }
      }
    },
    "glob": {
      "description": "Fast file pattern matching tool with safety limits (60s timeout, 100 file limit). Supports glob patterns like \"**/*.js\" or \"src/**/*.ts\". Returns matching file paths sorted by modification time. Use this tool when you need to find files by name patterns.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "pattern": {
              "description": "The glob pattern to match files against",
              "type": "string"
            },
            "path": {
              "description": "The directory to search in. If not specified, the current working directory will be used. IMPORTANT: Omit this field to use the default directory. DO NOT enter \"undefined\" or \"null\" - simply omit it for the default behavior. Must be a valid directory path if provided.",
              "type": "string"
            }
          },
          "required": [
            "pattern"
          ],
          "additionalProperties": false
        }
      }
    },
    "grep": {
      "description": "Fast content search tool with safety limits (60s timeout, 256KB output). Searches file contents using regular expressions. Supports full regex syntax (eg. \"log.*Error\", \"function\\s+\\w+\", etc.). Filter files by pattern with the include parameter (eg. \"*.js\", \"*.{ts,tsx}\"). Output modes: \"content\" shows matching lines, \"files_with_matches\" shows only file paths (default), \"count\" shows match counts per file.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "pattern": {
              "description": "The regex pattern to search for in file contents",
              "type": "string"
            },
            "include": {
              "description": "File pattern to include in the search (e.g. \"*.js\", \"*.{ts,tsx}\")",
              "type": "string"
            },
            "path": {
              "description": "The directory to search in. Defaults to the current working directory.",
              "type": "string"
            },
            "output_mode": {
              "description": "Output mode: \"content\" shows matching lines, \"files_with_matches\" shows only file paths (default), \"count\" shows match counts per file.",
              "type": "string",
              "enum": [
                "content",
                "files_with_matches",
                "count"
              ]
            },
            "head_limit": {
              "description": "Limit output to first N entries. 0 or omitted means no limit.",
              "type": "number"
            }
          },
          "required": [
            "pattern"
          ],
          "additionalProperties": false
        }
      }
    },
    "edit": {
      "description": "Edit a file by finding and replacing text, or by targeting named symbols.\n\n**Modes** (determined by which parameters you provide):\n\nMode priority: operations > edits > symbol (without oldString) > oldString (find/replace) > content-only (write)\n\n1. **Multi-file transaction** — pass `operations` array\n   Edits across multiple files with checkpoint-based rollback on failure.\n   Each operation: `{ \"file\": \"path\", \"command\": \"edit_match\" | \"write\", ... }`.\n   For `edit_match`: include `match`, `replacement`. For `write`: include `content`.\n   Example: `{ \"operations\": [{ \"file\": \"a.ts\", \"command\": \"edit_match\", \"match\": \"old\", \"replacement\": \"new\" }, { \"file\": \"b.ts\", \"command\": \"write\", \"content\": \"...\" }] }`\n\n2. **Batch edits** — pass `filePath` + `edits` array\n   Multiple edits in one file atomically. Each edit is either:\n   - `{ \"oldString\": \"old\", \"newString\": \"new\" }` — find/replace\n   - `{ \"startLine\": 5, \"endLine\": 7, \"content\": \"new lines\" }` — replace line range (1-based, both inclusive)\n   Set content to empty string to delete lines.\n\n3. **Symbol replace** — pass `filePath` + `symbol` + `content`\n   Replaces an entire named symbol (function, class, type) with new content.\n   Includes decorators, attributes, and doc comments in the replacement range.\n   **Important:** You must NOT provide `oldString` when using symbol mode — if present, the tool silently falls back to find/replace mode.\n   Example: `{ \"filePath\": \"src/app.ts\", \"symbol\": \"handleRequest\", \"content\": \"function handleRequest() { ... }\" }`\n\n4. **Find and replace** — pass `filePath` + `oldString` + `newString`\n   Finds the exact text in `oldString` and replaces it with `newString`.\n   Supports fuzzy matching (handles whitespace differences automatically).\n   If multiple matches exist, specify which one with `occurrence` or use `replaceAll: true`.\n   Example: `{ \"filePath\": \"src/app.ts\", \"oldString\": \"const x = 1\", \"newString\": \"const x = 2\" }`\n\n5. **Replace all occurrences** — add `replaceAll: true`\n   Replaces every occurrence of `oldString` in the file.\n   Example: `{ \"filePath\": \"src/app.ts\", \"oldString\": \"oldName\", \"newString\": \"newName\", \"replaceAll\": true }`\n\n6. **Select specific occurrence** — add `occurrence: N` (0-indexed)\n   When multiple matches exist, select the Nth one (0 = first, 1 = second, etc.).\n   Example: `{ \"filePath\": \"src/app.ts\", \"oldString\": \"TODO\", \"newString\": \"DONE\", \"occurrence\": 0 }`\n\nNote: Modes 5 and 6 are options on mode 4 (find/replace) — they require `oldString`.\n\n**Behavior:**\n- Backs up files before editing (recoverable via aft_safety undo)\n- Auto-formats using project formatter if configured\n- Tree-sitter syntax validation on all edits\n- Symbol replace includes decorators, attributes, and doc comments in range\n- LSP error-level diagnostics are returned automatically after non-dry-run edits\n\nReturns: JSON string for the selected edit mode. Dry runs return diff data; non-dry-run edits may append inline LSP error lines.\n\nCommon response fields: success (boolean), diff (object with before/after), backup_id (string), syntax_valid (boolean). Exact fields vary by mode.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "filePath": {
              "description": "Path to the file to edit (absolute or relative to project root). Required for all modes except 'operations' multi-file transactions",
              "type": "string"
            },
            "oldString": {
              "description": "Text to find (exact match, with fuzzy fallback)",
              "type": "string"
            },
            "newString": {
              "description": "Text to replace with (omit or set to empty string to delete the matched text)",
              "type": "string"
            },
            "replaceAll": {
              "description": "Replace all occurrences",
              "type": "boolean"
            },
            "occurrence": {
              "description": "0-indexed occurrence to replace when multiple matches exist",
              "type": "number"
            },
            "symbol": {
              "description": "Named symbol to replace (function, class, type)",
              "type": "string"
            },
            "content": {
              "description": "New content for symbol replace or file write",
              "type": "string"
            },
            "edits": {
              "description": "Batch edits — array of { oldString: string, newString: string } or { startLine: number (1-based), endLine: number (1-based, inclusive), content: string }",
              "type": "array",
              "items": {
                "type": "object",
                "propertyNames": {
                  "type": "string"
                },
                "additionalProperties": {}
              }
            },
            "operations": {
              "description": "Transaction — array of { file: string, command: 'edit_match' | 'write', match?: string, replacement?: string, content?: string } for multi-file edits with rollback. Note: uses 'file'/'match'/'replacement' (not filePath/oldString/newString)",
              "type": "array",
              "items": {
                "type": "object",
                "propertyNames": {
                  "type": "string"
                },
                "additionalProperties": {}
              }
            },
            "dryRun": {
              "description": "Preview changes without applying (returns diff, default: false)",
              "type": "boolean"
            }
          },
          "additionalProperties": false
        }
      }
    },
    "write": {
      "description": "Write content to a file, creating it (and parent directories) if needed.\n\nAutomatically creates parent directories. Backs up existing files before overwriting.\nIf the project has a formatter configured (biome, prettier, rustfmt, etc.), the file\nis auto-formatted after writing. Returns inline LSP diagnostics when available.\n\n**Behavior:**\n- Creates parent directories automatically (no need to mkdir first)\n- Existing files are backed up before overwriting (recoverable via aft_safety undo)\n- Auto-formats using project formatter if configured (biome.json, .prettierrc, etc.)\n- Returns LSP error-level diagnostics inline if type errors are introduced\n- Use this for creating new files or completely replacing file contents\n- For partial edits (find/replace), use the `edit` tool instead\n\nReturns: Status message string (for example: \"Created new file. Auto-formatted.\") with optional inline LSP error lines.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "filePath": {
              "description": "Path to the file to write (absolute or relative to project root)",
              "type": "string"
            },
            "content": {
              "description": "The full content to write to the file",
              "type": "string"
            }
          },
          "required": [
            "filePath",
            "content"
          ],
          "additionalProperties": false
        }
      }
    },
    "task": {
      "description": "Spawn agent task with category-based or direct agent selection.\n  \n  ⚠️  CRITICAL: You MUST provide EITHER category OR subagent_type. Omitting BOTH will FAIL.\n  \n  **COMMON MISTAKE (DO NOT DO THIS):**\n  ```\n  task(description=\"...\", prompt=\"...\", run_in_background=false)  // ❌ FAILS - missing category AND subagent_type\n  ```\n  \n  **CORRECT - Using category:**\n  ```\n  task(category=\"quick\", load_skills=[], description=\"Fix type error\", prompt=\"...\", run_in_background=false)\n  ```\n  \n  **CORRECT - Using subagent_type:**\n  ```\n  task(subagent_type=\"explore\", load_skills=[], description=\"Find patterns\", prompt=\"...\", run_in_background=true)\n  ```\n  \n  REQUIRED: Provide ONE of:\n  - category: For task delegation (uses Sisyphus-Junior with category-optimized model)\n  - subagent_type: For direct agent invocation (explore, librarian, oracle, etc.)\n  \n  **DO NOT provide both.** If category is provided, subagent_type is ignored.\n  \n  - load_skills: ALWAYS REQUIRED. Pass [] if no skills needed, or [\"skill-1\", \"skill-2\"] for category tasks.\n  - category: Use predefined category → Spawns Sisyphus-Junior with category config\n    Available categories:\n    - visual-engineering: Frontend, UI/UX, design, styling, animation\n  - ultrabrain: Use ONLY for genuinely hard, logic-heavy tasks. Give clear goals only, not step-by-step instructions.\n  - deep: Goal-oriented autonomous problem-solving. Thorough research before action. For hairy problems requiring deep understanding.\n  - artistry: Complex problem-solving with unconventional, creative approaches - beyond standard patterns\n  - quick: Trivial tasks - single file changes, typo fixes, simple modifications\n  - unspecified-low: Tasks that don't fit other categories, low effort required\n  - unspecified-high: Tasks that don't fit other categories, high effort required\n  - writing: Documentation, prose, technical writing\n  - subagent_type: Use specific agent directly (explore, librarian, oracle, metis, momus)\n  - run_in_background: REQUIRED. true=async (returns task_id), false=sync (waits). Use background=true ONLY for parallel exploration with 5+ independent queries.\n  - session_id: Existing Task session to continue (from previous task output). Continues agent with FULL CONTEXT PRESERVED - saves tokens, maintains continuity.\n  - command: The command that triggered this task (optional, for slash command tracking).\n  \n  **WHEN TO USE session_id:**\n  - Task failed/incomplete → session_id with \"fix: [specific issue]\"\n  - Need follow-up on previous result → session_id with additional question\n  - Multi-turn conversation with same agent → always session_id instead of new task\n  \n  Prompts MUST be in English.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "load_skills": {
              "description": "Skill names to inject. REQUIRED - pass [] if no skills needed.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "description": {
              "description": "Short task description (3-5 words)",
              "type": "string"
            },
            "prompt": {
              "description": "Full detailed prompt for the agent",
              "type": "string"
            },
            "run_in_background": {
              "description": "REQUIRED. true=async (returns task_id), false=sync (waits). Use false for task delegation, true ONLY for parallel exploration.",
              "type": "boolean"
            },
            "category": {
              "description": "REQUIRED if subagent_type not provided. Do NOT provide both category and subagent_type.",
              "type": "string"
            },
            "subagent_type": {
              "description": "REQUIRED if category not provided. Do NOT provide both category and subagent_type.",
              "type": "string"
            },
            "session_id": {
              "description": "Existing Task session to continue",
              "type": "string"
            },
            "command": {
              "description": "The command that triggered this task",
              "type": "string"
            },
            "write_output_to_file": {
              "description": "Write raw task output to file on completion",
              "type": "boolean"
            }
          },
          "required": [
            "load_skills",
            "description",
            "prompt",
            "run_in_background"
          ],
          "additionalProperties": false
        }
      }
    },
    "webfetch": {
      "description": "- Fetches content from a specified URL\n- Takes a URL and optional format as input\n- Fetches the URL content, converts to requested format (markdown by default)\n- Returns the content in the specified format\n- Use this tool when you need to retrieve and analyze web content\n\nUsage notes:\n  - IMPORTANT: if another tool is present that offers better web fetching capabilities, is more targeted to the task, or has fewer restrictions, prefer using that tool instead of this one.\n  - The URL must be a fully-formed valid URL\n  - HTTP URLs will be automatically upgraded to HTTPS\n  - Format options: \"markdown\" (default), \"text\", or \"html\"\n  - This tool is read-only and does not modify any files\n  - Results may be summarized if the content is very large\n",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "url": {
              "description": "The URL to fetch content from",
              "type": "string"
            },
            "format": {
              "description": "The format to return the content in (text, markdown, or html). Defaults to markdown.",
              "default": "markdown",
              "type": "string",
              "enum": [
                "text",
                "markdown",
                "html"
              ]
            },
            "timeout": {
              "description": "Optional timeout in seconds (max 120)",
              "type": "number"
            }
          },
          "required": [
            "url",
            "format"
          ],
          "additionalProperties": false
        }
      }
    },
    "todowrite": {
      "description": "Use this tool to create and manage a structured task list for tracking progress on multi-step work.\n\n## Todo Format (MANDATORY)\n\nEach todo title MUST encode four elements: WHERE, WHY, HOW, and EXPECTED RESULT.\n\nFormat: \"[WHERE] [HOW] to [WHY] — expect [RESULT]\"\n\nGOOD:\n- \"src/utils/validation.ts: Add validateEmail() for input sanitization — returns boolean\"\n- \"UserService.create(): Call validateEmail() before DB insert — rejects invalid emails with 400\"\n- \"validation.test.ts: Add test for missing @ sign — expect validateEmail('foo') to return false\"\n\nBAD:\n- \"Implement email validation\" (where? how? what result?)\n- \"Add dark mode\" (this is a feature, not a todo)\n- \"Fix auth\" (what file? what changes? what's expected?)\n\n## Granularity Rules\n\nEach todo MUST be a single atomic action completable in 1-3 tool calls. If it needs more, split it.\n\n**Size test**: Can you complete this todo by editing one file or running one command? If not, it's too big.\n\n## Task Management\n- One in_progress at a time. Complete it before starting the next.\n- Mark completed immediately after finishing each item.\n- Skip this tool for single trivial tasks (one-step, obvious action).",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "todos": {
              "description": "The updated todo list",
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "content": {
                    "description": "Brief description of the task",
                    "type": "string"
                  },
                  "status": {
                    "description": "Current status of the task: pending, in_progress, completed, cancelled",
                    "type": "string"
                  },
                  "priority": {
                    "description": "Priority level of the task: high, medium, low",
                    "type": "string"
                  }
                },
                "required": [
                  "content",
                  "status",
                  "priority"
                ],
                "additionalProperties": false
              }
            }
          },
          "required": [
            "todos"
          ],
          "additionalProperties": false
        }
      }
    },
    "skill": {
      "description": "Load a skill or execute a slash command to get detailed instructions for a specific task.\n\nSkills and commands provide specialized knowledge and step-by-step guidance.\nUse this when a task matches an available skill's or command's description.\n\n**How to use:**\n- Call with a skill name: name='code-review'\n- Call with a command name (without leading slash): name='publish'\n- The tool will return detailed instructions with your context applied.\n\n<available_items>\nPriority: project > user > opencode > builtin/plugin | Skills listed before commands\nInvoke via: skill(name=\"item-name\") — omit leading slash for commands.\n  <command>\n    <name>/mcp-vector-search-pr-mr-skill</name>\n    <description>(user - Skill) </description>\n    <scope>user</scope>\n  </command>\n  <command>\n    <name>/pre-publish-review</name>\n    <description>(opencode-project - Skill) Nuclear-grade 16-agent pre-publish release gate. Runs /get-unpublished-changes to detect all changes since last npm release, spawns up to 10 ultrabrain agents for deep per-change analysis, invokes /review-work (5 agents) for holistic review, and 1 oracle for overall release synthesis. Use before EVERY npm publish. Triggers: 'pre-publish review', 'review before publish', 'release review', 'pre-release review', 'ready to publish?', 'can I publish?', 'pre-publish', 'safe to publish', 'publishing review', 'pre-publish check'.</description>\n    <scope>opencode-project</scope>\n  </command>\n  <command>\n    <name>/github-triage</name>\n    <description>(opencode-project - Skill) Read-only GitHub triage for issues AND PRs. 1 item = 1 background task (category: quick). Analyzes all open items and writes evidence-backed reports to /tmp/{datetime}/. Every claim requires a GitHub permalink as proof. NEVER takes any action on GitHub - no comments, no merges, no closes, no labels. Reports only. Triggers: 'triage', 'triage issues', 'triage PRs', 'github triage'.</description>\n    <scope>opencode-project</scope>\n  </command>\n  <command>\n    <name>/work-with-pr</name>\n    <description>(opencode-project - Skill) Full PR lifecycle: git worktree → implement → atomic commits → PR creation → verification loop (CI + review-work + Cubic approval) → merge. Keeps iterating until ALL gates pass and PR is merged. Worktree auto-cleanup after merge. Use whenever implementation work needs to land as a PR. Triggers: 'create a PR', 'implement and PR', 'work on this and make a PR', 'implement issue', 'land this as a PR', 'work-with-pr', 'PR workflow', 'implement end to end', even when user just says 'implement X' if the context implies PR delivery.</description>\n    <scope>opencode-project</scope>\n  </command>\n  <command>\n    <name>/work-with-pr-workspace/iteration-1/benchmark</name>\n    <description>(opencode-project - Skill) </description>\n    <scope>opencode-project</scope>\n  </command>\n  <command>\n    <name>/playwright</name>\n    <description>(opencode - Skill) MUST USE for any browser-related tasks. Browser automation via Playwright MCP - verification, browsing, information gathering, web scraping, testing, screenshots, and all browser interactions.</description>\n    <scope>builtin</scope>\n  </command>\n  <command>\n    <name>/frontend-ui-ux</name>\n    <description>(opencode - Skill) Designer-turned-developer who crafts stunning UI/UX even without design mockups</description>\n    <scope>builtin</scope>\n  </command>\n  <command>\n    <name>/git-master</name>\n    <description>(opencode - Skill) MUST USE for ANY git operations. Atomic commits, rebase/squash, history search (blame, bisect, log -S). STRONGLY RECOMMENDED: Use with task(category='quick', load_skills=['git-master'], ...) to save context. Triggers: 'commit', 'rebase', 'squash', 'who wrote', 'when was X added', 'find the commit that'.</description>\n    <scope>builtin</scope>\n  </command>\n  <command>\n    <name>/dev-browser</name>\n    <description>(opencode - Skill) Browser automation with persistent page state. Use when users ask to navigate websites, fill forms, take screenshots, extract web data, test web apps, or automate browser workflows. Trigger phrases include 'go to [url]', 'click on', 'fill out the form', 'take a screenshot', 'scrape', 'automate', 'test the website', 'log into', or any browser interaction request.</description>\n    <scope>builtin</scope>\n  </command>\n  <command>\n    <name>/web-interface-guidelines</name>\n    <description>Review UI code for Vercel Web Interface Guidelines compliance</description>\n    <scope>user</scope>\n    <argument><file-or-pattern></argument>\n  </command>\n  <command>\n    <name>/omomomo</name>\n    <description>Easter egg command - about oh-my-opencode</description>\n    <scope>opencode-project</scope>\n  </command>\n  <command>\n    <name>/publish</name>\n    <description>Publish oh-my-opencode to npm via GitHub Actions workflow</description>\n    <scope>opencode-project</scope>\n    <argument><patch|minor|major></argument>\n  </command>\n  <command>\n    <name>/remove-deadcode</name>\n    <description>Remove unused code from this project with ultrawork mode, LSP-verified safety, atomic commits</description>\n    <scope>opencode-project</scope>\n  </command>\n  <command>\n    <name>/get-unpublished-changes</name>\n    <description>Compare HEAD with the latest published npm version and list all unpublished changes</description>\n    <scope>opencode-project</scope>\n  </command>\n  <command>\n    <name>/init-deep</name>\n    <description>(builtin) Initialize hierarchical AGENTS.md knowledge base</description>\n    <scope>builtin</scope>\n  </command>\n  <command>\n    <name>/ralph-loop</name>\n    <description>(builtin) Start self-referential development loop until completion</description>\n    <scope>builtin</scope>\n  </command>\n  <command>\n    <name>/ulw-loop</name>\n    <description>(builtin) Start ultrawork loop - continues until completion with ultrawork mode</description>\n    <scope>builtin</scope>\n  </command>\n  <command>\n    <name>/cancel-ralph</name>\n    <description>(builtin) Cancel active Ralph Loop</description>\n    <scope>builtin</scope>\n  </command>\n  <command>\n    <name>/refactor</name>\n    <description>(builtin) Intelligent refactoring command with LSP, AST-grep, architecture analysis, codemap, and TDD verification.</description>\n    <scope>builtin</scope>\n  </command>\n  <command>\n    <name>/start-work</name>\n    <description>(builtin) Start Sisyphus work session from Prometheus plan</description>\n    <scope>builtin</scope>\n  </command>\n  <command>\n    <name>/stop-continuation</name>\n    <description>(builtin) Stop all continuation mechanisms (ralph loop, todo continuation, boulder) for this session</description>\n    <scope>builtin</scope>\n  </command>\n  <command>\n    <name>/handoff</name>\n    <description>(builtin) Create a detailed context summary for continuing work in a new session</description>\n    <scope>builtin</scope>\n  </command>\n</available_items>",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "name": {
              "description": "The skill or command name (e.g., 'code-review' or 'publish'). Use without leading slash for commands.",
              "type": "string"
            },
            "user_message": {
              "description": "Optional arguments or context for command invocation. Example: name='publish', user_message='patch'",
              "type": "string"
            }
          },
          "required": [
            "name"
          ],
          "additionalProperties": false
        }
      }
    },
    "gemini_quota": {
      "description": "Retrieve current Gemini Code Assist quota usage for the authenticated user and project.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {},
          "additionalProperties": false
        }
      }
    },
    "session_list": {
      "description": "List all OpenCode sessions with optional filtering.\n\nReturns a list of available session IDs with metadata including message count, date range, and agents used.\n\nArguments:\n- limit (optional): Maximum number of sessions to return\n- from_date (optional): Filter sessions from this date (ISO 8601 format)\n- to_date (optional): Filter sessions until this date (ISO 8601 format)\n\nExample output:\n| Session ID | Messages | First | Last | Agents |\n|------------|----------|-------|------|--------|\n| ses_abc123 | 45 | 2025-12-20 | 2025-12-24 | build, oracle |\n| ses_def456 | 12 | 2025-12-19 | 2025-12-19 | build |",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "limit": {
              "description": "Maximum number of sessions to return",
              "type": "number"
            },
            "from_date": {
              "description": "Filter sessions from this date (ISO 8601 format)",
              "type": "string"
            },
            "to_date": {
              "description": "Filter sessions until this date (ISO 8601 format)",
              "type": "string"
            },
            "project_path": {
              "description": "Filter sessions by project path (default: current working directory)",
              "type": "string"
            }
          },
          "additionalProperties": false
        }
      }
    },
    "session_read": {
      "description": "Read messages and history from an OpenCode session.\n\nReturns a formatted view of session messages with role, timestamp, and content. Optionally includes todos and transcript data.\n\nArguments:\n- session_id (required): Session ID to read\n- include_todos (optional): Include todo list if available (default: false)\n- include_transcript (optional): Include transcript log if available (default: false)\n- limit (optional): Maximum number of messages to return (default: all)\n\nExample output:\nSession: ses_abc123\nMessages: 45\nDate Range: 2025-12-20 to 2025-12-24\n\n[Message 1] user (2025-12-20 10:30:00)\nHello, can you help me with...\n\n[Message 2] assistant (2025-12-20 10:30:15)\nOf course! Let me help you with...",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "session_id": {
              "description": "Session ID to read",
              "type": "string"
            },
            "include_todos": {
              "description": "Include todo list if available (default: false)",
              "type": "boolean"
            },
            "include_transcript": {
              "description": "Include transcript log if available (default: false)",
              "type": "boolean"
            },
            "limit": {
              "description": "Maximum number of messages to return (default: all)",
              "type": "number"
            }
          },
          "required": [
            "session_id"
          ],
          "additionalProperties": false
        }
      }
    },
    "session_search": {
      "description": "Search for content within OpenCode session messages.\n\nPerforms full-text search across session messages and returns matching excerpts with context.\n\nArguments:\n- query (required): Search query string\n- session_id (optional): Search within specific session only (default: all sessions)\n- case_sensitive (optional): Case-sensitive search (default: false)\n- limit (optional): Maximum number of results to return (default: 20)\n\nExample output:\nFound 3 matches across 2 sessions:\n\n[ses_abc123] Message msg_001 (user)\n...implement the **session manager** tool...\n\n[ses_abc123] Message msg_005 (assistant)\n...I'll create a **session manager** with full search...\n\n[ses_def456] Message msg_012 (user)\n...use the **session manager** to find...",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "query": {
              "description": "Search query string",
              "type": "string"
            },
            "session_id": {
              "description": "Search within specific session only (default: all sessions)",
              "type": "string"
            },
            "case_sensitive": {
              "description": "Case-sensitive search (default: false)",
              "type": "boolean"
            },
            "limit": {
              "description": "Maximum number of results to return (default: 20)",
              "type": "number"
            }
          },
          "required": [
            "query"
          ],
          "additionalProperties": false
        }
      }
    },
    "session_info": {
      "description": "Get metadata and statistics about an OpenCode session.\n\nReturns detailed information about a session including message count, date range, agents used, and available data sources.\n\nArguments:\n- session_id (required): Session ID to inspect\n\nExample output:\nSession ID: ses_abc123\nMessages: 45\nDate Range: 2025-12-20 10:30:00 to 2025-12-24 15:45:30\nDuration: 4 days, 5 hours\nAgents Used: build, oracle, librarian\nHas Todos: Yes (12 items, 8 completed)\nHas Transcript: Yes (234 entries)",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "session_id": {
              "description": "Session ID to inspect",
              "type": "string"
            }
          },
          "required": [
            "session_id"
          ],
          "additionalProperties": false
        }
      }
    },
    "background_output": {
      "description": "Get output from background task. Use full_session=true to fetch session messages with filters. System notifies on completion, so block=true rarely needed. All timeouts are in milliseconds (ms), NOT seconds.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "task_id": {
              "description": "Task ID to get output from",
              "type": "string"
            },
            "block": {
              "description": "Wait for completion (default: false). System notifies when done, so blocking is rarely needed.",
              "type": "boolean"
            },
            "timeout": {
              "description": "Max wait time in ms (default: 60000, max: 600000)",
              "type": "number"
            },
            "full_session": {
              "description": "Return full session messages with filters (default: false)",
              "type": "boolean"
            },
            "include_thinking": {
              "description": "Include thinking/reasoning parts in full_session output (default: false)",
              "type": "boolean"
            },
            "message_limit": {
              "description": "Max messages to return (capped at 100)",
              "type": "number"
            },
            "since_message_id": {
              "description": "Return messages after this message ID (exclusive)",
              "type": "string"
            },
            "include_tool_results": {
              "description": "Include tool results in full_session output (default: false)",
              "type": "boolean"
            },
            "thinking_max_chars": {
              "description": "Max characters for thinking content (default: 2000)",
              "type": "number"
            }
          },
          "required": [
            "task_id"
          ],
          "additionalProperties": false
        }
      }
    },
    "background_cancel": {
      "description": "Cancel running background task(s). Use all=true to cancel ALL before final answer.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "taskId": {
              "description": "Task ID to cancel (required if all=false)",
              "type": "string"
            },
            "all": {
              "description": "Cancel all running background tasks (default: false)",
              "type": "boolean"
            }
          },
          "additionalProperties": false
        }
      }
    },
    "background_wait": {
      "description": "Wait for the next background task to complete from a set of task IDs. Returns as soon as ANY one finishes, with metadata-only completed_tasks array (task_id, description, status, duration_s, session_id, output_file_path). Use background_output to fetch full results. Call repeatedly with remaining IDs until all are done. All timeouts are in milliseconds (ms), NOT seconds — e.g. timeout=120000 for 2 minutes.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "task_ids": {
              "description": "Task IDs to monitor — returns when ANY one reaches a terminal state",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "timeout": {
              "description": "Max wait in ms. Default: 120000 (2 min). The tool returns immediately when any task finishes, so large values are fine.",
              "type": "number"
            }
          },
          "required": [
            "task_ids"
          ],
          "additionalProperties": false
        }
      }
    },
    "call_omo_agent": {
      "description": "Spawn explore/librarian agent. run_in_background REQUIRED (true=async with task_id, false=sync).\n\nAvailable: - explore: Specialized agent for explore tasks\n- librarian: Specialized agent for librarian tasks\n- oracle: Specialized agent for oracle tasks\n- hephaestus: Specialized agent for hephaestus tasks\n- metis: Specialized agent for metis tasks\n- momus: Specialized agent for momus tasks\n- multimodal-looker: Specialized agent for multimodal-looker tasks\n- athena-junior: Specialized agent for athena-junior tasks\n\nPass `session_id=<id>` to continue previous agent with full context. Nested subagent depth is tracked automatically and blocked past the configured limit. Prompts MUST be in English. Use `background_output` for async results.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "description": {
              "description": "A short (3-5 words) description of the task",
              "type": "string"
            },
            "prompt": {
              "description": "The task for the agent to perform",
              "type": "string"
            },
            "subagent_type": {
              "description": "The type of specialized agent to use for this task (explore or librarian only)",
              "type": "string"
            },
            "run_in_background": {
              "description": "REQUIRED. true: run asynchronously (use background_output to get results), false: run synchronously and wait for completion",
              "type": "boolean"
            },
            "session_id": {
              "description": "Existing Task session to continue",
              "type": "string"
            }
          },
          "required": [
            "description",
            "prompt",
            "subagent_type",
            "run_in_background"
          ],
          "additionalProperties": false
        }
      }
    },
    "switch_agent": {
      "description": "Switch the active session agent. After calling this tool, the session will transition to the specified agent with the provided context as its starting prompt. Use this to route work to another agent (e.g., Atlas for fixes, Prometheus for planning). The switch executes when the current agent's turn completes.\n\nPermanent one-way handoff. Use ONLY when you're the wrong agent for the overall job, NEVER for subtasks (use task()). Targets: atlas, prometheus, sisyphus, hephaestus.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "agent": {
              "description": "Target agent name to switch to (e.g., 'atlas', 'prometheus')",
              "type": "string"
            },
            "context": {
              "description": "Context message for the target agent — include confirmed findings, the original question, and what action to take",
              "type": "string"
            }
          },
          "required": [
            "agent",
            "context"
          ],
          "additionalProperties": false
        }
      }
    },
    "look_at": {
      "description": "Extract basic information from media files (PDFs, images, diagrams) when a quick summary suffices over precise reading. Good for simple text-based content extraction without using the Read tool. NEVER use for visual precision, aesthetic evaluation, or exact accuracy — use Read tool instead for those cases.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "file_path": {
              "description": "Absolute path to the file to analyze",
              "type": "string"
            },
            "image_data": {
              "description": "Base64 encoded image data (for clipboard/pasted images)",
              "type": "string"
            },
            "goal": {
              "description": "What specific information to extract from the file",
              "type": "string"
            }
          },
          "required": [
            "goal"
          ],
          "additionalProperties": false
        }
      }
    },
    "skill_mcp": {
      "description": "Invoke MCP server operations from skill-embedded MCPs. Requires mcp_name plus exactly one of: tool_name, resource_name, or prompt_name.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "mcp_name": {
              "description": "Name of the MCP server from skill config",
              "type": "string"
            },
            "tool_name": {
              "description": "MCP tool to call",
              "type": "string"
            },
            "resource_name": {
              "description": "MCP resource URI to read",
              "type": "string"
            },
            "prompt_name": {
              "description": "MCP prompt to get",
              "type": "string"
            },
            "arguments": {
              "description": "JSON string or object of arguments",
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "object",
                  "properties": {},
                  "additionalProperties": false
                }
              ]
            },
            "grep": {
              "description": "Regex pattern to filter output lines (only matching lines returned)",
              "type": "string"
            }
          },
          "required": [
            "mcp_name"
          ],
          "additionalProperties": false
        }
      }
    },
    "prepare_council_prompt": {
      "description": "Save a council analysis prompt to a temp file so council members can read it.\n\nAthena-only tool. Saves the prompt once, then each council member task() call uses a short\n\"Read <path>\" instruction instead of repeating the full question. This keeps task() calls\nfast and small.\n\nThe \"mode\" parameter controls whether council members can delegate exploration to subagents:\n- \"solo\" (default): Members do all exploration themselves. More thorough but uses more tokens.\n- \"delegation\": Members can delegate to explore/librarian agents. Faster, lighter context.\n\nThe \"intent\" parameter controls the analysis framework injected into the prompt:\n- \"DIAGNOSE\": Trace a specific problem to its root cause through systematic investigation.\n- \"AUDIT\": Find issues, risks, violations with severity ratings (broad sweep).\n- \"PLAN\": Define current state, target state, phased path.\n- \"EVALUATE\": Compare options against criteria, surface tradeoffs.\n- \"EXPLAIN\": Build understanding of mechanisms and relationships.\n- \"CREATE\": Produce a deliverable directly (code, prose, design, spec).\n- \"PERSPECTIVES\": Surface genuine viewpoints, argue each at its strongest, take a position.\n- \"FREEFORM\": No analytical framework imposed — respond naturally.\n\nIf no intent is specified, no analysis framework is injected.\n\nReturns the file path to reference in subsequent task() calls.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "prompt": {
              "description": "The full analysis prompt/question for council members",
              "type": "string"
            },
            "mode": {
              "description": "Analysis mode: \"solo\" (default) or \"delegation\"",
              "type": "string"
            },
            "intent": {
              "description": "Question intent: \"DIAGNOSE\", \"AUDIT\", \"PLAN\", \"EVALUATE\", \"EXPLAIN\", \"CREATE\", \"PERSPECTIVES\", \"FREEFORM\"",
              "type": "string"
            }
          },
          "required": [
            "prompt"
          ],
          "additionalProperties": false
        }
      }
    },
    "council_finalize": {
      "description": "Finalize council task outputs: extract COUNCIL_MEMBER_RESPONSE content from raw task output files, write per-member archive files, inject intent-specific Athena runtime guidance, and create meta.yaml.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "task_ids": {
              "description": "Array of background task IDs whose output files should be processed",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "name": {
              "description": "Council name used in the archive directory name",
              "type": "string"
            },
            "intent": {
              "description": "Classified question intent used for runtime Athena guidance injection. Valid intents: DIAGNOSE, AUDIT, PLAN, EVALUATE, EXPLAIN, CREATE, PERSPECTIVES, FREEFORM",
              "type": "string"
            },
            "question": {
              "description": "Original user question that triggered the council",
              "type": "string"
            },
            "prompt_file": {
              "description": "Path to the council prompt temp file (will be moved into the archive)",
              "type": "string"
            },
            "mode": {
              "description": "Council guidance mode: \"interactive\" (default) includes action paths with Question tool, \"non-interactive\" strips action paths for programmatic use",
              "type": "string"
            }
          },
          "required": [
            "task_ids",
            "name",
            "intent"
          ],
          "additionalProperties": false
        }
      }
    },
    "athena_council": {
      "description": "Launch all council members in parallel for multi-model analysis.\n\nTakes a prompt file path (from prepare_council_prompt) and launches all specified council members\nas background tasks. Returns an array of task IDs for use with background_wait and background_output.\n\nAvailable council members:\n- Opus (github-copilot/claude-opus-4.6)\n- Sonnet Creative (anthropic/claude-sonnet-4-6)\n- GPT 5.4 high (github-copilot/gpt-5.4)\n- GPT 5.4 mini (github-copilot/gpt-5.4-mini)\n- GPT 4o (github-copilot/gpt-4o)\n- Minimax Free (opencode/minimax-m2.5-free)\n- MiMo V2 Pro Free (opencode/mimo-v2-pro-free)\n- Step Flash Free (openrouter/stepfun/step-3.5-flash:free)\n- Gemini 3.1 PRO (github-copilot/gemini-3.1-pro-preview)\n- OpenCode GO GLM 5 (opencode-go/glm-5)\n- OpenCode GO Kimi 2.5 (opencode-go/kimi-k2.5)\n- OpenCode GO MiniMax 2.7 (opencode-go/minimax-m2.7)\n- Bailan QWEN3.5 Plus (bailian-coding-plan/qwen3.5-plus)\n- Bailan GLM 5 (bailian-coding-plan/glm-5)\n- Bailan Kimi 2.5 (bailian-coding-plan/kimi-k2.5)\n- Bailan MiniMax 2.5 (bailian-coding-plan/MiniMax-M2.5)\n\nReturns JSON with launched task IDs and any launch failures.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "prompt_file": {
              "description": "Path to the prompt file created by prepare_council_prompt",
              "type": "string"
            },
            "members": {
              "description": "Optional list of council member names to launch. Defaults to all configured members.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "prompt_file"
          ],
          "additionalProperties": false
        }
      }
    },
    "ctx_reduce": {
      "description": "Reduce context by dropping tagged content you no longer need.\nUse §N§ identifiers visible in conversation. The `drop` param accepts ranges: \"3-5\", \"1,2,9\", \"1-5,8\".\n\nCRITICAL RULES:\n- NEVER blanket-drop large ranges (e.g., \"1-50\"). Always review what each tag contains first.\n- Only drop tool outputs you have already processed and no longer need.\n- Protected tags are accepted but deferred until they leave the last protected range.\n- Keep recent context — only reduce OLD content that is no longer relevant to current work.\n- Dropped content is gone forever.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "drop": {
              "description": "Tag IDs to drop entirely. Ranges: '3-5', '1,2,9'",
              "type": "string"
            }
          },
          "additionalProperties": false
        }
      }
    },
    "ctx_expand": {
      "description": "Decompress a compartment range to see the original conversation transcript. Use start/end from <compartment start=N end=M> attributes. Returns the compacted U:/A: transcript for that message range, capped at ~15K tokens.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "start": {
              "description": "Start message ordinal (from compartment start attribute)",
              "type": "number"
            },
            "end": {
              "description": "End message ordinal (from compartment end attribute)",
              "type": "number"
            }
          },
          "required": [
            "start",
            "end"
          ],
          "additionalProperties": false
        }
      }
    },
    "ctx_note": {
      "description": "Save or inspect durable session notes that persist for this session.\nUse this for short goals, constraints, decisions, or reminders worth carrying forward.\n\nActions:\n- `write`: Append one note.\n- `read`: Show current notes.\n- `clear`: Remove all notes.\n\nHistorian reads these notes, deduplicates them, and rewrites the remaining useful notes over time.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "action": {
              "description": "Operation to perform. Defaults to 'write' when content is provided, otherwise 'read'.",
              "type": "string",
              "enum": [
                "write",
                "read",
                "clear"
              ]
            },
            "content": {
              "description": "Note text to store when action is 'write'.",
              "type": "string"
            }
          },
          "additionalProperties": false
        }
      }
    },
    "ctx_memory": {
      "description": "Manage cross-session project memories. Write new memories, delete stale ones, or search stored memories by category. Memories persist across sessions and are automatically injected into new sessions.\n\nSupported actions: write, delete, search, list, update, merge, archive.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "action": {
              "description": "Action to perform on memories",
              "type": "string",
              "enum": [
                "write",
                "delete",
                "search",
                "list",
                "update",
                "merge",
                "archive"
              ]
            },
            "content": {
              "description": "Memory content (required for write, update, merge)",
              "type": "string"
            },
            "category": {
              "description": "Memory category (required for write, optional filter for search/list, optional override for merge)",
              "type": "string"
            },
            "id": {
              "description": "Memory ID (required for delete, update, archive)",
              "type": "number"
            },
            "ids": {
              "description": "Memory IDs to merge (required for merge)",
              "type": "array",
              "items": {
                "type": "number"
              }
            },
            "query": {
              "description": "Natural language search query for project memories (required for search)",
              "type": "string"
            },
            "limit": {
              "description": "Maximum results to return for search/list (default: 10)",
              "type": "number"
            },
            "reason": {
              "description": "Archive reason (optional for archive)",
              "type": "string"
            }
          },
          "required": [
            "action"
          ],
          "additionalProperties": false
        }
      }
    },
    "aft_outline": {
      "description": "Get a structural outline of a source file, multiple files, or an entire directory — lists all top-level symbols with their kind, name, line range, and visibility. Use this to understand file/directory structure before editing.\nEach entry includes 'name', 'kind' (function/class/struct/heading/etc), 'range', 'signature', and 'members' (nested children like methods in classes or sub-headings in markdown).\nFor Markdown files (.md, .mdx): returns heading hierarchy — h1/h2/h3 as nested symbols with section ranges covering all content until the next same-level heading.\n\nProvide 'filePath', 'files', or 'directory'. Priority: directory > files > filePath. If multiple provided, highest-priority wins.\nDirectory mode skips commonly ignored directories and dot-prefixed directories.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "filePath": {
              "description": "Path to a single file to outline (ignored if 'files' or 'directory' is also provided)",
              "type": "string"
            },
            "files": {
              "description": "Array of file paths to outline in one call (ignored if 'directory' is also provided)",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "directory": {
              "description": "Path to a directory — outlines all source files under it recursively (capped at 200 files, takes priority over 'filePath' and 'files')",
              "type": "string"
            }
          },
          "additionalProperties": false
        }
      }
    },
    "aft_zoom": {
      "description": "Inspect code symbols with call-graph annotations. Returns the full source of named symbols with what they call and what calls them.\n\nUse this when you need to understand a specific function, class, or type in detail.\n\n**Modes:**\n\n1. **Inspect symbol** — pass filePath + symbol\n   Returns full source + call graph annotations.\n   Example: { \"filePath\": \"src/app.ts\", \"symbol\": \"handleRequest\" }\n\n2. **Inspect multiple symbols** — pass filePath + symbols array\n   Returns multiple symbols in one call.\n   Example: { \"filePath\": \"src/app.ts\", \"symbols\": [\"Config\", \"createApp\"] }\n\nFor Markdown files, use heading text as symbol name.\n\nMode priority: symbols array > single symbol.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "filePath": {
              "description": "Path to file (absolute or relative to project root)",
              "type": "string"
            },
            "symbol": {
              "description": "Name of a single symbol to inspect",
              "type": "string"
            },
            "symbols": {
              "description": "Array of symbol names to inspect in one call",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "contextLines": {
              "description": "Lines of context before/after the symbol (default: 3)",
              "type": "number"
            }
          },
          "required": [
            "filePath"
          ],
          "additionalProperties": false
        }
      }
    },
    "aft_safety": {
      "description": "File safety and recovery operations.\n\nPer-file undo stack is capped at 20 entries (oldest evicted).\n\nOps:\n- 'undo': Undo the last edit to a file. Requires 'filePath'. Note: pops from the undo stack (irreversible, no redo). Use 'history' to inspect before undoing.\n- 'history': List all edit snapshots for a file. Requires 'filePath'.\n- 'checkpoint': Save a named snapshot of tracked files. Requires 'name'. Optional 'files' to snapshot specific files only.\n- 'restore': Restore files to a previously saved checkpoint. Requires 'name'.\n- 'list': List all available named checkpoints. No extra params needed.\n\nEach op requires specific parameters — see parameter descriptions for requirements.\n\nUse checkpoint before risky multi-file changes. Use undo for quick single-file rollback.\n\nReturns: undo { path, backup_id }. history { file, entries }. checkpoint { ok, name }. restore { ok, name }. list { checkpoints }.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "op": {
              "description": "Safety operation",
              "type": "string",
              "enum": [
                "undo",
                "history",
                "checkpoint",
                "restore",
                "list"
              ]
            },
            "filePath": {
              "description": "File path (required for undo, history). Absolute or relative to project root",
              "type": "string"
            },
            "name": {
              "description": "Checkpoint name (required for checkpoint, restore)",
              "type": "string"
            },
            "files": {
              "description": "Specific files to include in checkpoint (optional, defaults to all tracked files)",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "op"
          ],
          "additionalProperties": false
        }
      }
    },
    "aft_import": {
      "description": "Language-aware import management. Supports TS, JS, TSX, Python, Rust, and Go.\n\nOps:\n- 'add': Add an import. Auto-detects group (stdlib/external/internal), deduplicates. Requires 'module'. Optional 'names', 'defaultImport', 'typeOnly'.\n- 'remove': Remove an import or a specific named import. Requires 'module'. Provide 'removeName' to remove a single named import; omit to remove the entire import.\n- 'organize': Re-sort and re-group all imports by language convention, deduplicate. Requires only 'filePath'.\n\nReturns:\n- Dry run (any op): { ok, dry_run, diff, syntax_valid? }\n- add: { file, added, module, group?, already_present?, formatted?, syntax_valid?, format_skipped_reason?, validation_errors?, validate_skipped_reason?, backup_id?, lsp_diagnostics? }\n- remove: { file, removed, module, name?, formatted, syntax_valid?, format_skipped_reason?, validation_errors?, validate_skipped_reason?, backup_id?, lsp_diagnostics? }\n- organize: { file, groups: [{ name, count }], removed_duplicates, formatted?, syntax_valid?, format_skipped_reason?, validation_errors?, validate_skipped_reason?, backup_id?, lsp_diagnostics? }",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "op": {
              "description": "Import operation",
              "type": "string",
              "enum": [
                "add",
                "remove",
                "organize"
              ]
            },
            "filePath": {
              "description": "Path to the file (absolute or relative to project root)",
              "type": "string"
            },
            "module": {
              "description": "Module path (required for add, remove — e.g. 'react', './utils', 'std::fmt')",
              "type": "string"
            },
            "names": {
              "description": "Named imports to add (e.g. ['useState', 'useEffect'])",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "defaultImport": {
              "description": "Default import name (e.g. 'React')",
              "type": "string"
            },
            "typeOnly": {
              "description": "Type-only import (TS only, default: false)",
              "type": "boolean"
            },
            "removeName": {
              "description": "Named import to remove for 'remove' op; omit to remove entire import",
              "type": "string"
            },
            "validate": {
              "description": "Validation level: 'syntax' (default) or 'full'. Syntax = tree-sitter parse check only. Full = also runs LSP type-checking (slower, catches more errors)",
              "type": "string",
              "enum": [
                "syntax",
                "full"
              ]
            },
            "dryRun": {
              "description": "Preview without modifying the file (default: false)",
              "type": "boolean"
            }
          },
          "required": [
            "op",
            "filePath"
          ],
          "additionalProperties": false
        }
      }
    },
    "ast_grep_search": {
      "description": "Search code patterns across filesystem using AST-aware matching. Supports 6 languages.\n\nUse meta-variables: $VAR matches a single AST node, $$$ matches multiple nodes (variadic).\nIMPORTANT: Patterns must be complete AST nodes (valid code fragments).\nFor functions, include params and body: 'export async function $NAME($$$) { $$$ }' not just 'export async function $NAME'.\n\nExamples: pattern='console.log($MSG)' lang='typescript', pattern='async function $NAME($$$) { $$$ }' lang='javascript', pattern='def $FUNC($$$): $$$' lang='python'\n\nReturns: Text summary — 'Found N match(es) across M file(s)' followed by file:line blocks with matched text and captured meta-variables.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "pattern": {
              "description": "AST pattern with meta-variables ($VAR, $$$). Must be complete AST node.",
              "type": "string"
            },
            "lang": {
              "description": "Target language",
              "type": "string",
              "enum": [
                "typescript",
                "tsx",
                "javascript",
                "python",
                "rust",
                "go"
              ]
            },
            "paths": {
              "description": "Paths to search (default: ['.'])",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "globs": {
              "description": "Include/exclude globs (prefix ! to exclude)",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "contextLines": {
              "description": "Number of context lines to show around each match",
              "type": "number"
            }
          },
          "required": [
            "pattern",
            "lang"
          ],
          "additionalProperties": false
        }
      }
    },
    "ast_grep_replace": {
      "description": "Replace code patterns across filesystem with AST-aware rewriting. Applies changes by default — set dryRun=true to preview.\n\nUse meta-variables in the rewrite pattern to preserve matched content from the pattern.\nIMPORTANT: Patterns must be complete AST nodes (valid code fragments).\n\nExample: pattern='console.log($MSG)' rewrite='logger.info($MSG)' lang='typescript' — replaces all console.log calls with logger.info across TypeScript files.\n\n**Warning: This tool modifies files directly.** Use dryRun=true to preview. Consider creating an aft_safety checkpoint before bulk replacements.\n\nReturns: Text summary — 'Replaced N match(es) across M file(s)' (or '[DRY RUN] Would replace...') followed by file:line blocks with before/after text.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "pattern": {
              "description": "AST pattern with meta-variables ($VAR, $$$). Must be complete AST node.",
              "type": "string"
            },
            "rewrite": {
              "description": "Replacement pattern (can use $VAR from pattern)",
              "type": "string"
            },
            "lang": {
              "description": "Target language",
              "type": "string",
              "enum": [
                "typescript",
                "tsx",
                "javascript",
                "python",
                "rust",
                "go"
              ]
            },
            "paths": {
              "description": "Paths to search (default: ['.'])",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "globs": {
              "description": "Include/exclude globs (prefix ! to exclude)",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "dryRun": {
              "description": "Preview changes without applying (default: false)",
              "type": "boolean"
            }
          },
          "required": [
            "pattern",
            "rewrite",
            "lang"
          ],
          "additionalProperties": false
        }
      }
    },
    "lsp_diagnostics": {
      "description": "Get errors, warnings, hints from a language server. Returns: { diagnostics: Array<{ file, line, column, end_line, end_column, severity, message, code }>, total: number, files_with_errors: number }.",
      "inputSchema": {
        "jsonSchema": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "type": "object",
          "properties": {
            "filePath": {
              "description": "Path to file to get diagnostics for. Provide 'filePath' for a single file, 'directory' for all files under a path, or omit both for all tracked files",
              "type": "string"
            },
            "directory": {
              "description": "Path to directory to get diagnostics for all files under it. Mutually exclusive with 'filePath'",
              "type": "string"
            },
            "severity": {
              "description": "Filter by severity — 'error', 'warning', 'information', 'hint', 'all' (default: 'all')",
              "type": "string",
              "enum": [
                "error",
                "warning",
                "information",
                "hint",
                "all"
              ]
            },
            "waitMs": {
              "description": "Wait N ms for fresh diagnostics before returning (max 10000, default: 0). Use after edits to let the server re-analyze.",
              "type": "number"
            }
          },
          "additionalProperties": false
        }
      }
    },
    "websearch_web_search_exa": {
      "description": "Search the web for any topic and get clean, ready-to-use content.\n\nBest for: Finding current information, news, facts, or answering questions about any topic.\nReturns: Clean text content from top search results, ready for LLM use.\n\nQuery tips: describe the ideal page, not keywords. \"blog post comparing React and Vue performance\" not \"React vs Vue\".\nIf highlights are insufficient, follow up with crawling_exa on the best URLs.",
      "inputSchema": {
        "jsonSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Natural language search query. Should be a semantically rich description of the ideal page, not just keywords."
            },
            "numResults": {
              "type": "number",
              "minimum": 1,
              "maximum": 100,
              "description": "Number of search results to return (must be a number, default: 8)"
            },
            "type": {
              "type": "string",
              "enum": [
                "auto",
                "fast"
              ],
              "description": "Search type - 'auto': balanced search (default), 'fast': quick results"
            },
            "freshness": {
              "type": "string",
              "enum": [
                "24h",
                "week",
                "month",
                "year",
                "any"
              ],
              "description": "How recent results should be. Only set when recency matters."
            },
            "includeDomains": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "List of domains to include in the search. If specified, results will only come from these domains."
            }
          },
          "required": [
            "query"
          ],
          "additionalProperties": false,
          "$schema": "http://json-schema.org/draft-07/schema#"
        }
      }
    },
    "grep_app_searchGitHub": {
      "description": "Find real-world code examples from over a million public GitHub repositories to help answer programming questions.\n\n**IMPORTANT: This tool searches for literal code patterns (like grep), not keywords. Search for actual code that would appear in files:**\n- ✅ Good: 'useState(', 'import React from', 'async function', '(?s)try {.*await'\n- ❌ Bad: 'react tutorial', 'best practices', 'how to use'\n\n**When to use this tool:**\n- When implementing unfamiliar APIs or libraries and need to see real usage patterns\n- When unsure about correct syntax, parameters, or configuration for a specific library\n- When looking for production-ready examples and best practices for implementation\n- When needing to understand how different libraries or frameworks work together\n\n**Perfect for questions like:**\n- \"How do developers handle authentication in Next.js apps?\" → Search: 'getServerSession' with language=['TypeScript', 'TSX']\n- \"What are common React error boundary patterns?\" → Search: 'ErrorBoundary' with language=['TSX']\n- \"Show me real useEffect cleanup examples\" → Search: '(?s)useEffect\\(\\(\\) => {.*removeEventListener' with useRegexp=true\n- \"How do developers handle CORS in Flask applications?\" → Search: 'CORS(' with matchCase=true and language=['Python']\n\nUse regular expressions with useRegexp=true for flexible patterns like '(?s)useState\\(.*loading' to find useState hooks with loading-related variables. Prefix the pattern with '(?s)' to match across multiple lines.\n\nFilter by language, repository, or file path to narrow results.",
      "inputSchema": {
        "jsonSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "The literal code pattern to search for (e.g., 'useState(', 'export function'). Use actual code that would appear in files, not keywords or questions."
            },
            "matchCase": {
              "type": "boolean",
              "description": "Whether the search should be case sensitive",
              "default": false
            },
            "matchWholeWords": {
              "type": "boolean",
              "description": "Whether to match whole words only",
              "default": false
            },
            "useRegexp": {
              "type": "boolean",
              "description": "Whether to interpret the query as a regular expression",
              "default": false
            },
            "repo": {
              "type": "string",
              "description": "Filter by repository.\n            Examples: 'facebook/react', 'microsoft/vscode', 'vercel/ai'.\n            Can match partial names, for example 'vercel/' will find repositories in the vercel org."
            },
            "path": {
              "type": "string",
              "description": "Filter by file path.\n            Examples: 'src/components/Button.tsx', 'README.md'.\n            Can match partial paths, for example '/route.ts' will find route.ts files at any level."
            },
            "language": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Filter by programming language.\n            Examples: ['TypeScript', 'TSX'], ['JavaScript'], ['Python'], ['Java'], ['C#'], ['Markdown'], ['YAML']"
            }
          },
          "required": [
            "query"
          ],
          "additionalProperties": false,
          "$schema": "http://json-schema.org/draft-07/schema#"
        }
      }
    },
    "context7_resolve-library-id": {
      "description": "Resolves a package/product name to a Context7-compatible library ID and returns matching libraries.\n\nYou MUST call this function before 'Query Documentation' tool to obtain a valid Context7-compatible library ID UNLESS the user explicitly provides a library ID in the format '/org/project' or '/org/project/version' in their query.\n\nEach result includes:\n- Library ID: Context7-compatible identifier (format: /org/project)\n- Name: Library or package name\n- Description: Short summary\n- Code Snippets: Number of available code examples\n- Source Reputation: Authority indicator (High, Medium, Low, or Unknown)\n- Benchmark Score: Quality indicator (100 is the highest score)\n- Versions: List of versions if available. Use one of those versions if the user provides a version in their query. The format of the version is /org/project/version.\n\nFor best results, select libraries based on name match, source reputation, snippet coverage, benchmark score, and relevance to your use case.\n\nSelection Process:\n1. Analyze the query to understand what library/package the user is looking for\n2. Return the most relevant match based on:\n- Name similarity to the query (exact matches prioritized)\n- Description relevance to the query's intent\n- Documentation coverage (prioritize libraries with higher Code Snippet counts)\n- Source reputation (consider libraries with High or Medium reputation more authoritative)\n- Benchmark Score: Quality indicator (100 is the highest score)\n\nResponse Format:\n- Return the selected library ID in a clearly marked section\n- Provide a brief explanation for why this library was chosen\n- If multiple good matches exist, acknowledge this but proceed with the most relevant one\n- If no good matches exist, clearly state this and suggest query refinements\n\nFor ambiguous queries, request clarification before proceeding with a best-guess match.\n\nIMPORTANT: Do not call this tool more than 3 times per question. If you cannot find what you need after 3 calls, use the best result you have.",
      "inputSchema": {
        "jsonSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "The question or task you need help with. This is used to rank library results by relevance to what the user is trying to accomplish. The query is sent to the Context7 API for processing. Do not include any sensitive or confidential information such as API keys, passwords, credentials, personal data, or proprietary code in your query."
            },
            "libraryName": {
              "type": "string",
              "description": "Library name to search for and retrieve a Context7-compatible library ID."
            }
          },
          "required": [
            "query",
            "libraryName"
          ],
          "$schema": "http://json-schema.org/draft-07/schema#",
          "additionalProperties": false
        }
      }
    },
    "context7_query-docs": {
      "description": "Retrieves and queries up-to-date documentation and code examples from Context7 for any programming library or framework.\n\nYou must call 'Resolve Context7 Library ID' tool first to obtain the exact Context7-compatible library ID required to use this tool, UNLESS the user explicitly provides a library ID in the format '/org/project' or '/org/project/version' in their query.\n\nIMPORTANT: Do not call this tool more than 3 times per question. If you cannot find what you need after 3 calls, use the best information you have.",
      "inputSchema": {
        "jsonSchema": {
          "type": "object",
          "properties": {
            "libraryId": {
              "type": "string",
              "description": "Exact Context7-compatible library ID (e.g., '/mongodb/docs', '/vercel/next.js', '/supabase/supabase', '/vercel/next.js/v14.3.0-canary.87') retrieved from 'resolve-library-id' or directly from user query in the format '/org/project' or '/org/project/version'."
            },
            "query": {
              "type": "string",
              "description": "The question or task you need help with. Be specific and include relevant details. Good: 'How to set up authentication with JWT in Express.js' or 'React useEffect cleanup function examples'. Bad: 'auth' or 'hooks'. The query is sent to the Context7 API for processing. Do not include any sensitive or confidential information such as API keys, passwords, credentials, personal data, or proprietary code in your query."
            }
          },
          "required": [
            "libraryId",
            "query"
          ],
          "$schema": "http://json-schema.org/draft-07/schema#",
          "additionalProperties": false
        }
      }
    }
  },
  "activeTools": [
    "question",
    "bash",
    "read",
    "glob",
    "grep",
    "edit",
    "write",
    "task",
    "webfetch",
    "todowrite",
    "skill",
    "gemini_quota",
    "session_list",
    "session_read",
    "session_search",
    "session_info",
    "background_output",
    "background_cancel",
    "background_wait",
    "call_omo_agent",
    "switch_agent",
    "look_at",
    "skill_mcp",
    "prepare_council_prompt",
    "council_finalize",
    "athena_council",
    "ctx_reduce",
    "ctx_expand",
    "ctx_note",
    "ctx_memory",
    "aft_outline",
    "aft_zoom",
    "aft_safety",
    "aft_import",
    "ast_grep_search",
    "ast_grep_replace",
    "lsp_diagnostics",
    "websearch_web_search_exa",
    "grep_app_searchGitHub",
    "context7_resolve-library-id",
    "context7_query-docs"
  ],
  "headers": {},
  "maxRetries": 0,
  "abort": {
    "present": false,
    "aborted": false
  },
  "experimentalTelemetry": {
    "isEnabled": false,
    "metadata": {
      "userId": "ufukaltinok",
      "sessionId": "ses_2fa8148b8ffeY1vcEeJKshru4H"
    }
  }
}