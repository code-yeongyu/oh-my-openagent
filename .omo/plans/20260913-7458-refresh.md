# PR #7823 local refresh: apply_patch plan

Goal: verified uncommitted integration of exact PR head c16b92b3ed6e92aae4c574fb53cb561f864bdf84 and origin/dev cfdaa1d16d25d6152410dea514fd978acfd62bab. No remote mutation, commit, or common user-config change.

## Patch units and verification

- [x] Create fresh task-owned suffixed worktree and unique maintenance branch from exact head; leave main checkout and all existing worktrees untouched.
- [x] Read ROADMAP, applicable AGENTS instructions, opencode-qa skill, checker source/test, entry-shape helper, config paths, historical harnesses and probe. Confirm remote dev equals local origin/dev via read-only ls-remote; confirm no upstream production-file changes since e0746bcbc.
- [x] Merge origin/dev with --no-commit --no-ff and command-only MoerAI identity. Preserve the three production files and regression test; no source patch expected. If conflict appears, inspect before any targeted patch.
- [x] Execute dependency/verification gates with Bun 1.4.0 and Node 24. Checker suite passed once (41 tests); adapter compiler and bundle passed. LSP rejected sibling-worktree paths. Install's implicit prepare triggered the root build before explicit diagnostics and failed on inherited submodule file-transport policy; full-build verification remains blocked, documented in evidence. No unrelated generated drift was staged.
- [x] Add .omo/evidence/20260913-7458-refresh/replay.mjs: deterministic real OpenCode server/SSE driver. Reuse historical probe source by resolving its root-relative imports while bundling. Isolate HOME, all XDG roots, TMPDIR and config with an allowlisted child environment. Subscribe to server output, SSE connected/session.created, and probe filesystem write before triggers, with bounded timeouts, never sleep/poll. Assert pinned tuple-preceded entry and local-dev tuple behavior. Capture host DB session counts using read-only sqlite before/after. No auth or personal config copied.
- [x] Add sanitized captures and README in the same evidence directory: commands, tool versions, precise results, isolation, limitations. Keep raw logs private. Historical scripts remain untouched because their polling, suppressed failures, inherited environment and missing assertions are unsuitable for new verification.
- [x] Audit new evidence content for machine-user paths, auth, personal configuration and skill identifiers; syntax-check replay and execute it. Stage only the merge integration plus explicitly audited plan/evidence. Check diff versus origin/dev, merge head, unresolved entries, and unstaged generated drift.

## Scope and stop condition

opencode-qa Case B (server/API/SSE) plus read-only host DB count. No model/provider/TUI behavior changes, so no live prompt or TUI needed. Probe exercises actual checker functions inside a real session.created handler; it does not prove npm update downloads or the whole plugin orchestration. Stop on verified integration or precise blocker within three materially different attempts. Preserve workspace and merge state.

`apply_patch` is not available on PATH in this child; this on-disk file specifies the exact patch units before merging. New evidence files use the available write tool; any existing-file change uses a targeted edit after rereading.
