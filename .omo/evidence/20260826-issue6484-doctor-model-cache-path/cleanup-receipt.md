# Cleanup receipt

Scope: worktree `/home/viprix/projects/oom-wt-6484` (branch `fix/6484-doctor-model-cache-path`),
QA sandbox `/tmp/opencode/issue-6484/`.

## Actions taken

1. Restored unintended churn: `packages/omo-codex/scripts/install-dist/install-local.mjs`
   (generated bundle regenerated by the interrupted `bun install` postinstall) ->
   `git checkout --`. Final diff contains exactly the 5 vertical files.
2. `bun install` in the worktree hung >300s AFTER populating node_modules (known
   network-restricted behavior); process killed via timeout. node_modules left populated
   for test runs; no further action needed (regenerable).
3. No background tasks/daemons spawned by this lane. The only real-`~/.omo` mutation observed
   during QA was `codegraph/projects/.../daemon.log`, written by a pre-existing host daemon,
   not by this lane; excluded and documented in `qa-isolation-omo-proof.txt`.
4. QA sandbox left in place under `/tmp/opencode/issue-6484/` (ephemeral tmp, contains only
   synthetic cache data + bun compile caches; no secrets). Reviewer may `rm -rf` it freely.
5. Real user state untouched: `~/.cache/oh-my-opencode/*`, `~/.cache/opencode`, `~/.omo`
   digests verified unchanged (see qa-isolation-* files).

## Not done (per mandate)

- No git commit / push / PR.
- No staging of `packages/shared-skills/upstreams/*` submodule churn (none touched).
- No tests weakened or deleted; 1 preservation-pin test added, 3 RED tests added.

## Final tree state

5 modified tracked files, all under the doctor/shared model-cache vertical:
- packages/omo-opencode/src/cli/doctor/checks/model-resolution-cache.ts
- packages/omo-opencode/src/cli/doctor/checks/model-resolution-cache.test.ts
- packages/omo-opencode/src/cli/doctor/checks/model-resolution-details.ts
- packages/omo-opencode/src/cli/doctor/checks/model-resolution-types.ts
- packages/omo-opencode/src/shared/connected-providers-cache.ts
