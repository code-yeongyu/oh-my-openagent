# Local gate summary

All references in this document are repository-relative to this evidence directory.

## Original repair evidence

- `red-fresh-candidate-retry.log`: unchanged code was RED (`Expected: 3`, `Received: 6`).
- `green-candidate-sweep.log`: 6 pass, 0 fail.
- `green-facts-failure-store.log`: 9 pass, 0 fail; this file contains 8 persistence cases plus 1 layout case.
- `package-memory-core.log`: 652 pass, 0 fail.
- `typecheck-full.log`: full typecheck passed.
- `build-full.log`: full build passed.
- `generated-bundle-check.log` and `generated-directive-check.log`: generated Senpi artifacts were current.
- `senpi-drive-live.json`: isolated real Senpi driver passed; host state checks passed.

## Post-Cubic scheduler evidence

- `red-resweep-scheduling.log`: before the scheduler repair, controlled-clock coverage failed because the aged candidate was never reswept and a non-sharing cleanup error did not rearm the next sweep.
- `green-resweep-scheduling.log`: after the repair, 7 pass, 0 fail. It proves immediate fresh cleanup is skipped, an aged sharing-blocked candidate is retried once at eligibility, the next immediate acquire is deferred again, a later healthy eligibility reclaims it, and a non-sharing throw rearms the next sweep.

## Post-merge verification

- `post-merge-evidence-path-resolution.txt`: canonical Senpi evidence resolver matched.
- `post-merge-focused.log`: 16 pass, 0 fail across the focused lock and facts suites.
- `post-merge-generated-bundle-check.log`: committed extension bundles are current.
- `post-merge-test-senpi.log`: 2,530 pass, 1 Windows-only skip, 0 fail.
- `post-merge-senpi-drive-self-test.log` and `post-merge-senpi-drive-live.json`: isolated real Senpi self-test and live driver passed with protected host state unchanged.

## Final non-sharing stale-sweep verification

- `red-nonsharing-stale-sweep.log`: controlled-clock RED showed an advisory stale `EIO` left the directory ineligible for its next acquire.
- `green-nonsharing-stale-sweep.log`: 8 pass, 0 fail after non-sharing stale failures rearm the scheduler while sharing failures preserve the age window.
- `final-focused.log`: 17 pass, 0 fail.
- `final-memory-core.log`: 654 pass, 0 fail.
- `final-typecheck.log`: full repository typecheck passed.
- `final-generated-bundle-build.log` and `final-generated-bundle-check.log`: bundled extensions were regenerated from source and are current.
- `final-test-senpi.log`: 2,530 pass, 1 Windows-only skip, 0 fail.
- `final-senpi-drive-self-test.log`, `final-senpi-drive-live.json`, and `final-driver-cleanup-results.txt`: exact-source isolated real Senpi QA passed with sandbox and child-process absence recorded.

## Full-root suite accounting

The local root-suite attempts are environmental diagnostics, not passing gates:
- Normal HOME: 17,155 pass + 9 skip + 5 fail = 17,169.
- Clean HOME: 17,144 pass + 9 skip + 16 fail = 17,169.

`full-suite-failure-summary.txt` lists every failure, every skip, and its cause without workstation paths. The authoritative Windows proof is CI run `33610291206`.
