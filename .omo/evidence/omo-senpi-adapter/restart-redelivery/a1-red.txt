bun test v1.4.0 (34cbb9a40)

packages/senpi-task/src/completion/notifier-reconcile-unnotified.test.ts:
(pass) createCompletionNotifier - reconcile unnotified terminal completions > #given a terminal notify_on_terminal record with an unnotified epoch w4notif #when the session reconciles twice #then exactly one notification is delivered [6.85ms]
116 |       parentState: { kind: "idle" },
117 |       parentTailInterrupted: true,
118 |     })
119 | 
120 |     // then
121 |     expect(parent.calls).toHaveLength(1)
                               ^
error: expect(received).toHaveLength(expected)

Expected length: 1
Received length: 0

      at <anonymous> (/Volumes/mengmotaStorage/local-workspaces/omo-wt/restart-wake-redelivery/packages/senpi-task/src/completion/notifier-reconcile-unnotified.test.ts:121:26)
(fail) createCompletionNotifier - reconcile unnotified terminal completions > #given a delivered-but-unconsumed terminal epoch w4notif #when the interrupted parent session reconciles #then it is redelivered exactly once [5.83ms]
(pass) createCompletionNotifier - reconcile unnotified terminal completions > #given a delivered-but-unconsumed terminal epoch w4notif #when a non-interrupted parent session reconciles #then the record is not redelivered [0.20ms]
(pass) createCompletionNotifier - reconcile unnotified terminal completions > #given a delivered-and-consumed terminal epoch w4notif #when an interrupted parent session reconciles #then the record is not redelivered [0.10ms]
167 |       parentState: { kind: "idle" },
168 |       parentTailInterrupted: true,
169 |     })
170 | 
171 |     // then
172 |     expect(parent.calls).toHaveLength(1)
                               ^
error: expect(received).toHaveLength(expected)

Expected length: 1
Received length: 0

      at <anonymous> (/Volumes/mengmotaStorage/local-workspaces/omo-wt/restart-wake-redelivery/packages/senpi-task/src/completion/notifier-reconcile-unnotified.test.ts:172:26)
(fail) createCompletionNotifier - reconcile unnotified terminal completions > #given a legacy delivered terminal epoch without consumed_epoch w4notif #when the parent session reconciles with an interrupted tail #then it is redelivered [0.17ms]
(pass) createCompletionNotifier - reconcile unnotified terminal completions > #given a legacy delivered terminal epoch without consumed_epoch w4notif #when the parent session reconciles without an interrupted tail #then it is not redelivered [0.07ms]
(pass) createCompletionNotifier - reconcile unnotified terminal completions > #given an already-notified epoch w4notif #when the session reconciles #then the record is skipped [0.08ms]
(pass) createCompletionNotifier - reconcile unnotified terminal completions > #given a terminal unnotified record of another session w4notif #when this session reconciles #then the record is skipped [0.10ms]
(pass) createCompletionNotifier - reconcile unnotified terminal completions > #given a terminal unnotified record without opt-in or failed delivery w4notif #when the session reconciles #then the record is skipped [0.10ms]
(pass) createCompletionNotifier - reconcile unnotified terminal completions > #given a legacy failed-delivery record without notify_on_terminal w4notif #when the session reconciles #then the in-flight retry is preserved [0.16ms]
(pass) createCompletionNotifier - reconcile unnotified terminal completions > #given delivery fails during reconcile w4notif #when the enqueue throws #then notification_failed_epoch is recorded and the retry is scheduled [0.41ms]
(pass) createCompletionNotifier - reconcile unnotified terminal completions > #given a completion buffered behind a transient parent state w4notif #when the session reconciles before the flush #then reconcile skips it and the flush delivers exactly once [0.53ms]
301 |     const { store, records, mutated } = fakeStore([matching, other])
302 |     const parent = capturingNotifier()
303 |     const completion = createCompletionNotifier({ notifier: parent.notifier, store })
304 | 
305 |     // when
306 |     completion.markConsumed({ sessionId: "session-a" })
                     ^
TypeError: createCompletionNotifier({ notifier: parent.notifier, store }).markConsumed is not a function. (In 'createCompletionNotifier({ notifier: parent.notifier, store }).markConsumed({ sessionId: "session-a" })', 'createCompletionNotifier({ notifier: parent.notifier, store }).markConsumed' is undefined)
      at <anonymous> (/Volumes/mengmotaStorage/local-workspaces/omo-wt/restart-wake-redelivery/packages/senpi-task/src/completion/notifier-reconcile-unnotified.test.ts:306:16)
(fail) createCompletionNotifier - reconcile unnotified terminal completions > #given delivered notifications for multiple sessions w4notif #when the matching session marks consumed #then only its notifications are stamped and all other fields survive [0.35ms]
(pass) createCompletionNotifier - reconcile unnotified terminal completions > #given a residency claim lands between read and notified-epoch persist w4notif #when the epoch is stamped #then the claim survives [0.12ms]
(pass) createCompletionNotifier - reconcile unnotified terminal completions > #given a residency claim lands before a failed-delivery persist w4notif #when the failure epoch is stamped #then the claim survives [0.13ms]

packages/senpi-task/src/store/record-parse.test.ts:
28 | 
29 |     // when
30 |     const record = parseTaskRecord(stored, "record.json")
31 | 
32 |     // then
33 |     expect(record.notification.consumed_epoch).toBe(1)
                                                    ^
error: expect(received).toBe(expected)

Expected: 1
Received: undefined

      at <anonymous> (/Volumes/mengmotaStorage/local-workspaces/omo-wt/restart-wake-redelivery/packages/senpi-task/src/store/record-parse.test.ts:33:48)
(fail) record-parse notification consumption > #given a persisted record with consumed_epoch #when parsed #then the consumption stamp round-trips [0.56ms]
(pass) record-parse notification consumption > #given a legacy record without consumed_epoch #when parsed #then it remains valid without a consumption stamp [0.10ms]
(pass) record-parse launch evidence > #given a lost record with started_at #when persisted JSON is parsed #then the task-level launch evidence round-trips [0.12ms]
(pass) record-parse launch evidence > #given a legacy pending record without started_at #when parsed #then it stays readable without inventing launch evidence [0.08ms]
(pass) record-parse launch evidence > #given a legacy running record without started_at #when parsed #then it stays readable without inventing launch evidence [0.08ms]
(pass) record-parse launch evidence > #given a legacy lost record without started_at #when parsed #then it stays readable without inventing launch evidence [0.07ms]
(pass) record-parse launch evidence > #given malformed started_at %s #when parsed #then the persisted record is rejected [0.10ms]
(pass) record-parse launch evidence > #given malformed started_at %s #when parsed #then the persisted record is rejected [0.06ms]
(pass) record-parse run_stats token totals > #given a persisted run_stats without the new token fields #when parsed #then the record round-trips and the new fields stay undefined [0.16ms]
(pass) record-parse run_stats token totals > #given a run_stats with all four token totals and status fields #when parsed #then every value round-trips exactly [0.10ms]
(pass) record-parse run_stats token totals > #given a run_stats carrying an unknown token_status #when parsed #then the record is rejected [0.09ms]
(pass) record-parse run_stats token totals > #given a run_stats carrying non-numeric token totals #when parsed #then the record is rejected [0.08ms]
(pass) record-parse task ordinals and background mode > #given a persisted record with task_seq, config_generation and background_mode #when parsed #then all three round-trip exactly [0.08ms]
(pass) record-parse task ordinals and background mode > #given a persisted record with an unknown background_mode #when parsed #then the record is rejected [0.09ms]

4 tests failed:
(fail) createCompletionNotifier - reconcile unnotified terminal completions > #given a delivered-but-unconsumed terminal epoch w4notif #when the interrupted parent session reconciles #then it is redelivered exactly once [5.83ms]
(fail) createCompletionNotifier - reconcile unnotified terminal completions > #given a legacy delivered terminal epoch without consumed_epoch w4notif #when the parent session reconciles with an interrupted tail #then it is redelivered [0.17ms]
(fail) createCompletionNotifier - reconcile unnotified terminal completions > #given delivered notifications for multiple sessions w4notif #when the matching session marks consumed #then only its notifications are stamped and all other fields survive [0.35ms]
(fail) record-parse notification consumption > #given a persisted record with consumed_epoch #when parsed #then the consumption stamp round-trips [0.56ms]

 25 pass
 4 fail
 58 expect() calls
Ran 29 tests across 2 files. [1.94s]
