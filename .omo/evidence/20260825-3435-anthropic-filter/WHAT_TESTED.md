# WHAT WAS TESTED

Surface: outbound chat payload strings generated by the OpenCode plugin and
injected toward provider APIs (Anthropic in scope for #3435).

1. RED (pre-fix), `red-run.txt`:
   - `bun test packages/omo-opencode/src/shared/system-directive.test.ts`
     failed: regression suite imports not-yet-existing exports, proving tests
     were wired against unfixed code.
   - Direct evaluation of the real module pre-fix confirmed every exported
     directive type rendered a payload containing the trigger literal
     (`OH-MY-OPENCODE`, case-insensitive match of `opencode`).
2. GREEN (post-fix), `green-run.txt`:
   - `bun test` over system-directive + every consumer hook dir
     (todo-continuation-enforcer, keyword-detector, atlas,
     sisyphus-junior-notepad, compaction-context-injector, prometheus-md-only,
     ralph-loop): 680 pass / 0 fail / 1451 expect() calls.
3. Typecheck, `typecheck.txt`:
   - `tsgo --noEmit -p packages/omo-opencode/tsconfig.json`: exit 0, no output.

Behavior proven by new co-located given/when/then tests:
- Shipped prefix and all 7 directive payloads contain no `opencode` substring.
- Recognition parity: current-format AND legacy-format directives (with and
  without leading `ultrawork` keyword) are still recognized by
  `isSystemDirective`, so directives persisted in in-flight sessions keep being
  skipped after upgrade.
- Containment parity: double-injection guards detect both formats via the new
  `containsSystemDirective`.
