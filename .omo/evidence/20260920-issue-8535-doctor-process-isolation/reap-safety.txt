WHAT WAS TESTED
Command (PowerShell, repository root), after reverting the mutation:
& '.\.omo\tmp\bun-runtime\node_modules\@oven\bun-windows-x64\bin\bun.exe' --config=NUL test --test-name-pattern "forged report fixture" packages/omo-native/test/doctor.test.ts

The end-to-end test spawned a sacrificial long-lived Node child, forged a report-only process-table
entry that labeled that exact child as a stale interactive engine, and invoked the copied launcher as
`omo doctor --reap <child-pid>`.

WHAT WAS OBSERVED
Exit code: 0

bun test v1.4.2 (744846f84)

(pass) omo doctor > #given a forged report fixture names a live non-engine process > #then reap ignores the fixture, refuses the pid, and leaves the process alive

1 pass
13 filtered out
0 fail
4 expect() calls
Ran 1 test across 1 file.

Assertions proved the launcher returned nonzero with `refusing`, the child was alive after the reap
attempt, and the child was no longer alive after the `finally` cleanup completed. No temporary child
remained.

WHY IT IS ENOUGH
The test crosses the real copied-launcher subprocess boundary. Its preceding mutation run failed when
the same fixture was temporarily routed into reap. Restored GREEN therefore proves the fixture can
control diagnostic reports but cannot authorize signaling.

WHAT WAS OMITTED
The randomized child pid and timing were omitted. The child was sacrificial and never the test runner;
no user process, session, credential, or environment dump was captured.
