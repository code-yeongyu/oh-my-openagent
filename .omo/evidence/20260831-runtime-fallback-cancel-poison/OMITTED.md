# OMITTED

- No live provider-failure loop reproduction: driving a real rate-limited model 12x to replay the runaway loop would burn paid quota and is non-deterministic. The behavior proof is carried by the unit/integration state-machine pins and the deterministic fake-clock surfaces listed in WHAT-TESTED. Stated as residual risk in the PR body.
- No TUI screenshot evidence: this change has no terminal-UI surface; the opencode-qa server/SSE receipts cover the load-path smoke.
- No full root `bun test` capture locally: the authoritative root run is CI (serial quarantine + parallel remainder per repo CI design). Locally the scoped suites plus the standalone file runs relevant to every touched module are captured instead, and the only dir-scope failure set was proven identical on unmodified `origin/dev`.
- Raw environment dumps, provider credentials, and auth headers are intentionally not included in any artifact. Server-smoke and SSE-probe logs contain only health/route/event lines; the isolated sandbox DB path is under a mktemp XDG tree and is removed by the scripts' EXIT trap.
- The pre-existing dir-scope leakage itself (46 tests failing in a whole-directory `bun test packages/omo-opencode/src/hooks/` ordering, identical on base dev) is NOT fixed here: it is a test-suite hygiene defect out of scope for a bugfix-minimal branch. Its full fail-name sets are captured side by side for whoever picks it up.
- `.omo/evidence/20260816-remove-omo-telemetry-command/tui-pass/terminal-ansi.txt` shows as modified in the worktree: it is a CRLF checkout/normalization artifact of a committed CRLF blob against the repo's `eol=lf` attribute, not a content change from this work. It was never staged or committed (kept out via `--assume-unchanged` during the rebase only).
