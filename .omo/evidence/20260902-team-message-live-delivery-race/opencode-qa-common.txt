Command: bash .agents/skills/opencode-qa/scripts/lib/common.sh --self-check
Result: pass.
- Required dependencies are present.
- The protected host database was read only; its path is redacted.
- The helper created an isolated XDG sandbox, placed HOME and OPENCODE_TEST_HOME inside it, preserved the OpenCode shim, and removed the sandbox on exit.
