# Issue #6657 profile CLI QA evidence

Verdict: PASS for the scoped profile CLI change on the frozen implementation tree described below.

## Frozen tree

- Base commit: `038ed0cbbefe2b40677b63867aeea0d16bc303e0`.
- Rebased implementation head: `8c4b764bcfda7f3728230634e1e515e859783966`.
- The original generated-bundle commit was replaced after rebase by an official `build:senpi-plugin:stage` regeneration, so no conflict markers or hand-edited minified output remain.
- Revalidated on `2026-08-14` after rebasing onto the current `dev` head.
- `git diff --check`: exit 0.

## What was tested

The automated gate record is in `automated-gates.md`: 46 focused tests passed with 0 failures, both affected package typechecks passed, the OMO schema regenerated byte-for-byte, and the real CLI bundle built successfully.

The actual built product entrypoint was driven with `npx --yes bun dist/cli/index.js profile ...` under an isolated temporary HOME, USERPROFILE, project, and all four XDG directories. `real-cli.json` contains sanitized commands, exit codes, stdout/stderr, before/after JSONC, file hashes, assertions, host-isolation metadata, and cleanup receipt. `qa-runner.ps1` is the reproducible runner.

Observed real-surface behavior:

- Unsorted `kimi`/`gpt` definitions listed as `gpt`, then `kimi`.
- `use gpt` exited 0, preserved the JSONC comment, and wrote only user `active_profile`.
- `current` reported persisted `gpt`; `OMO_PROFILE=kimi` took precedence.
- `clear` removed only persisted selection, preserved the comment, and left `OMO_PROFILE=kimi` effective.
- A missing environment-selected profile emitted a diagnostic but reported base config as effective.
- A project-only profile was labeled and rejected for global persistence with exit 1 and no user-config change.
- Blank and whitespace-only `use` values exited 1 with no user-config change.
- Whitespace-only `active_profile` in config exited 1 with no stdout and a validation diagnostic.
- Malformed JSONC exited 1 with no misleading stdout and a parse diagnostic.

All 27 real-surface assertions passed. Expected-success scenarios exited 0; the five expected-negative invocations exited 1.

## Isolation and cleanup

- Real host `~/.omo/omo.jsonc` contents were never read into evidence or copied. It did not exist before or after, so metadata was unchanged.
- `opencode` was absent before and after. Consequently, an OpenCode DB path/checksum/session count was not available or applicable. The product CLI itself was the real surface required by the Windows fallback.
- The sandbox used `%TEMP%/omo-qa-6657-*`; the resolved path was checked to be a dedicated child of the system temp directory before recursive removal.
- Cleanup receipt: sandbox removed, every child process exited, and a final scan found 0 matching temp roots.

## Why this is sufficient

Focused unit/integration tests cover loader and schema precedence, config-chain integration, Commander registration, writer preservation, and fixed error branches. The built-product transcript independently proves the user-observable CLI lifecycle and all review-found negative cases through the shipped entrypoint. Schema reproducibility, package typechecks, diff whitespace checks, host metadata, and cleanup checks cover the main adjacent regression and isolation risks.

## Omitted or redacted

- No tokens, credential values, environment dumps, auth headers, or host config contents were captured.
- Temporary and workspace absolute paths are replaced with `%QA_ROOT%` and `%WORKSPACE%`.
- npm's one-time upgrade notice in the first child invocation is incidental tooling stderr and is retained as captured; it contains no private data.
- Known broader Windows-only symlink permission and POSIX `rm` tooling failures are separated in `automated-gates.md` and are not represented as product failures.
