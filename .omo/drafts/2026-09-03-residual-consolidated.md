# Residual task consolidation

Source: `plans/omo-agent-toolkit-rename.md`

- [ ] 1. Root CLI: bin map gains `omo-agent-toolkit`, drops `omo`, plus OMO_EDITION edition routing (breaking) (source: omo-agent-toolkit-rename.md)
- [ ] 2. senpi adapter: toolkit-first `resolveOmoBin` chain with `.js`-target-aware spawn (source: omo-agent-toolkit-rename.md)
- [ ] 3. Codex: linker/wrapper/bootstrap/doctor rename with marker-guarded legacy `omo` DELETION (source: omo-agent-toolkit-rename.md)
- [ ] 4. Agent-facing command strings: migrate `omo ulw-loop|boulder` emit sites via a checked-in categorized audit (source: omo-agent-toolkit-rename.md)
- [ ] 5. Release surfaces: publish.yml codex smoke assertions + docs + CHANGELOG (source: omo-agent-toolkit-rename.md)
- [ ] 6. Aggregate proof: isolated install smokes on both editions + full gates (source: omo-agent-toolkit-rename.md)
- [ ] F1. Plan compliance audit (source: omo-agent-toolkit-rename.md)
- [ ] F2. Code quality review (source: omo-agent-toolkit-rename.md)
- [ ] F3. Real manual QA (source: omo-agent-toolkit-rename.md)
- [ ] F4. Scope fidelity (source: omo-agent-toolkit-rename.md)
- [ ] Fresh packed-root global install (isolated prefix) links EXACTLY five bins {oh-my-opencode, oh-my-openagent, omo-agent-toolkit, lazycodex, lazycodex-ai} on the ROOT package (the lazycodex-ai package keeps its own two-alias map, unchanged vs its baseline) and `omo` is ABSENT; `omo-agent-toolkit --version` exits 0 with the version on stdout; the postinstall rename line is exercisable with `--foreground-scripts` while an ordinary install completes with no output dependency (npm >=7 hides lifecycle output by default - the notice is best-effort; the reliable migration surface is the CHANGELOG/docs/README). Evidence: task-6 (+task-1 baselines), task-5 (postinstall test). (source: omo-agent-toolkit-rename.md)
- [ ] The FOUR surviving aliases are byte-identical-after-normalization on STDOUT and exit codes vs pre-change characterization baselines (`--version`, `doctor --help`): the ONLY allowed delta is the todo-4 rename map applied to emitted usage/help strings (e.g. cli-program.ts:250's `$ omo doctor ...` example), each transitioning exactly old -> new. No deprecation/removal notice branch exists anywhere in the CLI; `jq -e .bin.omo package.json` exits 1. Evidence: task-1, task-4. (source: omo-agent-toolkit-rename.md)
- [ ] Upgrade-in-place is proven, not assumed: installing the new package over a prefix that already had the old one leaves NO `omo` bin and adds `omo-agent-toolkit`. Evidence: task-6(a2). (source: omo-agent-toolkit-rename.md)
- [ ] Codex isolated install produces `omo-agent-toolkit` (marker + OMO_INVOCATION_NAME + OMO_EDITION exports) and NO `omo`; a planted marker-bearing `omo` is DELETED; a planted unmarked user `omo` survives byte-identical; bin-set diff vs baseline is exactly {+omo-agent-toolkit(.cmd), -omo(.cmd)}; hooks.json byte-identical; second run idempotent. Existing installs need zero manual steps (installer AND session-start bootstrap share linkRootRuntimeBin). Evidence: task-3, task-6. (source: omo-agent-toolkit-rename.md)
- [ ] Doctor is correct in every install state: fresh/migrated installs pass with the new expected bin set; a leftover marker-bearing `omo` produces a warning with a reinstall fix; an unmarked user-owned `omo` produces silence; a codex-installed `omo-agent-toolkit doctor` reports the CODEX edition via OMO_EDITION. Evidence: task-1 (routing units), task-3 (doctor tests), task-6(b) (real run). (source: omo-agent-toolkit-rename.md)
- [ ] The categorized agent-command audit is GREEN for BOTH regexes (agent commands and human commands) with zero remaining `emit-migrate`/`test-expectation` entries - only `input-compat-preserve` + `docs` entries remain; every emitted instruction says `omo-agent-toolkit ...`; every old-form input still parses identically (equivalence table). Evidence: task-4. (source: omo-agent-toolkit-rename.md)
- [ ] publish.yml smoke asserts the canonical bin AND the ABSENCE of `omo`, plus the marker-bearing-removal / unmarked-preservation upgrade simulation; actionlint clean - the user's next release run passes without publish-side edits. Evidence: task-5 + post-merge release run. (source: omo-agent-toolkit-rename.md)
- [ ] Docs and CHANGELOG carry the full removal story: no doc suggests bare `omo` as a command anywhere (both audit regexes green over docs), README's third-party-`omo` warning is rewritten, CHANGELOG has a BREAKING entry with the migration one-liner, the auto-migration notes, and the one-time in-flight-agent note; no shell alias is ever recommended. Evidence: task-5 docs diff + reviewer-read notes. (source: omo-agent-toolkit-rename.md)
- [ ] `bun test`, `bun run typecheck`, `bun run test:codex` green on the final tree; the diff contains NO omo-ai/omo-native surfaces (scope fidelity). Evidence: task-6. (source: omo-agent-toolkit-rename.md)
- [ ] Naming end-state documented: CLI family = `omo-agent-toolkit` everywhere; the freed `omo` name is reserved for the future native senpi edition (`omo-ai` = senpi + packages/*senpi*, per the mengmotaMac source plan) and stays reserved in RESERVED_NESTED_BIN_NAMES - documented as context only, nothing shipped. Evidence: task-5 docs diff, task-3 (reserved-set test). (source: omo-agent-toolkit-rename.md)
