# task-e2e live run (2026-08-23, senpi 2026.8.22-2)

`SENPI_BIN=.../dist/senpi` `realSenpiUntouched=true`

Result: FAIL (1/26). `task_output_peek` PASS. Remaining: `resume_finished_steerable` (`task_send` → `not_continuable`, no live handle for the finished child at first tool).

Print-mode resume no longer fails `already processing` after:
- infer print mode from `--mode json` when session_start omits mode
- skip immediate liveness notify on print-mode session_start
- defer `reconcileOnSessionStart` to `before_agent_start` so `-p` owns the first turn
