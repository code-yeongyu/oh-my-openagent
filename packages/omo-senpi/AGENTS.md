# omo-senpi

Senpi harness adapter for oh-my-openagent.

This package is a Wave 1 scaffold. Keep it adapter-only: it may depend on core packages, but core packages must not import Senpi or this adapter.
